# @headless-three/renderer Compatibility Matrix

This matrix is the public support contract for the current renderer. The package accepts normal Three.js scene objects and renders through the TypeScript adapter, N-API, and native `wgpu`; it does not provide a WebGL context or emulate `THREE.WebGLRenderer`.

Status keys:

- **Supported**: implemented and covered by smoke or conformance tests.
- **Partial**: implemented for common cases, with known gaps listed here.
- **Unsupported**: not implemented; scenes should be preprocessed or should expect clear documented gaps.

The committed Khronos fixture corpus is tracked separately in
[Khronos glTF Sample Asset Coverage](./gltf-sample-assets.md), with a docs
consistency test keeping that list synchronized with `gltf.test.mjs`.

## Public API

| Feature | Status | Notes |
|---|---|---|
| `render(scene, camera, options)` / `renderAsync(scene, camera, options)` | Supported | Accepts a `THREE.Scene` or `THREE.Object3D` root and returns PNG by default or raw RGBA when `options.format` is `'rgba'`; `renderAsync()` is a Promise-returning compatibility wrapper around the same scene-output contract. Malformed scene/children/camera/userData/option containers, unsupported output formats, invalid explicit or aspect-derived dimensions, and invalid camera clipping ranges or matrix containers/values fail clearly. |
| `Renderer` reusable instance | Supported | Reuses the native renderer object across calls and supports target, output-size, drawing-buffer-size, pixel-ratio, inert `domElement` output/client-size/style mirroring plus style-property, attribute, event-target, and canvas-export probes, clear-color/depth/stencil value state, output color-space, tone-mapping state, reusable global and material-local clipping state, viewport/scissor, `shadowMap`, inert WebGLInfo/CommonRenderer `info`, `debug`, and `inspector` compatibility state, inert `xr` enabled/cameraAutoUpdate/framebuffer-scale/controller/reference-space/session/base-layer/binding/frame/environment/depth-texture/depth/foveation/camera-texture/event-target dispatch probes with real session binding failing clearly, validated inert WebGLRenderer-style constructor parameters for common context attributes with cloned `getContextAttributes()`, read-only `alpha`/`depth`/`stencil`/`logarithmicDepthBuffer` metadata, CommonRenderer-style `isRenderer`/`initialized` readiness probes and `highPrecision=false` metadata with high-precision enablement failing clearly, and default output-buffer-type getter readback, and documented resource-init/readiness/clear/auto-clear/dispose compatibility state. |
| `renderToTarget()` / `options.target` / `Renderer.setRenderTarget()` | Supported | Populates non-array target-like `{ width, height, data, texture.image.data, texture.source.data }` fields for a color output, including `target.texture`, target texture arrays, `target.textures`, MRT-shaped targets, and reusable-renderer target state through `Renderer.setRenderTarget(target); renderer.render(...)` for regular targets plus typed single-attachment target arrays, marking written textures `needsUpdate`. `Renderer.readRenderTargetPixels()` and `readRenderTargetPixelsAsync()` copy stored CPU color data from regular targets, explicit cube target faces, and selected color attachment indices into caller-provided buffers; async readback can allocate a matching output buffer and accepts the common-renderer `(target, x, y, width, height, textureIndex, faceIndex)` argument shape. Top-level `target.data` remains RGBA8; color textures receive Alpha/Red/RG/RGB/RGBA and RedIntegerFormat/RGIntegerFormat/RGBIntegerFormat/RGBAIntegerFormat channel data by requested format, normalized `Float32Array` data for `THREE.FloatType`, signed or unsigned normalized integer arrays for `ByteType`/`ShortType`/`IntType` and `UnsignedShortType`/`UnsignedIntType`, packed `Uint16Array` data for `UnsignedShort4444Type`, `UnsignedShort5551Type`, packed `Uint32Array` RGB9_E5 data for `UnsignedInt5999Type`, packed `Uint32Array` R11F_G11F_B10F data for `UnsignedInt101111Type`, or `Uint16Array` half-float data for `HalfFloatType`, including through `options.target`. Regular-camera, ArrayCamera, and CubeCamera MRT-shaped targets can populate secondary color textures with explicit multi-pass auxiliary outputs when each secondary texture declares `texture.userData.headlessThreeRenderer.renderMode` as `"color"`, `"mask"`, `"object-id"`, `"normal"`, or `"depth"`; `Renderer.getMRT()` returns `null`, `Renderer.setMRT(null)` is accepted as a clear operation, and unhinted secondary attachments or arbitrary native MRT shader outputs including non-null `Renderer.setMRT()` fail clearly. Object-id renders populate `objectIdEntries`/`objectIdMap` through target APIs, including reusable renderer target state and auxiliary object-id attachments, and clear stale metadata on later color renders. Target `depthTexture` objects receive normalized depth readback with image/source writeback, including `FloatType`, `HalfFloatType`, and packed `UnsignedInt248Type` depth through reusable renderer target state, scissor clipping, base-texture and alpha-map alpha-tested cutouts, `alphaHash` cutouts, and transparent material default/explicit `depthWrite` behavior; `THREE.FloatType` depth textures receive scalar `Float32Array` data, `HalfFloatType` receives `Uint16Array` half-float data, `UnsignedByteType`/`UnsignedShortType`/`UnsignedIntType` depth textures receive scalar unsigned typed arrays, `UnsignedInt248Type` receives `Uint32Array` data with normalized depth24 in the high bits and zero stencil bytes, and plain depth target objects receive RGBA8 bytes. 4x MSAA resolves into target readback buffers, including reusable renderer target state; malformed targets, target image containers, nested target texture/mipmap/source containers, malformed target scissor flags, array/3D target texture objects, compressed target texture objects or compressed target format constants, regular-camera cube target texture objects, unsupported sample counts, unsupported color target texture format/type requests, and unsupported depth texture type/format pairings fail clearly. |
| Post-processing options | Supported | Exposure, contrast, saturation, vignette, grayscale, invert, and `enabled=false` opt-out, with direct conformance for exposure, contrast, saturation, vignette, boolean grayscale, invert, and bypass output paths; invalid effect values fail clearly. |
| `Renderer.setEffects()` | Partial | Accepted as a validated no-op WebGLRenderer compatibility hook for no argument, `null`, or arrays. Actual effects are controlled by per-render `options.postProcessing`, and malformed effect containers fail clearly. |
| `Renderer.transmissionResolutionScale` | Partial | Stored as positive finite WebGLRenderer compatibility state with default `1`; invalid values fail clearly, and the value scales the scene-color texture sampled by physical transmission. Exact WebGLRenderer edge-case parity for downscaled transmission remains planned. |
| `Renderer.isWebGLRenderer` | Partial | Exposed as `true` so Three.js helpers can select WebGLRenderer-compatible readback branches, with direct coverage for `LightProbeGenerator.fromCubeRenderTarget()` reading renderer-owned cube targets, standard CopyShader `ShaderPass`/`TexturePass`/`SavePass` fullscreen flows, `OutputPass` fullscreen output-copy flow, CopyShader additive accumulation through `SSAARenderPass` and low-opacity `TAARenderPass` samples, WebGLCubeRenderTarget and CommonRenderer `CubeRenderTarget` clear-state flows, `StereoEffect` scissored eye-camera renders, `Reflector`/`Refractor` helper-owned target prepasses, and `EXRExporter`/`KTX2Exporter` exporting renderer-owned targets. `ClearMaskPass` can touch the inert stencil state, but this marker does not imply a browser WebGL context: `MaskPass`, `Renderer.getContext()`, and direct WebGL paths still fail clearly. |
| `Renderer.state` | Partial | Exposes inert `state.buffers.color.setMask()`/`setLocked()`/`setClear()`, `state.buffers.depth.getReversed()`/`setTest()`/`setMask()`/`setFunc()`/`setLocked()`/`setClear()`, and `state.buffers.stencil.setTest()`/`setMask()`/`setFunc()`/`setOp()`/`setLocked()`/`setClear()` probes, plus state-level blending/material/flip-sided/cull-face/line-width/polygon-offset/scissor-test/scissor/viewport probes and no-op `state.reset()`/`state.unbindTexture()` hooks. Raw WebGL binding/upload state methods fail clearly. Broader WebGL state-buffer control remains outside the scene-oriented contract. |
| `Renderer.shadowMap.transmitted` | Partial | Stored as validated boolean CommonRenderer/WebGLRenderer compatibility state with default `false`; invalid values fail clearly. It is currently state-only because transmitted shadow-map passes are not modeled separately. |
| `Renderer.debug.getShaderAsync()` | Unsupported | Present as a validated async clear-failure probe for CommonRenderer compatibility. It validates scene, camera, and object inputs, then rejects because generated backend shader source is not exposed by the scene-oriented native renderer. |
| `Renderer.onDeviceLost` | Partial | Exposed as assignable callback state for CommonRenderer compatibility. The default callback is a no-op and invalid assignments fail clearly; native device-loss events are not surfaced by the scene-oriented renderer. |
| Low-level direct rendering hooks | Unsupported | `Renderer.renderBufferDirect()`, `Renderer.renderObject()`, non-null `Renderer.setRenderObjectFunction()`, non-null `Renderer.setOutputRenderTarget()`/`setCanvasTarget()`, direct `Renderer.setTexture2D()`/`setTextureCube()`/`setTextureCubeDynamic()`/`setTexture3D()`/`setTexture2DArray()`, `Renderer.copyTextureToTexture3D()`, `Renderer.compute()`/`computeAsync()`, `Renderer.getArrayBufferAsync()`, `Renderer.resolveTimestampsAsync()`, and `Renderer.waitForGPU()` calls are present only to fail clearly: low-level WebGL buffer binding, renderer-internal render-object dispatch, direct material program dispatch, backend-owned common-renderer output/canvas targets, browser WebGL texture units, direct texture binding, backend texture-layer copies, WebGPU compute pipelines, storage-buffer readback, timestamp query pools, and direct GPU synchronization are outside the scene-oriented contract. `Renderer.getRenderObjectFunction()` returns `null`, `Renderer.setRenderObjectFunction(null)` is accepted as a clear operation, `Renderer.getOutputRenderTarget()`/`getCanvasTarget()` return `null`, and `Renderer.setOutputRenderTarget(null)`/`setCanvasTarget(null)` are accepted as clear operations. Use `Renderer.render()` or `renderToTarget()`, and provide textures through material, background, environment, or render-target texture inputs. |
| WebGLRenderer-compatible state machine | Partial | Non-goal beyond the minimal target, size, drawing-buffer-size, pixel-ratio, inert `Renderer.domElement` output/client/offset-size/bounding-rect/style mirror plus style-property, attribute, event-target, and canvas-export probes, clear-color/depth/stencil value, output color-space, WebGL coordinate-system getter, tone-mapping, global/material-local clipping state, viewport, scissor, info/debug/inspector, conservative capabilities probes including WebGL limit fields, draw-buffer limits, texture support flags, and renderer-level `getMaxAnisotropy()`, and conservative `hasFeature()`/`hasFeatureAsync()`/`hasCompatibility()` plus `isOccluded()` probes, inert extension lookup, scratch properties/render-list helpers, inert `Renderer.state` buffer and state-level setter/lock probes with raw WebGL binding/upload failures, inert XR event-target dispatch and WebXRManager-adjacent probes, shadow-map, and validated inert constructor-parameter state needed for `Renderer.setRenderTarget()`, `Renderer.setSize()`/`getSize()`, `Renderer.setDrawingBufferSize()`/`getDrawingBufferSize()`, `Renderer.setPixelRatio()`/`getPixelRatio()`, `Renderer.domElement`, `Renderer.setClearColor()`/`getClearColor()`/`setClearAlpha()`/`getClearAlpha()`/`setClearDepth()`/`getClearDepth()`/`setClearStencil()`/`getClearStencil()`, `Renderer.outputColorSpace`/`currentColorSpace`/`_outputColorSpace`, `Renderer.coordinateSystem`, `Renderer.toneMapping`/`currentToneMapping`/`toneMappingExposure`, `Renderer.needsFrameBufferTarget=false`, `Renderer.clippingPlanes`, `Renderer.localClippingEnabled`, inert `Renderer.info` compatibility counters with WebGLInfo- and CommonRenderer-style `update()`/`reset()`/`dispose()`, inert `Renderer.debug` shader-diagnostic state, inert `Renderer.inspector` lifecycle and copy-hook state, `Renderer.capabilities`, `Renderer.extensions`, `Renderer.properties`, `Renderer.renderLists`, `Renderer.state`, inert `Renderer.xr` enabled/cameraAutoUpdate/framebuffer-scale/controller/reference-space/session/base-layer/binding/frame/environment/depth-texture/depth/foveation/camera-texture/event-target dispatch probes with real session binding failing clearly, `Renderer.setViewport()`/`getViewport()`/`getCurrentViewport()`, `Renderer.setScissor()`/`getScissor()`/`setScissorTest()`, `Renderer.shadowMap.enabled` gating plus `shadowMap.autoUpdate`/`needsUpdate`/known `type` compatibility state, cloned `Renderer.getContextAttributes()`, read-only `Renderer.alpha`/`depth`/`stencil`/`logarithmicDepthBuffer` metadata, CommonRenderer-style `Renderer.isRenderer`/`initialized` probes, `Renderer.highPrecision=false` metadata with `true` failing clearly, `Renderer.samples`/`currentSamples`/`isOutputTarget` probes, and default `Renderer.getOutputBufferType()`/`getColorBufferType()` readback, material-set `compile()`/`compileAsync()` compatibility hooks, no-op `init()`/`initRenderTarget()`/`initTexture()`/`initTextureAsync()`/`hasInitialized()`/`clear()`/`clearAsync()`/`clearTarget()`/`clearColor()`/`clearColorAsync()`/`clearDepth()`/`clearDepthAsync()`/`clearStencil()`/`clearStencilAsync()`/`resetState()`/`resetGLState()`/`forceContextLoss()`/`forceContextRestore()`/`setAnimationLoop()`/`getAnimationLoop()` hooks plus no-op `autoClear`/`autoClearColor`/`autoClearDepth`/`autoClearStencil` flags for pass-owned buffers, active-target CopyShader additive accumulation for SSAA-style and low-opacity TAA samples, no-op `dispose()` for object-lifetime native cleanup, null `setRenderObjectFunction()`/`getRenderObjectFunction()` compatibility, unsupported `renderObject()` clear failure, `CubeCamera.update(renderer, scene)`, `StereoEffect` scissored eye-camera renders, and `Reflector`/`Refractor` helper-owned target prepasses. Constructor `alpha`, `depth`, `stencil`, `antialias`, `premultipliedAlpha`, `preserveDrawingBuffer`, `powerPreference`, `failIfMajorPerformanceCaveat`, and default `outputBufferType` values are validated as compatibility no-ops; constructor-level `samples` values above `1`, explicit browser `canvas`/`context` objects, `logarithmicDepthBuffer: true`, `reversedDepthBuffer: true`, and non-default `outputBufferType` values fail clearly. `Renderer.getContext()`, `Renderer.domElement.getContext()`, `Renderer.domElement` canvas export/capture APIs, `setRenderTargetTextures()`, and `setRenderTargetFramebuffer()` also fail clearly because this package does not provide a WebGL context or external WebGL texture/framebuffer binding; `Renderer.renderObject()` and non-null `Renderer.setRenderObjectFunction()` fail clearly because renderer-internal render-object dispatch is unsupported; `Renderer.copyTextureToTexture3D()` fails clearly because backend texture-layer copies are unsupported; invalid DOM style/attribute/event/canvas, compile material values, renderer info update values, inspector values, renderer state, and XR event/controller/reference-space probes fail clearly; `copyFramebufferToTexture()` supports source-rectangle CPU copies from the active render target or cube-face readable color data into readable raw texture base or mip levels and fails clearly without an active readable target, with out-of-bounds source rectangles, oversized rectangle destinations, channel mismatches, or invalid mip levels; `copyTextureToTexture()` supports base/mip CPU copies from readable raw, canvas-like, or OffscreenCanvas-backed source textures into readable raw destination textures and fails clearly for unreadable, out-of-bounds, channel-mismatched, array/3D texture, non-raw destination, or invalid mip-level inputs. Drawing-buffer size is treated as explicit output pixels, and pixel ratio is stored for compatibility without multiplying output dimensions. The contract is scene input and image/target output. |

## Scene Graph And Geometry

| Feature | Status | Notes |
|---|---|---|
| Scene graph root traversal | Supported | Accepts `THREE.Scene` or `THREE.Object3D` roots and honors `visible === false` on ancestors plus renderable-object `frustumCulled=false` opt-outs; malformed child containers and `object.visible`/`object.frustumCulled` values fail clearly. |
| `THREE.Mesh` | Supported | Indexed and non-indexed `BufferGeometry`, groups, material arrays, transforms, normals, UVs, vertex colors, and WebGL-style bounding-sphere frustum culling; malformed geometry attribute/data and object/geometry bounding-sphere containers, invalid geometry attribute, index, group, draw range, malformed material containers, and transform matrix values fail clearly. |
| `THREE.SkinnedMesh` | Supported | CPU skinning from skeleton bones and inverse bind matrices; malformed mesh/skeleton containers and invalid bone, inverse bind, and mesh bind matrix values fail clearly. |
| `THREE.InstancedMesh` | Supported | CPU-expanded per instance with `instanceMatrix` and `instanceColor`; malformed instance matrix/color attributes fail clearly. This favors compatibility over native GPU instancing performance. |
| `THREE.BatchedMesh` | Partial | Common packed-geometry batches are CPU-expanded per active visible instance with matrix and color textures honored, deleted/inactive instance entries skipped, deleted/inactive packed geometry ranges skipped, optimized repacked active geometry ranges honored, packed geometry groups/material arrays including non-indexed group ranges, partial range/group intersections, group sort metadata when present, common per-object sphere frustum culling with `perObjectFrustumCulled` opt-out, range-local internal sort centers including transparent material-array groups, `sortObjects=false` instance-order draws, `customSort` callbacks with BatchedMesh `this` binding, render camera, and list validation, and malformed instance table entries, malformed or out-of-bounds geometry ranges, cached culling bounds, culling controls, and sort controls fail clearly. Exact per-object frustum culling edge cases, broader source-group translation/preservation edge cases beyond optimized range repacking, and native batched drawing remain planned. |
| Morph targets | Supported | CPU-baked relative and absolute position/normal morph targets; malformed morph attribute containers, invalid influence values, and malformed `geometry.morphTargetsRelative` values fail clearly. |
| `THREE.Line`, `LineSegments`, `LineLoop` | Partial | Rendered as unlit lines with bounding-sphere frustum culling and `LineBasicMaterial.linewidth`/`LineDashedMaterial.linewidth` values greater than 1 expanded to camera-facing quads. Opacity, material arrays with geometry groups including active dashed group gaps, WebGL-compatible `linecap`/`linejoin` and unlit `receiveShadow` no-ops, scene fog, `material.fog` opt-out, common instanced-geometry offset/color attributes, selected instanced UV attributes, main-pass `alphaHash` and `alphaToCoverage` opacity cutouts, and directional/spot/point shadow casting with line material base-map/alpha-map, `alphaHash`, and `alphaToCoverage` opacity cutouts are honored. `LineBasicMaterial.map` samples line UVs, including texture UV transforms, horizontal/vertical repeat/mirrored wrapping, channel 0-3 `texture.channel` UV selection, distinct selected UV channels for color and alpha maps, texture RGB with sRGB color-space decode, and alpha-tested texture alpha. Line alpha maps honor channel 0-3 `texture.channel` UV selection and horizontal/vertical repeat/mirrored wrapping. `LineDashedMaterial` dash/gap/scale patterns are CPU-expanded for common non-instanced and instanced cases with custom `lineDistance` attributes, including descending spans, while `scale=0` and missing `lineDistance` attributes stay solid like WebGL; reconstructed map/alpha-map UVs, selected instanced map UV attributes, texture UV transforms including explicit matrices, channel 0-3 `texture.channel` UV selection including distinct color-map and alpha-map channels, dashed-map and alpha-map horizontal/vertical repeat/mirrored wrapping, dashed-map sRGB color-space decode, and interpolated vertex colors are honored. Malformed line scalar values, `linecap`/`linejoin` values, and custom `lineDistance` attributes fail clearly. The generated browser-reference corpus covers simple dashed-line patterns and textured alpha-tested dashed-line output, and renderer-only generated corpus coverage exercises selected dashed-line UV-channel texture routing and wide-line expansion. Exact native/browser wide-line parity and broader line material options remain limited. |
| `THREE.Points` | Partial | `PointsMaterial` is CPU-expanded into camera-facing quads with size, opacity, material arrays with geometry groups, rendered-size-aware bounding-sphere frustum culling, perspective size attenuation, depth-independent orthographic sizing, square untextured point-sprite corners, selected per-point geometry UV channels when present, including distinct color-map and alpha-map channels and `alphaMap` channel 3 from `uv3`, point-sprite map/alpha-map UVs otherwise, map/alpha-map UV transforms, horizontal/vertical repeat/mirrored wrapping for color and alpha maps, sRGB color-space decode for maps, vertex colors, scene fog and `material.fog` opt-out, layers, render ordering, main-pass clipping, main-pass `alphaHash` and `alphaToCoverage` opacity cutouts, common instanced-geometry offset/color attributes, selected instanced UV attributes, directional/spot/point shadow casting from expanded billboards with source-material base-map/alpha-map, `alphaHash` opacity, and `alphaToCoverage` opacity cutouts, WebGL-compatible unlit `receiveShadow` no-op behavior, alpha-tested plus source-`alphaHash`/source-`alphaToCoverage` opacity directional/spot custom-depth and point custom-distance shadow material cutouts, and selected geometry UV channels plus texture transforms for point-light custom-distance base/alpha maps. The generated browser-reference corpus covers textured alpha-tested and instanced point billboards, and renderer-only generated corpus coverage exercises point-sprite, alphaHash/alphaToCoverage, selected geometry UV sampling, and custom shadow alpha-map cutouts. Invalid point size and size-attenuation values fail clearly, and oversized point billboards are clamped to a WebGL-style point-size cap. Exact WebGL point rasterization edge-case parity remains planned. |
| Object/camera layers | Supported | Renderable objects, lights, and LightProbes are filtered against the active camera layers; invalid layer containers/masks fail clearly. |
| `renderOrder` | Partial | Native sorting includes inherited `THREE.Group.renderOrder`/`groupOrder`, object `renderOrder`, material id, WebGL material variant, transmissive/transparent buckets, projected object-or-geometry bounding-sphere z, stable object/insertion ties, `sortObjects`, and custom opaque/transparent sort callbacks with object/material/geometry/group plus group-order/render-order/z/material-variant render-item metadata for object and opaque/transparent grouped mesh/line/point draws and source-object metadata for BatchedMesh-expanded draws, with invalid sort option and reusable-renderer setter values failing clearly. The generated browser-reference corpus includes a grouped opaque-sort callback fixture; broader exact browser-golden parity for callback render-item edge cases remains limited. |
| Renderer bucket flags | Partial | `Renderer.opaque` and `Renderer.transparent` default to `true` and gate native opaque plus transmissive/transparent render buckets like Three.js CommonRenderer, with direct conformance for default rendering, opaque-only output skipping both transmissive and ordinary transparent buckets, transparent/transmissive-only output, and background-only output. Invalid flag values fail clearly. |
| `InstancedBufferGeometry` | Partial | CPU-expanded for mesh, point, line, and dashed-line geometry with instanced vertex colors, common instanced offset attributes (`instanceOffset`, `instancePosition`, `offset`, `translate`, `translation`), instanced UV attributes for current built-in texture streams, Three.js' default `instanceCount = Infinity`, and `meshPerAttribute` repeat values; malformed instanced attributes fail clearly. Arbitrary custom vertex attributes still require additional strategy. |
| `THREE.Sprite` / billboards | Partial | CPU-expanded into camera-facing quads with `SpriteMaterial` color, opacity, scene fog and `material.fog` opt-out, map with texture UV transforms, horizontal/vertical repeat/mirrored wrapping, and sRGB color-space decode, alphaMap with UV transforms and horizontal/vertical repeat/mirrored wrapping, center, scale, rotation, size attenuation, layers, render ordering, main-pass clipping, main-pass `alphaHash` and `alphaToCoverage` opacity cutouts, directional/spot/point shadow casting with source-material base-map/alpha-map, `alphaHash` opacity, and `alphaToCoverage` opacity cutouts, WebGL-compatible unlit `receiveShadow` no-op behavior, and alpha-tested plus source-`alphaHash`/source-`alphaToCoverage` opacity directional/spot custom-depth and point custom-distance shadow material cutouts, including source/custom base-map and alpha-map texture transforms for directional custom-depth and point custom-distance shadows. The generated browser-reference corpus covers a mapped rotated billboard plus a directional sprite shadow caster, and renderer-only generated corpus coverage exercises alpha-map and alphaHash/alphaToCoverage cutouts plus custom shadow alpha-map cutouts. Invalid billboard center, scale, rotation, and size-attenuation values fail clearly. Exact WebGL edge cases remain planned. |
| LOD | Supported | `THREE.LOD` auto-update selects the active child from camera distance/zoom before traversal, including hysteresis threshold behavior across repeated renders; invalid auto-update flags, camera zoom, malformed level entries, or level distance/hysteresis values fail clearly. |
| Clipping planes | Partial | Render-option global `clippingPlanes`, reusable `Renderer.clippingPlanes` global fallback planes, scene-level `THREE.ClippingGroup`, and material-local `clippingPlanes`/`clipIntersection` are evaluated in the main pass for meshes, sprites, points, and lines, up to eight active world-space planes; malformed plane containers, invalid plane/control values, malformed clipping control booleans, and over-budget global/group/material combinations fail clearly. Nested parent/child `ClippingGroup` union/intersection composition has direct conformance plus renderer-only generated corpus coverage because `ClippingGroup` is not WebGLRenderer-referenceable in this Three.js version. The browser-reference generator applies fixture clipping controls to WebGLRenderer-compatible clipping fixtures. Alpha-to-coverage materials smooth MSAA main-pass clipping edges, including custom WGSL fragment materials. `options.localClippingEnabled: false` and reusable `Renderer.localClippingEnabled = false` disable material-local planes while preserving global/group planes; per-render options override renderer state. `material.clipShadows` and group `clipShadows` clip mesh and line shadow-caster fragments in the shadow pass, including direct LineBasicMaterial and LineDashedMaterial caster coverage across directional, spot, and point lights, and source-material clipping state is carried onto custom shadow casters. Deeper browser-golden clipping parity remains planned. |
| Fog | Partial | `THREE.Fog` and `THREE.FogExp2` are applied in mesh, shadow, sprite, point, and line material paths using Three.js-style view-space depth, with `material.fog = false` opt-out. CSS string fog colors are accepted; malformed fog objects/color containers plus invalid fog color and parameter values fail clearly. Direct conformance covers Fog/FogExp2 material output, CSS string color input, shadow receiver, sprite/point/line paths, material fog opt-out, and view-depth behavior, and the generated browser-reference corpus covers linear fog with a material opt-out; broader browser-golden fog parity remains planned. |

## Cameras And Output

| Feature | Status | Notes |
|---|---|---|
| Perspective cameras | Supported | Uses the camera projection and inverse world matrices; invalid clipping ranges and matrix containers/values fail clearly. |
| Orthographic/custom projection cameras | Supported | Any camera with `projectionMatrix` and `matrixWorldInverse`; invalid aspect-derived dimensions, clipping distances, and matrix containers/values fail clearly. |
| Array/cube cameras | Partial | `THREE.ArrayCamera` sub-cameras compose into PNG, RGBA, and target output using each sub-camera viewport, including target depth readback, object-id target metadata, and reusable `Renderer.setRenderTarget()` state for composed color/depth targets. `THREE.CubeCamera` renders six RGBA faces plus optional depth faces into `WebGLCubeRenderTarget.texture.image`/`source.data`, exposes object-id target metadata for object-id cube renders, writes nonzero `activeMipmapLevel` output to the active mip entry, supports active-mip target scissor clipping with direct conformance, supports explicit cube-face `Renderer.readRenderTargetPixels()` readback, captures through `CubeCamera.update(renderer, scene)` using renderer-compatible active cube-face/mip target state while preserving inert `Renderer.xr` enabled/cameraAutoUpdate/framebuffer-scale/controller/reference-space/session/base-layer/binding/frame/environment/depth-texture/depth/foveation/camera-texture/event-target dispatch probes, and captured color textures can be reused as cube background/environment inputs. Real XR session binding and malformed nested ArrayCamera/CubeCamera camera containers fail clearly, with renderer-only generated corpus coverage for ArrayCamera viewport composition and CubeCamera face/update capture; native PMREM generation and exact WebGL cube viewport/face semantics remain planned. |
| Scene background color | Supported | `scene.background` and `options.background` accept `THREE.Color`, CSS strings, and RGBA color-like objects/arrays with preserved output alpha; `options.background: null` clears the scene background for one render. Reusable `Renderer.setClearColor()`/`getClearColor()`/`setClearAlpha()`/`getClearAlpha()` fallback clear state supports hex, CSS string, and color-like clear-color inputs, plus `scene.backgroundIntensity` and `options.backgroundIntensity`; scene and option-supplied backgrounds override renderer clear state, option backgrounds ignore scene background controls unless the matching option control is supplied, and malformed scene, option, or renderer clear-color values fail clearly. Direct conformance covers RGBA clear alpha, renderer clear-color fallback/precedence, CSS scene/option background colors, and the generated browser-reference corpus covers option-level color background overrides. |
| Background textures/cubemaps | Partial | 2D, equirectangular, raw, encoded, CubeUV-mapped readable six-face cube, and packed 2D PMREM/CubeUV sharp-atlas texture backgrounds from `scene.background` or `options.background` are rendered with texture sRGB decode, background intensity, approximate `backgroundBlurriness`, and output color-space conversion. Raw one-channel, two-channel, RGB, and RGBA numeric `DataTexture` backgrounds expand through the same texture-rendering path as material maps, including readable `texture.image` and `texture.source.data` payloads, with direct one/two-channel and source-backed conformance. Option-supplied backgrounds use their own texture transforms plus `options.backgroundIntensity`, `options.backgroundBlurriness`, and `options.backgroundRotation` rather than scene background controls. 2D backgrounds honor wrap modes and UV transforms; equirectangular/cube backgrounds sample from camera direction, direct conformance covers option texture transform and intensity/blurriness control isolation plus cube `options.backgroundRotation` overrides and packed CubeUV sharp-face decoding, and the generated browser-reference corpus covers option-level color background overrides, 2D background texture transforms, and equirectangular background rotation/intensity, with renderer-only generated corpus coverage for raw, CubeUV cube, and packed CubeUV background rotation and cube `options.backgroundRotation`. Invalid background color/control/rotation values and malformed packed PMREM/CubeUV background layouts fail clearly, and exact packed PMREM prefiltered LOD semantics, mapping/color-space/blur parity remain planned. |
| Output color space | Partial | `options.outputColorSpace` and reusable `Renderer.outputColorSpace`/`currentColorSpace`/`_outputColorSpace` support `THREE.SRGBColorSpace` (`"srgb"`, default) and `THREE.LinearSRGBColorSpace` (`"srgb-linear"`, `"linear-srgb"`, `"linearsrgb"`, or `"linear"`) for material and texture background output conversion; per-render options override renderer state, and unsupported values fail clearly. The browser-reference generator normalizes the same aliases in generated manifests, and the generated corpus covers linear output color-space conversion; exact WebGLRenderer color-management state parity remains planned. |
| Tone mapping | Partial | Material output uses `THREE.ACESFilmicToneMapping` by default with `Renderer.toneMappingExposure` and supports reusable `Renderer.toneMapping` plus `currentToneMapping` values `THREE.NoToneMapping`, `THREE.LinearToneMapping`, `THREE.ReinhardToneMapping`, `THREE.CineonToneMapping`, `THREE.ACESFilmicToneMapping`, `THREE.CustomToneMapping`, `THREE.AgXToneMapping`, and `THREE.NeutralToneMapping` while preserving per-material `toneMapped=false`. `THREE.CustomToneMapping` uses Three.js' default identity custom function; GLSL shader-chunk customizations remain part of the broader shader translation gap. `Renderer.needsFrameBufferTarget` reports `false` because output conversion is inline in the native render pass instead of a CommonRenderer framebuffer target. Invalid tone-mapping constants and exposure values fail clearly. |
| Mask/object-id/normal/depth render modes | Partial | `options.renderMode: "mask"` writes white visible geometry on black; `"object-id"` encodes adapter object sort IDs plus one into RGB bytes with zero reserved for background; `"normal"` writes view-space normal colors matching `MeshNormalMaterial` for visible geometry; `"depth"` writes normalized grayscale depth for visible geometry. Target object-id renders expose `objectIdEntries`/`objectIdMap` reverse lookup metadata through `renderToTarget()`, `options.target`, and reusable `Renderer.setRenderTarget()` state, including composed `ArrayCamera` and `CubeCamera` output, and stale metadata is cleared on later color renders. Regular-camera, ArrayCamera, and CubeCamera MRT-shaped targets can request secondary `"mask"`, `"object-id"`, `"normal"`, and `"depth"` attachments with `texture.userData.headlessThreeRenderer.renderMode`, using explicit multi-pass readback. Invalid render mode values fail clearly. These passes bypass lighting, environment, fog, post-processing, and scene backgrounds while preserving depth, culling, clipping, base texture alpha, `material.alphaMap`, `alphaTest`, and `alphaHash`, with direct conformance for post-processing bypass. The browser-reference generator and generated corpus cover simple mask and normal render-mode output, and renderer-only generated corpus covers object-id and depth output; native arbitrary MRT remains planned. |
| Viewport/scissor | Partial | `options.viewport`, `options.scissor`, `target.viewport`, `target.scissor` with `target.scissorTest`, and reusable `Renderer.setSize()`/`getSize()` plus `setDrawingBufferSize()`/`getDrawingBufferSize()` and `setPixelRatio()`/`getPixelRatio()` compatibility state and `setViewport()`/`setScissor()`/`setScissorTest()` state accept output pixel sizes and `[x, y, width, height]`, `{ x, y, width, height }`, or Vector4-style `{ x, y, z, w }` pixel rectangles in top-left output coordinates; common-renderer viewport depth ranges are accepted only for the default `0..1` range. Direct conformance covers render-option, `renderToTarget()`, `options.target`, reusable `Renderer.setRenderTarget()` state for target-owned dimensions and viewport/scissor fields including disabled target scissor tests, reusable renderer-state size/drawing-buffer-size/rectangle inputs, pixel-ratio no-scaling output-pixel behavior, `Renderer.getSize()`/`getDrawingBufferSize()`/`getViewport()`/`getCurrentViewport()`/`getScissor()` clone plus target-object population, active-render-target `copyFramebufferToTexture()` source-rectangle and mip-level CPU copies, raw/canvas-like/OffscreenCanvas-backed source `copyTextureToTexture()` CPU copies into raw destinations, and `copyTextureToTexture3D()` clear-failure coverage for unsupported texture-layer copies, plus inert `Renderer.domElement` output/client/offset-size/bounding-rect/style mirroring plus style-property, attribute, event-target, canvas-export, and `ViewHelper.render()` offset-size/viewport-restore probes; inert `Renderer.debug`, inert `Renderer.inspector` lifecycle and copy-hook state, conservative `Renderer.capabilities` WebGL limit, draw-buffer, texture probes, and renderer-level `getMaxAnisotropy()`, and conservative `hasFeature()`/`hasFeatureAsync()`/`hasCompatibility()` plus `isOccluded()` probes, inert `Renderer.extensions`, scratch `Renderer.properties`/`renderLists`, inert `Renderer.xr` enabled/cameraAutoUpdate/framebuffer-scale/controller/reference-space/session/base-layer/binding/frame/environment/depth-texture/depth/foveation/camera-texture/event-target dispatch probes, reusable material-set `Renderer.compile()`/`compileAsync()` hooks, `Renderer.clear()`/`clearAsync()`/`clearTarget()`/`clearColor()`/`clearColorAsync()`/`clearDepth()`/`clearDepthAsync()`/`clearStencil()`/`clearStencilAsync()`/`init()`/`initRenderTarget()`/`initTexture()`/`initTextureAsync()`/`hasInitialized()`/`resetState()`/`resetGLState()`/`dispose()`/`setAnimationLoop()`/`getAnimationLoop()` hooks, clear depth/stencil value state, and `autoClear`/`autoClearColor`/`autoClearDepth`/`autoClearStencil` flags are accepted as compatibility state because each native render pass owns its buffers and native resources follow object lifetime. Explicit options override target and renderer state, while target dimensions override renderer size fallback. Invalid sizes, size updateStyle flags, pixel ratios, rectangles, non-default viewport depth ranges, debug values, inspector values, capability/extension/property/render-list probes, DOM style/attribute/event/canvas probes, XR booleans/values/events/controller/reference-space/session binding, compile material values, clear values/booleans, resource-init targets/textures, external WebGL texture/framebuffer handles, copy targets, unreadable, array/3D, or otherwise unsupported texture-copy inputs, animation-loop callbacks, auto-clear flags, and malformed scissor flags fail clearly; external framebuffer/texture handle APIs and no-active-target framebuffer copies fail clearly as unsupported. The browser-reference generator applies fixture viewport/scissor options and the generated corpus covers a clipped render-option rectangle. Exact WebGLRenderer state-machine parity remains out of scope for the scene-oriented API. |
| Depth output / MRT | Partial | Target color textures can receive Alpha/Red/RG/RGB/RGBA and RedIntegerFormat/RGIntegerFormat/RGBIntegerFormat/RGBAIntegerFormat channel data, normalized FloatType, normalized signed/unsigned integer, packed 16-bit color, RGB9_E5/R11F_G11F_B10F packed color, or HalfFloatType readback while top-level `target.data` remains RGBA8, with texture image/source containers updated and `needsUpdate` set, including target texture arrays, MRT-shaped targets, `options.target`, reusable `Renderer.setRenderTarget()` state for typed single-attachment target arrays, and `Renderer.readRenderTargetPixels()` attachment-index plus explicit cube-face reads. Regular-camera, ArrayCamera, and CubeCamera MRT-shaped targets can populate secondary color textures with explicit multi-pass `"color"`, `"mask"`, `"object-id"`, `"normal"`, and `"depth"` attachments declared by `texture.userData.headlessThreeRenderer.renderMode`; `Renderer.getMRT()` returns `null`, `Renderer.setMRT(null)` is accepted as a clear operation, and unhinted secondary attachments fail clearly. Target `depthTexture` objects receive normalized depth readback for single-output targets, including image/source writeback and `needsUpdate` marking through `renderToTarget()`, `options.target`, and reusable `Renderer.setRenderTarget()` state for `FloatType`, `HalfFloatType`, and packed `UnsignedInt248Type` depth, scissor clipping, base-texture and alpha-map alpha-tested visible geometry, `alphaHash` cutouts, and transparent material default/explicit `depthWrite` behavior. `THREE.FloatType` depth textures receive scalar `Float32Array` data, `HalfFloatType` receives `Uint16Array` half-float data, `UnsignedByteType`/`UnsignedShortType`/`UnsignedIntType` depth textures receive scalar unsigned typed arrays, with direct coverage for `UnsignedShortType` source writeback; `UnsignedInt248Type` receives `Uint32Array` data with normalized depth24 in the high bits and zero stencil bytes, and plain depth target objects receive RGBA8 bytes. Malformed target image containers, nested target texture/mipmap/source containers, array/3D target texture objects, compressed target texture objects or compressed target format constants, regular-camera cube target texture objects, unsupported color target formats/types, unsupported explicit depth texture types or format/type pairings, and arbitrary native MRT shader outputs including non-null `Renderer.setMRT()` fail clearly, with representative validation covered through `options.target`; native hardware depth-texture/stencil readback and native MRT remain planned. |
| MSAA controls | Partial | Render-option and target sample counts support 4x MSAA with resolve into normal output and target readback buffers, with direct coverage for render-option `samples` plus target `samples` and `sampleCount` aliases through `renderToTarget()`, `options.target`, and reusable `Renderer.setRenderTarget()` state. `Renderer.samples` reports `0` because constructor-level default MSAA is unsupported, while `Renderer.currentSamples` reflects an active target sample count and `Renderer.isOutputTarget` reports whether rendering is currently directed at the default output; constructor-level `samples > 1` fails clearly with per-render/target MSAA guidance. `material.alphaToCoverage` uses native alpha-to-coverage for multisampled renders, with generated browser-reference corpus coverage for the current 4x-MSAA path. Unsupported sample counts fail clearly; broader WebGL sample-count/state parity remains planned. |

## Materials And Textures

| Feature | Status | Notes |
|---|---|---|
| `MeshBasicMaterial` | Supported | Unlit path, including supported material-level reflection/refraction `envMap` inputs with legacy multiply/mix/add combine modes, `reflectivity`, `refractionRatio`, and shared env-map rotation. The generated browser-reference corpus covers shared Basic/Lambert material-level envMap output. |
| `MeshLambertMaterial` | Supported | Diffuse-only lighting path, including supported material-level reflection/refraction `envMap` inputs with legacy multiply/mix/add combine modes, `reflectivity`, `refractionRatio`, and shared env-map rotation. The generated browser-reference corpus covers shared Basic/Lambert material-level envMap output. |
| `MeshPhongMaterial` | Partial | Blinn-Phong direct lighting with `specular`, `shininess`, and `specularMap` strength is supported. Scene-level environment/reflection probes and supported material-level reflection/refraction `envMap` inputs feed an approximate specular reflection path, including `specularMap` masking plus legacy multiply/mix/add combine modes, `reflectivity`, `refractionRatio`, and shared env-map rotation; the generated browser-reference corpus covers a Phong material-level envMap fixture. Exact remaining transform edge cases remain planned. |
| `MeshStandardMaterial` | Supported | PBR metallic/roughness path. |
| `MeshPhysicalMaterial` | Partial | Clearcoat, sheen, anisotropy, scalar iridescence, specular intensity/color, roughness-aware scene-color transmission, approximate dispersion, IOR, thickness, transmission volume attenuation color/distance, and related maps are supported, with direct conformance for scalar clearcoat/roughness IBL response plus committed glTF coverage for transmission maps, volume thickness/attenuation, and IOR factors. Malformed physical color containers and invalid physical color/scalar/vector/range values fail clearly. Exact iridescence, attenuation, transmission mip/bicubic behavior, and several edge cases remain planned. |
| `MeshNormalMaterial` | Supported | View-space normal color output is supported, including `flatShading`, tangent-space and object-space normal maps, and bump maps. CPU-baked displacement affects triangle geometry. The generated browser-reference corpus covers standalone normal-material output, and renderer-only generated corpus coverage exercises tangent-space normal-map perturbation, object-space normal-map interpretation, and bump-map perturbation. |
| `MeshMatcapMaterial` | Supported | Matcap texture sampling from view-space normals is supported, including `flatShading`, separate color maps with `texture.channel` values 0-3 and direct `uv1`-`uv3` conformance, transforms, horizontal/vertical repeat/mirrored wrapping, and nearest/linear filtering, tangent-space and object-space normal maps, and bump maps. CPU-baked displacement affects triangle geometry. The generated browser-reference corpus covers matcap map output, and renderer-only generated corpus coverage exercises flat-shading lookup, tangent/object-space normal-map lookup, and bump-map lookup perturbation. |
| `MeshDepthMaterial` | Partial | Basic, RGBA, RGB, and RG depth packing are supported, while unsupported depth-packing constants fail clearly. Wireframe output, base/alpha map alpha testing, and CPU-baked displacement are supported through the common material path. The generated browser-reference corpus covers BasicDepthPacking and wireframe scene output, and renderer-only generated corpus coverage exercises non-Basic depth-packing variants and displacement-map depth output. `Object3D.customDepthMaterial` is honored for directional and spot mesh shadow caster base-map/alpha-map alpha-tested inputs, including selected base-map and alpha-map `uv1`-`uv3` channels, base-map and alpha-map texture transforms, displacement, visibility flags, and wireframe inputs, and source-material base-map/alpha-map alpha cutouts including selected base-map and alpha-map `uv1`-`uv3` channels, base-map and alpha-map texture transforms, `alphaHash` opacity cutouts, `alphaToCoverage` opacity cutouts, clipping state, displacement maps, and wireframe state are carried onto custom-depth casters; alpha-tested sprite/point billboard shadow cutouts and malformed custom shadow material containers/visibility values fail clearly. |
| `MeshDistanceMaterial` | Partial | Red-channel distance output is supported, including wireframe output, optional `referencePosition`, `nearDistance`, and `farDistance` overrides plus base/alpha map alpha testing and CPU-baked displacement through the common material path. Direct conformance covers range/reference controls, invalid range/reference values, malformed material userData and renderer hint containers, alpha-map cutouts, selected custom-shadow and source-material base-map/alpha-map `uv1`-`uv3` cutouts, custom/source shadow base-map and alpha-map texture transforms, point-billboard base-map/alpha-map selected geometry UV-channel cutouts, and displacement-map `uv1`-`uv3` channel selection; renderer-only generated corpus covers range, displacement-map, and wireframe output because browser WebGL uses `MeshDistanceMaterial` as a shadow-internal material. `Object3D.customDistanceMaterial` is honored for point-light mesh shadow caster base-map/alpha-map alpha-tested inputs, including selected base-map and alpha-map `uv1`-`uv3` channels, base-map and alpha-map texture transforms, displacement, visibility flags, and wireframe inputs, and source-material base-map/alpha-map alpha cutouts including selected base-map and alpha-map `uv1`-`uv3` channels, base-map and alpha-map texture transforms, `alphaHash` opacity cutouts, `alphaToCoverage` opacity cutouts, clipping state, displacement maps, and wireframe state are carried onto custom-distance casters; alpha-tested sprite/point billboard shadow cutouts, selected geometry UV channels for point-billboard base/alpha maps, and malformed custom shadow material containers/visibility values fail clearly. |
| `MeshToonMaterial` | Partial | Lit toon shading supports Three.js' no-gradient-map fallback bands, red-channel `gradientMap` ramps, and the common map, normal, bump, emissive, AO, light-map, alpha, displacement, side, fog, and shadow paths. Direct conformance covers fallback bands, gradient-map filtering, horizontal repeat/mirrored wrapping, and color-space behavior, normal/bump-map lighting perturbation, base-map `uv1`-`uv3` channels, emissive-map `uv1`-`uv3` channels, light-map `uv1`-`uv3` channels, and alpha-map cutouts. The generated browser-reference corpus covers gradient-map and alpha-map cutout output, and renderer-only generated corpus coverage exercises fallback bands plus normal/bump-map lighting perturbation. Exact browser-golden edge-case parity remains planned. |
| `ShadowMaterial` | Partial | Transparent shadow receiver output is supported for the renderer's current shadow map path, including multiple packed shadow lights, `material.color`, `material.opacity`, scene fog, `material.fog` opt-out, and output color-space conversion. Direct conformance covers color tint, opacity, Fog/FogExp2 fog opt-out, output conversion, and multiple directional shadow lights, and the generated browser-reference corpus covers focused received-shadow tint and fog opt-out fixtures. Exact WebGLRenderer edge-case parity remains planned. |
| Base color, opacity, and visibility | Supported | Includes CSS string material color inputs, `scene.overrideMaterial` replacement for normal color renders, transparent sorting path, and `material.visible === false` skipping; malformed color containers plus invalid color, opacity, and material visibility values fail clearly. |
| Vertex colors | Supported | Multiplied with material color; invalid `material.vertexColors` values fail clearly. |
| `material.side` | Supported | Front, back, and double-sided culling; unsupported side constants fail clearly. |
| `material.wireframe` | Partial | Supported mesh materials render triangle edges in the main pass, with direct coverage for `MeshBasicMaterial`, `MeshDepthMaterial`, and `MeshDistanceMaterial`; legacy `wireframeLinewidth`, `wireframeLinecap`, and `wireframeLinejoin` hints are validated and accepted as native no-ops; mesh custom depth/distance shadow materials can cast line-list wireframe shadows. Exact WebGL wireframe rasterization parity remains planned. |
| Alpha test | Supported | Fragment discard cutoff, with alpha-to-coverage threshold smoothing on multisampled main-pass renders. |
| Alpha coverage | Partial | `material.alphaHash` applies deterministic stochastic discard before depth/color writes. `material.alphaToCoverage` uses native MSAA alpha-to-coverage when the effective sample count is greater than 1, including output-alpha coverage, alpha-test threshold smoothing, and smoothed main-pass clipping edges, with direct conformance plus generated browser-reference corpus coverage for the current 4x-MSAA path; exact WebGL sample-mask parity remains planned. |
| Blending and depth/color state | Partial | Built-in `NoBlending`, `NormalBlending`, `AdditiveBlending`, `SubtractiveBlending`, and `MultiplyBlending` modes, `CustomBlending` equations/factors including min/max equations, separate alpha equation/factors, and constant color/alpha factors, plus `material.depthTest`, `material.depthFunc`, `material.depthWrite`, `material.colorWrite`, `material.polygonOffset`, standard and custom-WGSL material `premultipliedAlpha`, transparent default depth writes, `forceSinglePass` accepted as a native single-pass no-op, boolean-validated `material.dithering` and supported `material.precision` strings accepted as native no-ops, stencil state, and 4x-MSAA `alphaToCoverage` are honored in the main pass. Direct conformance covers all supported depth comparison constants, all supported stencil comparison functions, all supported stencil z-pass operations, stencil write/func masks, custom blending, stencil masking, accepted `forceSinglePass`, accepted `dithering` and `precision`, custom-WGSL premultiplied output, and the current 4x-MSAA alpha-to-coverage path, and the generated browser-reference corpus covers those paths. Unsupported blending, custom blend equation/factor, depth function, stencil function, stencil operation constants, unsupported precision strings, and malformed boolean/numeric render-state values fail clearly. Exact WebGLRenderer state parity remains planned. |
| Base color map | Supported | Raw one-channel, two-channel, RGB, and RGBA numeric `DataTexture` inputs from `texture.image` or `texture.source.data`, including byte, signed/unsigned normalized integer, packed 16-bit color, packed RGB9_E5/R11F_G11F_B10F color, float, and half-float typed data, plus encoded PNG/JPEG/WebP buffers. `texture.channel` values 0-3 are accepted, with malformed channel values failing clearly; channels 1-3 route selected non-primary UV attributes through the available native UV streams, direct conformance covers `uv2` and `uv3` base-map sampling, mesh material draws can share up to two distinct selected texture channels across current supported slots, and draws requiring three or more distinct texture channels fail clearly. Base color maps honor `flipY === false`, raw `texture.premultiplyAlpha`, and `texture.colorSpace === THREE.SRGBColorSpace` before shading. Mismatched raw channel layouts fail clearly. |
| Texture wrap modes | Supported | Repeat, mirrored repeat, and clamp-to-edge for supported texture slots. Direct conformance covers base color-map and background texture repeat/mirrored wrapping. |
| Normal and bump maps | Supported | Tangent-space and object-space normal maps are supported, with direct conformance for tangent-space `normalScale`, `texture.channel` values 0-3 through `uv1`-`uv3`, horizontal/vertical normal-map repeat/mirrored wrapping, horizontal/vertical bump-map repeat/mirrored wrapping, texture color-space decode, and nearest/linear normal-map filtering; unsupported `normalMapType` values and malformed `normalScale` values fail clearly. Bump maps include `bumpScale`. Texture transforms, wrap modes, and `NearestFilter`/`LinearFilter` sampler settings are honored, with renderer-only generated corpus coverage for MeshNormalMaterial tangent-space normal-map perturbation, object-space normal-map interpretation, and bump-map perturbation, MeshMatcapMaterial tangent/object-space normal-map and bump-map lookup perturbation, and MeshToonMaterial fallback bands and normal/bump-map lighting perturbation. |
| Displacement map | Partial | Triangle meshes with UVs and normals are CPU-displaced along object-space normals using the red channel, `displacementScale`, `displacementBias`, `texture.channel` values 0-3 with direct `uv1`-`uv3` conformance, texture color space, texture transforms, wrap modes, and nearest/linear filters. Direct conformance covers texture color space, texture transforms, explicit matrices, horizontal/vertical repeat/mirrored wrapping, nearest/linear sampler filtering, and bias, and renderer-only generated corpus coverage exercises displacement-map depth and distance output. Exact WebGL vertex-shader edge cases, line/point displacement, mip LOD, and shadow-reference nuances remain planned. |
| Metallic/roughness maps | Supported | glTF convention: roughness in G, metalness in B. `texture.channel` values 0-3, texture color space, texture transforms, and wrap/filter sampler settings are honored, with direct roughness-map `uv1`-`uv3` conformance and direct metalness-map and roughness-map horizontal/vertical repeat/mirrored wrapping plus nearest/linear filtering coverage. |
| Emissive map | Supported | Combined with emissive color/intensity. `texture.channel` values 0-3 are honored with direct `uv1`-`uv3` conformance, and texture transforms, sRGB color-space decode, horizontal/vertical repeat/mirrored wrapping, and nearest/linear filtering are supported. |
| AO map | Supported | Red channel is applied to indirect lighting. `texture.channel` values 0-3 are honored with direct `uv1`-`uv3` conformance, and texture color space, texture transforms, horizontal/vertical repeat/mirrored wrapping, and nearest/linear filtering are supported. |
| Light map | Partial | RGB channels add baked indirect diffuse lighting with `lightMapIntensity`; `texture.channel` values 0-3 are honored with direct `uv1`-`uv3` conformance, and texture transforms, sRGB color-space decode, horizontal/vertical repeat/mirrored wrapping, and nearest/linear filtering are supported. Broader light-map parity remains planned. |
| Specular map | Partial | Red channel modulates `MeshPhongMaterial` specular strength; `texture.channel` values 0-3 are honored with direct `uv1`-`uv3` conformance, and texture color space, texture transforms, horizontal/vertical repeat/mirrored wrapping, and nearest/linear filtering are supported. Non-Phong uses remain planned. |
| Alpha map | Supported | Green channel multiplies material opacity and participates in alpha testing/blending. `texture.channel` values 0-3 are honored with direct `uv1`-`uv3` conformance, and texture color space, `offset`, `repeat`, `rotation`, `center`, explicit `matrix`, `flipY`, horizontal/vertical repeat/mirrored wrapping, and nearest/linear filtering are supported. |
| Toon gradient map | Supported | `MeshToonMaterial.gradientMap` samples the red channel as the direct diffuse ramp at `dot(N, L) * 0.5 + 0.5`, with sRGB color-space decode, horizontal repeat/mirrored wrapping, and nearest/linear filter sampler settings honored. |
| Physical extension maps | Partial | Clearcoat, sheen, anisotropy, iridescence factor/thickness, specular color/intensity, transmission, and thickness map subsets are supported; all current physical-extension maps honor primary/secondary `texture.channel` UV selection, and direct clearcoat, sheen, anisotropy, iridescence factor/thickness, specular color/intensity, transmission, and thickness coverage exercises `uv1`-`uv3` routing. They also support texture transforms including explicit matrices, packed texture-group sampler settings, and `THREE.SRGBColorSpace` decode for clearcoat, clearcoat roughness/normal, anisotropy, iridescence factor/thickness, transmission, thickness, and sheen/specular color map RGB channels. Direct conformance covers encoded anisotropy texture loading plus sampler state, explicit anisotropy rotations, direction textures, scalar strength grids, specular factor/color texture role assignment, nearest/linear filtering across current physical-extension maps, and horizontal/vertical repeat/mirrored wrapping for iridescence factor/thickness maps. Conflicting sampler settings between maps packed into one native texture fail clearly; additional physical slots and deeper BRDF parity remain planned. |
| Physical transmission dispersion | Partial | Approximate `MeshPhysicalMaterial.dispersion` is supported for scene-color transmission/refraction, with direct conformance and renderer-only generated corpus coverage comparing normal and dispersed transmission over a red/blue edge. Browser-reference golden parity remains planned. |
| Custom WGSL fragment body | Partial | Supported through `material.userData.headlessThreeRenderer.fragmentWgsl` and top-level `material.customFragmentWgsl`/`customFragmentShader`/`headlessFragmentWgsl`/`headlessFragmentShader` aliases, including as an explicit opt-in path for `ShaderMaterial`/`RawShaderMaterial` inputs and materials with `onBeforeCompile` customizations. Narrow built-in Three.js fullscreen CopyShader and OutputShader pass adapters are recognized for covered EffectComposer helper flows. Returned custom fragment color honors `material.premultipliedAlpha`. Malformed custom-fragment override values plus malformed material userData and renderer hint containers fail clearly. This is not Three.js shader parity. |
| Texture transforms | Partial | Base color, 2D background, sprite/point color and alpha, line/dashed-line, matcap color, normal, bump, displacement, metallic/roughness, emissive, AO, light, specular, alpha, clearcoat, clearcoat roughness, clearcoat normal, sheen color/roughness, anisotropy, iridescence factor/thickness, specular color/intensity, transmission, and thickness maps honor `offset`, `repeat`, `rotation`, `center`, `flipY`, and explicit `matrix` when `matrixAutoUpdate === false`; malformed transform vector containers, malformed `flipY`/`matrixAutoUpdate` values, and invalid transform values fail clearly. Current color-producing transform slots have direct conformance for background `flipY` and color-space decode after explicit matrices, the generated browser-reference corpus covers explicit base-map matrix sampling with sRGB decode, and committed glTF coverage verifies multi-slot `KHR_texture_transform` routing across base color, emissive, normal, metallic/roughness, occlusion, unlit, and clearcoat maps; exact browser golden parity for broader texture slots remains planned. |
| Texture color-space parity | Partial | Base color, sprite, point, line/dashed-line, matcap, normal/bump, displacement, metallic/roughness, emissive, AO, light, Phong specular, alpha, toon gradient, physical scalar RGB maps, sheen color, physical specular color, supported background texture maps, and equirectangular/cube scene-environment, reflection-probe, and supported material-level LDR IBL inputs honor explicit `THREE.SRGBColorSpace`/`THREE.LinearSRGBColorSpace`, including the documented linear string aliases; unsupported texture color-space/encoding values fail clearly; base color maps honor `flipY`; raw material/background/current IBL texture uploads honor `texture.premultiplyAlpha`; material and texture background output conversion supports `THREE.SRGBColorSpace`/`THREE.LinearSRGBColorSpace`; and standard material shaders honor `premultipliedAlpha`. The generated browser-reference corpus covers explicit base-map matrix sampling with sRGB decode plus linear output color-space conversion; remaining slot parity and broader WebGLRenderer color-management state parity remain planned. |
| Texture sampler settings | Partial | Base color/matcap, background, normal/bump, displacement, metallic/roughness, emissive, AO/light, alpha, Phong specular, toon gradient, matcap color-map, and packed physical-extension texture groups honor wrap modes plus `NearestFilter`/`LinearFilter`-family `magFilter` and `minFilter`, with direct coverage for glTF non-power-of-two repeat/mipmap sampler state, linear min/mag filtering, background texture repeat/mirrored wrapping, base color-map repeat/mirrored wrapping, normal-map repeat/mirrored wrapping, bump-map repeat/mirrored wrapping, displacement-map repeat/mirrored wrapping, metalness-map repeat/mirrored wrapping, roughness-map repeat/mirrored wrapping, matcap color-map repeat/mirrored wrapping, Phong specular-map repeat/mirrored wrapping, AO-map repeat/mirrored wrapping, emissive-map repeat/mirrored wrapping, light-map repeat/mirrored wrapping, alpha-map repeat/mirrored wrapping, sprite color-map repeat/mirrored wrapping, sprite alpha-map repeat/mirrored wrapping, point color-map repeat/mirrored wrapping, point alpha-map repeat/mirrored wrapping, line color-map repeat/mirrored wrapping, line alpha-map repeat/mirrored wrapping, dashed-line color-map repeat/mirrored wrapping, dashed-line alpha-map repeat/mirrored wrapping, toon gradient-map horizontal repeat/mirrored wrapping, current physical-extension map nearest/linear filtering, and iridescence factor/thickness repeat/mirrored wrapping; unsupported sampler constants fail clearly. Mipmap min filters generate native mip chains from the source level for supported material/background texture uploads, and raw `DataTexture`-style explicit mipmap chains, including half-float levels, upload for unpacked 2D material/background texture slots; malformed texture source containers, malformed `generateMipmaps` values, invalid `unpackAlignment` values, and explicit mipmap containers fail clearly. Material/background/current IBL texture slots accept WebGL-compatible `unpackAlignment` values for tightly packed readable uploads, material/background sampler slots accept non-default texture anisotropy and use native sampler anisotropy when the effective sampler is linear-filtered, and explicit mipmap arrays still fail clearly for packed physical-extension maps and scene-environment/reflection-probe/material-envMap texture slots. Conflicting sampler settings, including anisotropy, between maps packed into one native texture also fail clearly. Deeper browser LOD parity remains planned. |
| Additional texture slots | Partial | Scalar iridescence plus iridescence factor/thickness maps are supported, with direct conformance for explicit matrices, `uv1`-`uv3` channel routing, horizontal/vertical repeat/mirrored wrapping, and nearest/linear sampler filtering. Direct physical-map coverage also exercises `uv1`-`uv3` channel routing for clearcoat, sheen, anisotropy, specular color/intensity, transmission, and thickness maps. Point color and alpha maps honor selected geometry UV channels when geometry UVs are present, including direct `alphaMap` channel 3 coverage from `uv3`, and committed glTF coverage verifies primary/secondary UV routing across base-color and emissive textures. Remaining work is broader physical texture-slot parity and exact edge-case behavior. |
| Additional material classes | Partial | `THREE.Scene.overrideMaterial` replaces object materials for normal color renders, including material-envMap discovery, while adapter render modes intentionally keep their flat override passes. Remaining class work is mostly deeper parity for supported classes plus unsupported/custom material strategies documented above. Material-level reflection `envMap` inputs are supported for `MeshBasicMaterial`, `MeshStandardMaterial`, `MeshPhysicalMaterial`, `MeshPhongMaterial`, and `MeshLambertMaterial` when one shared native IBL map can represent the render, and `envMap` properties on Depth, Distance, Matcap, Normal, and Toon mesh materials are accepted as no-ops. The generated browser-reference corpus covers Phong and shared Basic/Lambert material-level envMap fixtures plus focused MeshDepthMaterial depth/wireframe, MeshNormalMaterial, MeshMatcapMaterial, MeshToonMaterial gradient/alpha-cutout, and ShadowMaterial receiver/fog fixtures. Renderer-only generated corpus covers shared Standard/Physical material-level envMap fixtures, MeshNormalMaterial tangent/object-space normal-map and bump-map output, MeshMatcapMaterial flat-shading, tangent/object-space normal-map, and bump-map lookup output, MeshToonMaterial fallback bands and normal/bump-map lighting perturbation, and MeshDistanceMaterial range, displacement-map, and wireframe output, plus sprite/point billboard custom shadow alpha-map cutouts. Unsupported material classes, unsupported material env-map options, invalid material env-map scalar/rotation values, `ShaderMaterial`, `RawShaderMaterial`, NodeMaterial, and built-in material `onBeforeCompile` customizations fail clearly unless a documented alternative path or the covered CopyShader/OutputShader helper exception is used. |
| Compressed textures | Unsupported | KTX2/Basis/`THREE.CompressedTexture` inputs and compressed texture format constants fail with a clear pre-decode error across material, material-envMap, background, scene-environment, and reflection-probe texture slots. Direct conformance covers material-map and material-envMap compressed textures, compressed-format texture objects, explicit mipmaps, DataArrayTexture/Data3DTexture failures across material/background/environment/probe/envMap slots, opaque browser-like image objects without readable or drawable pixel APIs, canvas-like image object uploads, OffscreenCanvas-backed image-like uploads, readable `texture.source.data` payloads, malformed source containers, mismatched raw payload lengths, and packed-depth raw texture type constants across material/background/environment/probe/envMap slots. Array/3D texture inputs, cube/PMREM material texture-slot inputs across mesh, sprite, point, line, and physical-material extension slots, opaque browser image objects without readable or drawable pixel APIs, encoded 2D/equirectangular texture uploads with `premultiplyAlpha === true`, and raw DataTexture inputs with mismatched payload lengths fail clearly across those slots. Raw material/background/current IBL half-float DataTextures decode binary16 texels, signed and unsigned integer DataTextures normalize texels for material/background and current environment/reflection-probe/material-envMap paths, and packed `UnsignedShort4444Type`/`UnsignedShort5551Type`/`UnsignedInt5999Type`/`UnsignedInt101111Type` DataTextures unpack color channels for material/background and current IBL paths before RGBA8 upload. Decode compressed textures to RGB/RGBA `DataTexture` data, an encoded PNG/JPEG/WebP image, a canvas-like object exposing `getContext("2d").getImageData()`, or an image-like object that an available `OffscreenCanvas`/2D canvas polyfill can draw and read with `getImageData()` before rendering; decode encoded images to raw RGBA when upload-time premultiplication is required. |

## Lighting, Shadows, And IBL

| Feature | Status | Notes |
|---|---|---|
| Ambient light | Supported | Summed scene ambient lights, camera-layer filtered; CSS string colors are accepted, while malformed color containers and invalid color/intensity values fail clearly. |
| Directional light | Supported | Position/target direction and intensity; CSS string colors are accepted, while malformed color/target containers and invalid color, intensity, and transform matrix values fail clearly, with direct conformance for invalid intensity. |
| Point light | Supported | Distance and decay attenuation; CSS string colors are accepted, while malformed color containers and invalid color, distance/decay values or ranges, intensity, and transform matrix values fail clearly, with direct conformance for invalid intensity. The generated browser-reference corpus covers point light shading in a combined point/spot fixture. |
| Spot light | Supported | Angle, penumbra, distance, and decay; CSS string colors are accepted, while malformed color/target containers and invalid color, angle/penumbra/distance/decay values or ranges, intensity, and transform matrix values fail clearly, with direct conformance for invalid intensity. The generated browser-reference corpus covers spot light shading in a combined point/spot fixture. |
| Hemisphere light | Supported | Sky/ground gradient ambient term; CSS string sky/ground colors are accepted, while malformed color containers and invalid sky/ground color, intensity, and transform matrix values fail clearly, with direct conformance for invalid intensity. |
| `RectAreaLight` | Partial | One-sided finite-area direct-light approximation using the light center, local `-Z` direction, width, and height. CSS string colors are accepted. Direct conformance covers front/back orientation, size-based radiance scaling, invalid intensity values, and invalid size/transform values; the browser-reference generator initializes Three.js' RectAreaLight uniforms and the generated corpus covers finite-area light shading. Invalid color values also fail clearly; exact Three.js LTC/LUT behavior remains planned. |
| Light layers | Supported | Lights are filtered against the active camera layers; invalid layer containers/masks fail clearly. |
| Light limit | Partial | Up to 64 visible non-ambient lights are supported and covered by scale tests. Larger visible non-ambient light sets fail clearly until native light arrays are expanded. |
| Shadows | Partial | Directional, spot, point, and up to four directional cascaded shadow maps are supported across a packed twelve-layer native shadow-map array. Directional and spot shadows use one layer, point shadows use six layers, and directional cascades use one layer per cascade; over-budget shadow-light sets fail clearly. Reusable `Renderer.shadowMap.enabled` defaults to true for current scene-oriented behavior and can suppress renderer-owned shadow maps when set false. `shadowMap.autoUpdate`, `shadowMap.needsUpdate`, and `shadowMap.transmitted` are accepted as validated compatibility state. |
| Shadow behavior parity | Partial | Supports common camera bounds, rectangular directional/spot/cascaded map sizes, up to four directional cascades, square point-light cube-face maps, multiple directional/spot/point shadow lights within the native layer budget, `Renderer.shadowMap.enabled` opt-out gating, `Renderer.shadowMap.type` Basic single-compare sampling plus PCF-family 3×3 sampling, bias, normal bias, `shadow.radius` PCF scaling, PCF-irrelevant `shadow.blurSamples` inputs, cast/receive flags, texture base-map and alpha-map alpha-tested casters, mesh/line/billboard `alphaHash` shadow-caster opacity cutouts, directional/spot/point sprite and point billboard shadow casters with source-material base-map/alpha-map cutouts, directional/spot/point line-object shadow casters with line material base-map/alpha-map cutouts, mesh custom depth/distance shadow material selected base-map and alpha-map UV-channel cutouts plus base-map and alpha-map texture transforms, displacement, visibility flags, and wireframe line-list shadows, source-material selected base-map and alpha-map UV-channel cutouts plus base-map and alpha-map texture transforms, `alphaHash` opacity, `alphaToCoverage` opacity cutouts, `shadowSide`, clipping, displacement, and wireframe with custom shadow materials, sprite/point billboard custom shadow material alpha cutouts, sprite custom-depth/custom-distance source/custom base-map and alpha-map texture transforms, and point-light point-billboard custom-distance source/custom base-map and alpha-map texture transforms plus selected geometry UV channels. Direct conformance covers native light extraction for shadow map sizing, map-size vector/dimension alias validation, bias, normal bias, radius, blur sample validation, camera bounds/ranges, malformed shadow flag/option containers, malformed renderer shadow-map booleans/type constants, renderer shadowMap Basic single-compare versus PCF/PCFSoft/VSM filtered type sampling, malformed light userData/cascade hint containers/values, multiple directional shadow maps, mixed point/directional shadow maps, renderer shadowMap gating, mesh base-map and alpha-map shadow cutouts, alphaToCoverage shadow-caster cutoff approximation including line and billboard casters, mesh/line/billboard `alphaHash` shadow opacity cutouts, mesh custom shadow selected base-map and alpha-map UV-channel cutouts plus base-map and alpha-map texture transforms, custom shadow material visibility, source-material selected base-map and alpha-map UV-channel cutouts plus base-map and alpha-map texture transforms, `alphaHash`, `alphaToCoverage` opacity cutouts, `shadowSide`, clipping, displacement, and wireframe with mesh custom shadow materials, Line/LineSegments/LineLoop directional/spot/point shadows, LineBasicMaterial and LineDashedMaterial base-map/alpha-map plus `alphaHash`/`alphaToCoverage` directional/spot/point shadow cutouts, billboard source-material base-map, alpha-map, `alphaHash`, and custom-shadow `alphaToCoverage` opacity cutouts, sprite custom/source base-map and alpha-map texture transforms for custom-depth/custom-distance shadows, point-light point-billboard custom-distance source/custom base-map and alpha-map texture transforms plus selected geometry UV-channel maps, and mesh custom shadow wireframes. The generated browser-reference corpus covers a directional sprite shadow-caster fixture, and renderer-only generated corpus coverage exercises multiple directional shadow maps and sprite/point billboard custom shadow alpha-map cutouts. Non-square point-light shadow map sizes, over-budget cascade hints, invalid custom shadow material visibility values, and over-budget shadow layer sets fail clearly; true shadow atlas allocation, broader custom shadow material translation, and VSM-specific blur behavior remain planned. |
| Environment IBL | Supported | CPU precomputes diffuse irradiance, prefiltered specular, and BRDF LUT from equirectangular byte/Float16/Float32 inputs, including one-channel grayscale and two-channel R/G raw data expansion from `texture.image` or `texture.source.data`, plus raw or encoded six-face cube reflection inputs, CubeUV-mapped six-face cube inputs, and packed 2D PMREM/CubeUV sharp/base-atlas inputs. Raw scene-environment, reflection-probe, and supported material-level IBL inputs honor `texture.premultiplyAlpha`. Standard/physical materials use diffuse/specular IBL, and Basic/Lambert/Phong material env maps use legacy multiply/mix/add combine modes with `reflectivity` and supported material-level reflection/refraction maps. Explicit `THREE.SRGBColorSpace`/`THREE.LinearSRGBColorSpace`, including the documented linear string aliases, is honored for scene-environment, reflection-probe, and supported material-level LDR inputs. Material-level `envMap` support is limited to one shared map and one shared rotation for Basic/standard/physical/Phong/Lambert material paths, while non-env-map material classes ignore `envMap` like Three.js; malformed environment/probe values, unsupported IBL texture input classes, raw data layouts/types, explicit mipmaps, color-space/legacy-encoding values, PBR material refraction mappings, and malformed packed 2D PMREM/CubeUV environment mappings fail clearly. Exact PMREM prefiltered LOD semantics remain planned beyond decoded sharp/base atlas faces. |
| Reflection probe hint | Supported | Uses `scene.userData.headlessThreeRenderer.reflectionProbe` or first probe in `reflectionProbes` when `scene.environment` is absent; malformed scene userData, hint containers, and probe-list hints fail clearly. |
| Environment intensity | Supported | `scene.environmentIntensity` and `options.environmentIntensity` for scene environments and reflection probes; invalid scene/probe/option intensity values fail clearly. |
| `LightProbe` | Partial | Spherical-harmonics coefficients from visible, camera-layer-matching `THREE.LightProbe` objects contribute diffuse indirect lighting, and `LightProbeGenerator.fromCubeRenderTarget()` can generate probes from renderer-owned cube targets through the WebGLRenderer marker/readback path. Invalid coefficient shapes and component values fail clearly. Direct conformance covers visibility filtering, camera layer filtering, Standard, Physical, Lambert, Phong, and Toon diffuse paths, scene-environment combinations across those lit material models, and helper cube-target readback, and the generated browser-reference corpus covers Lambert plus mixed Standard/Physical/Phong/Toon diffuse lighting. Exact golden-tested parity across the full material/environment matrix remains planned. |
| Environment/background rotation and intensity controls | Partial | `scene.environmentIntensity`, `options.environmentIntensity`, reflection-probe intensity, `scene.backgroundIntensity`, `options.backgroundIntensity`, `options.backgroundBlurriness`, approximate texture `backgroundBlurriness`, equirectangular/cube `scene.backgroundRotation`/`options.backgroundRotation`, and equirectangular/cube `scene.environmentRotation`/`options.environmentRotation` are supported for current background/environment texture mappings. Explicit option rotation values are validated independently of active maps. Direct conformance covers cube background/environment option rotation overrides, with generated renderer-only corpus fixtures for the same option paths. Invalid background/environment control or rotation values and unsupported background rotations fail clearly; exact blur parity and newer mapping controls remain planned. |

## Test Coverage

| Area | Status | Notes |
|---|---|---|
| Smoke tests | Supported | Module load, simple renders, output dimensions, reusable renderer, and reusable renderer state freshness for mutated mesh transform/material/background/environment/reflection-probe/transmission texture inputs. |
| Conformance invariants | Supported | Scene-level render invariants for materials, PBR, IBL, shadows, post-processing, lines, points, layers, and render order. |
| Scale budget tests | Supported | CI renders a 1,936-mesh scene, 2,048-group nested hierarchy with 256 visible meshes, mixed 144-mesh raw-texture/light scene, 7,056-instance scene, 225-map raw texture, 169-map encoded texture, 512 x 512 raw material texture, 512 x 512 output-size frame, generated NodePerformanceTest-shaped glTF graph, and supported 64-light budget scenes, and checks the clear failure path for larger visible non-ambient light sets. Covered budgets and platform notes are documented in [scale-budgets.md](./scale-budgets.md). |
| Packed artifact verification | Supported | CI packs the package and matching native binary across Linux x64, Linux arm64, macOS x64, macOS arm64, and Windows x64, installs them in clean temp projects, and renders a scene. |
| Node loader setup docs/helpers | Supported | Local `GLTFLoader` setup is documented for relative paths, POSIX/Windows absolute paths, `file://` URLs, external buffers, URI-encoded filenames with spaces, encoded image files, PNG/JPEG/WebP data URI images, helper-normalized GLB bufferView images, Blob URL image handlers for custom flows, and optional image polyfills. The package exports `loadGltfFromFile()`, `loadVrmFromFile()`, `loadVrmAnimationFromFile()`, `applyVrmAnimation()`, and `createNodeGltfLoader()` for higher-level local glTF/GLB/VRM/VRMA loading from relative paths, absolute paths, or `file://` URLs with plugin hooks and still-frame avatar animation posing from direct VRM/VRMA objects or loaded glTF wrappers, including mixer `setTime()`/`update()` seeking and `updateVrm=false`, plus lower-level encoded image texture loading with `load()`/`loadAsync()`, normalized relative/absolute/`file://` helper root directories and `setPath()` prefixes, optional `LoadingManager` item start/end/error notifications, and local `file://` fetch helpers; malformed helper boolean options, path values, option containers, plugin/loader callbacks, custom managers, avatar animation inputs/options, mixer/action contracts, glTF image metadata, remote asset/root URLs, unsupported data URI/Blob URL image types, and unsupported external or embedded compressed image payloads fail clearly. |
| Golden-image parity corpus | Partial | The repo has scene invariants, a generated no-crash/visibility corpus, a browser `THREE.WebGLRenderer` reference generator under `test/browser-reference/` that applies fixture-level background/environment controls, sort controls, viewport/scissor rectangles, clipping controls, browser-default `NoToneMapping` state, and mask/normal render-mode overrides for WebGLRenderer-compatible fixtures, an optional Playwright-driven headless wrapper for writing PNGs plus `manifest.json`, and a `test:golden` comparison gated by `HEADLESS_THREE_BROWSER_REFERENCE_DIR` or an auto-detected committed platform reference directory. Linux x64 has an initial committed browser-generated reference corpus with a default mean RGBA tolerance of `18` and no fixture-scoped tolerance exceptions; renderer-only corpus fixtures, including raw PBR material-envMap and ArrayCamera viewport composition coverage, remain in `test:corpus` and are omitted from the browser manifest. CI requires golden references on the Linux x64 matrix target and invokes the golden harness in no-reference skip mode on platforms without committed references; `HEADLESS_THREE_REQUIRE_BROWSER_REFERENCES=1` converts missing references into a clear failure when another platform is ready to require them. The [release checklist](./release-checklist.md) requires release notes to call out the golden-reference decision before tagging. Additional platform corpora and required all-platform CI comparison remain planned. |
| Curated external asset corpus | Partial | CI runs generated in-repo fixtures for transparent layers, custom blend equations/factors, 4x-MSAA alpha-to-coverage, renderer-only alpha-to-coverage alpha-test and clipping smoothing, stencil-masked render state, option-level background overrides, renderer-only ArrayCamera viewport composition, renderer-only CubeCamera face/update capture, render-option viewport/scissor clipping, global, material-local, and nested ClippingGroup planes, LightProbe Lambert and mixed Standard/Physical/Phong/Toon diffuse lighting, point/spot light shading, RectAreaLight shading, MeshBasicMaterial wireframe output, MeshDepthMaterial depth, renderer-only non-Basic depth-packing, displacement-map, and wireframe output, renderer-only MeshDistanceMaterial range, displacement-map, and wireframe output, MeshNormalMaterial flat, tangent/object-space normal-map, and bump-map output, MeshMatcapMaterial map plus renderer-only flat-shading, tangent/object-space normal-map, and bump-map lookup output, MeshToonMaterial renderer-only fallback bands, gradient-map, renderer-only normal/bump-map perturbation, and alpha-map cutout output, ShadowMaterial receiver shadows and fog opt-out, mapped SpriteMaterial billboards, renderer-only billboard alphaHash/alphaToCoverage cutouts, sprite alpha-map cutouts, sprite shadow casting, and sprite/point billboard custom shadow alpha-map cutouts, scene fog opt-out behavior, renderer-only signed raw material/background DataTextures, renderer-only narrow raw IBL, explicit texture-matrix/sRGB base-map sampling, linear output color-space conversion, renderer-only custom WGSL premultiplied-alpha output, renderer-side scene override-material output, simple mask, normal render-mode output, and renderer-only object-id and depth render-mode output, 2D, equirectangular, renderer-only raw, CubeUV cube, and packed CubeUV background textures, and renderer-only cube background option rotations, Phong, Basic/Lambert, PBR, renderer-only CubeUV cube and packed CubeUV material-level env maps, and renderer-only cube environment option rotations, custom grouped sort callbacks, skinning/morphs, avatar-style toon/skinned combinations, IBL/shadows, textured alpha-tested, renderer-only point UV-channel sampling, and instanced point billboards, basic lines, simple, renderer-only selected-UV-channel and wide-line, and textured alpha-tested dashed lines, renderer-only instanced texture UV streams, renderer-only renderable-object frustum culling, BatchedMesh instance colors, deleted/inactive geometry ranges, per-object culling, and custom sorting, LOD zoom selection/groups, and pathological geometry, plus glTF fixture coverage for local geometry/material loading, vertex-color attributes, morph targets, skinning attributes, encoded data-URI texture integration, GLB bufferView image integration, external image file resolution, external buffer resolution, committed Khronos glTF Sample Assets AlphaBlendModeTest, AnimatedCube, AnimatedColorsCube, AnimatedMorphCube, AnimatedTriangle, AnimationPointerUVs, AnisotropyBarnLamp, AnisotropyDiscTest, AnisotropyRotationTest, AnisotropyStrengthTest, AntiqueCamera, AttenuationTest, Avocado, BarramundiFish, BoomBox, BoomBoxWithAxes, Box, Box With Spaces, BoxAnimated, BoxInterleaved, BoxTextured, BoxTexturedNonPowerOfTwo, BoxVertexColors, BrainStem, Cameras, CarbonFibre, CesiumMan, CesiumMilkTruck, ChairDamaskPurplegold, ClearCoatTest, ClearCoatCarPaint, ClearcoatWicker, CommercialRefrigerator, CompareAlphaCoverage, CompareAmbientOcclusion, CompareAnisotropy, CompareBaseColor, CompareClearcoat, CompareDispersion, CompareEmissiveStrength, CompareIor, CompareIridescence, CompareMetallic, CompareNormal, CompareRoughness, CompareSheen, CompareSpecular, CompareTransmission, CompareVolume, Corset, CubeVisibility, DamagedHelmet, DirectionalLight, DispersionTest, DragonAttenuation, DragonDispersion, Duck, EmissiveStrengthTest, EnvironmentTest, FlightHelmet, Fox, GlamVelvetSofa, GlassBrokenWindow, GlassHurricaneCandleHolder, GlassVaseFlowers, InterpolationTest, IridescenceAbalone, IORTestGrid, IridescenceDielectricSpheres, IridescenceLamp, IridescenceMetallicSpheres, IridescenceSuzanne, IridescentDishWithOlives, Lantern, LightVisibility, LightsPunctualLamp, MaterialsVariantsShoe, MetalRoughSpheres, MetalRoughSpheresNoTextures, MeshPrimitiveModes, MeshoptCubeTest, MorphPrimitivesTest, MorphStressTest, MultiUVTest, MultipleScenes, NegativeScaleTest, NormalTangentMirrorTest, NormalTangentTest, OrientationTest, PlaysetLightTest, PointLightIntensityTest, PotOfCoals, PrimitiveModeNormalsTest, RecursiveSkeletons, RiggedFigure, RiggedSimple, SciFiHelmet, SheenChair, SheenCloth, SheenTestGrid, SimpleInstancing, SimpleMaterial, SimpleMeshes, SimpleMorph, SimpleSkin, SimpleSparseAccessor, SimpleTexture, SpecularSilkPouf, SpecularTest, SunglassesKhronos, Suzanne, TextureCoordinateTest, TextureEncodingTest, TextureLinearInterpolationTest, TextureSettingsTest, TextureTransformTest, TextureTransformMultiTest, ToyCar, TransmissionOrderTest, TransmissionRoughnessTest, TransmissionTest, TransmissionThinwallTestGrid, Triangle, TriangleWithoutIndices, TwoSidedPlane, Unicode❤♻Test, UnlitTest, VertexColorTest, VirtualCity, WaterBottle, and XmpMetadataRoundedCube fixtures covering external binary buffers, URI-encoded space-containing and Unicode buffer/image paths, byteStride interleaved attributes, imported cameras, imported-camera metallic/roughness sphere grids, imported `KHR_lights_punctual` directional, point, and spot lights, textured PlaysetLightTest directional/point lighting with emissive-strength lamp geometry, `KHR_node_visibility` filtering for imported mesh and punctual-light hierarchies, shared encoded JPEG/PNG texture reuse across double-sided transmission lamp materials, `KHR_materials_emissive_strength` factors, external animation buffers, textured quaternion animation, animation interpolation modes, material color animation pointers, source-level `KHR_animation_pointer` texture-transform tracks across physical material slots, animated morph weight tracks with normal targets, dense morph weight stress tracks, multi-primitive morph target meshes, two-bone skinned animation, full rigged figure bone animation, multi-primitive BrainStem skinned animation, textured character skinning, textured vehicle wheel animation with shared JPEG maps, textured animal skinning with multiple skeletal animation clips, recursive skinned hierarchies with normalized RGBA vertex colors, alpha modes, alpha coverage material variants, AO texture material pairs, anisotropy scalar/texture comparison pairs, real anisotropic carbon-fiber material maps, real anisotropic clearcoated barn-lamp maps with emissive filament and transmission glass, multi-mesh antique camera base-color/roughness/normal texture sets, single-mesh corset tangent-space ORM/normal maps, base-color factor, texture, and vertex-color material variants, textured vertex-color swatches, clearcoat/IOR comparison variants, clearcoat car-paint normal-map texture transforms, emissive coal and copper clearcoat texture maps, textured clearcoat wicker normal maps, dispersion comparison pairs, dispersion/IOR prism grids, real dispersion dragon thickness maps, dedicated IOR/specular/transmission grids, emissive strength comparison pairs, iridescence factor texture comparison pairs, iridescence dielectric and metallic IOR/thickness sphere grids, iridescence Suzanne thickness textures with punctual light, real abalone iridescence/thickness maps, iridescent dish transmission/volume/IOR glass with olives/gold-leaf PBR maps, real eyewear transmission/volume/IOR/iridescence lens materials, multi-material toy car clearcoat/emissive/fabric-sheen/transmission maps, required-transmission refrigerator glass with animated door and emissive interior maps, Unity-exported multi-mesh lantern PBR maps, scalar PBR material factors, dense textured PBR meshes, canonical packed PBR helmet maps, multi-material FlightHelmet PBR and transmission lens maps, separate sci-fi helmet AO/metallic-roughness/normal maps, Unity-exported PBR bottle maps with shared base-color/ORM/normal/emissive textures, metallic texture comparison pairs, normal-map comparison pairs, roughness texture comparison pairs, sheen factor comparison pairs, real transformed sheen cloth texture maps, velvet sofa sheen/specular variants with transformed normal maps and imported directional light, sheen color/roughness grids, specular factor/color texture pairs, real shot-silk specular/sheen material factors, transformed damask fabric sheen/specular material maps, broken-window transmission glass color-alpha/normal maps, hurricane candle-holder volume thickness maps, flower-vase alpha mask/transmission volume material pairs, alpha-versus-transmission material pairs, mixed alpha blend/mask/opaque/transmission ordering scenes, texture-driven transmission grids, thin-wall versus volume transmission/IOR grids, volume attenuation/thickness comparison pairs, attenuation distance and thickness-factor/texture grids, real attenuation dragon variants with thickness maps, packed metallic/roughness sphere-grid texture samples, scalar-only metalness/roughness sphere grids, `KHR_materials_anisotropy` texture maps over roughness ramps plus explicit anisotropy rotations, direction textures, and scalar strength grids, `KHR_materials_clearcoat` factor/roughness/normal maps, `KHR_materials_iridescence` thickness maps plus transmission/volume/IOR factors, `KHR_materials_transmission` maps with `KHR_materials_volume` thickness/attenuation, roughness-grid samples, and `KHR_materials_ior` factors, `KHR_materials_sheen` factors with transformed fabric textures and variant metadata, dedicated `KHR_materials_variants` shoe mappings, `KHR_materials_specular` scalar factors, HDR colors, and specular factor/color textures, meshopt-compression fallback buffers with KHR_mesh_quantization, primitive modes, primitive-mode normals/normalized color attributes, minimal indexed primitives, non-indexed primitives, organic tangent-space packed PBR meshes, normal-map and packed occlusion/roughness/metallic texture loading, explicit mirrored tangent attributes, negative determinant node transforms and front-face rendering, quaternion and matrix node rotations/translations, coordinate-system multi-mesh transform hierarchies, large multi-camera textured city scenes with animated nodes, shared mesh reuse across nodes, multiple glTF scenes/default scene selection, primary/secondary glTF UV routing for base-color and emissive textures, EXT_mesh_gpu_instancing, sparse accessors, glTF texture role color-space assignment for base/emissive versus metallic-roughness PNGs, sampler wrap/filter state including power-of-two and non-power-of-two PNG repeat/mipmap sampler state, axis-specific repeat/clamp/mirror modes, and linear min/mag filtering, material sidedness, mapped double-sided PBR planes, single-slot and multi-slot KHR_texture_transform across core, unlit, and clearcoat texture slots, KHR_materials_unlit, KHR_xmp and KHR_xmp_json_ld metadata packets, split external binary attribute/index buffers, and PNG/JPEG assets, minimal and generated humanoid synthetic committed VRM/VRMA extension fixtures, the VRM Consortium Seed-san practical full-avatar fixture, paired VRM Consortium VRMC_vrm expression conformance fixtures, and Pixiv three-vrm-animation's real VRMC_vrm_animation fixture. Runnable glTF/GLB and VRM/VRMA examples are included; broader glTF Sample Assets coverage and additional platform browser references remain planned. |

## Packaging

| Platform package | Status |
|---|---|
| `@headless-three/renderer-linux-x64-gnu` | Supported |
| `@headless-three/renderer-linux-arm64-gnu` | Supported |
| `@headless-three/renderer-darwin-x64` | Supported |
| `@headless-three/renderer-darwin-arm64` | Supported |
| `@headless-three/renderer-win32-x64-msvc` | Supported |

Published packages are verified on every supported CI platform by installing the packed root package and matching native package in a clean project, then rendering a real Three.js scene.
