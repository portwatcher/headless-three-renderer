# TODO: Three.js Scene Support Roadmap

## Goal

Render normal Three.js scene graphs in Node.js through the TypeScript adapter,
N-API, and the native `wgpu` renderer.

Non-goal: provide a WebGL context in Node.js or emulate the full
`THREE.WebGLRenderer` API/state machine. The public contract is scene input and
image/target output, not browser WebGL compatibility.

## Current Status

Current state: strong alpha for mesh-centric still rendering.

Already supported in the current implementation:

- `THREE.Scene` or `THREE.Object3D` root + `THREE.Camera` input, PNG/RGBA output through `Renderer.render()` plus Promise-wrapped `Renderer.renderAsync()` compatibility output, with malformed scene/children/camera/userData/option containers, unsupported format values, invalid explicit/aspect-derived dimensions, and invalid camera clipping/matrix containers or values failing clearly, reusable renderer with `setSize()`/`getSize()` and `setDrawingBufferSize()`/`getDrawingBufferSize()` output-size fallback, and target-like raw RGBA output with texture image/source writeback plus `Renderer.readRenderTargetPixels()`/`readRenderTargetPixelsAsync()` CPU readback from stored target, attachment, and cube-face data, including optional async buffer allocation and common-renderer async texture-index argument compatibility.
- Meshes, skinned meshes, sprites, geometry groups, vertex colors, indices, normals, UVs, object visibility flags, object `frustumCulled` sphere culling with opt-out, object world transforms, and common `THREE.BatchedMesh` packed-geometry CPU expansion with per-instance matrices, colors, visibility flags, deleted/inactive instance entries, deleted/inactive packed geometry ranges, optimized repacked active geometry ranges, packed geometry groups/material arrays including non-indexed group ranges and partial range/group intersections, group sort metadata when present, per-object sphere frustum culling, range-local internal sort centers including transparent material-array groups, `sortObjects`, and `customSort` callback context/camera/list handling, with malformed geometry attribute/data and object/geometry bounding-sphere containers, invalid geometry attribute/index/group/draw-range, malformed material containers, visibility, transform matrix values, malformed BatchedMesh instance table entries, malformed or out-of-bounds BatchedMesh geometry ranges, cached culling bounds, and culling/sort controls failing clearly.
- `THREE.InstancedMesh` CPU expansion with `instanceMatrix` and `instanceColor`, with invalid explicit instance counts and malformed instance matrix/color attributes failing clearly.
- `THREE.InstancedBufferGeometry` CPU expansion for mesh, point, and line geometry with common per-instance offset, color, and mesh normal attributes plus instanced UV attributes for built-in texture streams, including dashed-line map UV expansion, Three.js' default `instanceCount = Infinity`, `meshPerAttribute` repeat values, dashed line expansion, invalid explicit instance counts, malformed instanced attributes, or per-attribute repeat values failing clearly, and custom WGSL fragment materials failing clearly when paired with unsupported arbitrary instanced vertex attributes.
- CPU skinning and morph target baking before native rendering, with malformed mesh/skeleton containers, invalid skinning matrix values, malformed morph attribute containers, morph target influence values, and malformed morph mode flags failing clearly.
- `MeshBasicMaterial`, `MeshDepthMaterial` with supported depth-packing modes and clear failures for unsupported depth-packing constants, `MeshDistanceMaterial`, `MeshLambertMaterial`, `MeshMatcapMaterial`, `MeshNormalMaterial`, `MeshPhongMaterial`, `MeshToonMaterial`, `ShadowMaterial`, `MeshStandardMaterial`, and a substantial `MeshPhysicalMaterial` subset, including common main-pass mesh wireframe output.
- Base color, opacity, material visibility, scene override materials for normal color renders, premultiplied alpha, base/sprite/point/line/dashed-line/matcap/normal/bump/displacement/metallic/roughness/emissive/AO/light/Phong specular/alpha/toon gradient/physical color map, supported background texture, and equirectangular/cube scene-environment and reflection-probe LDR IBL sRGB/linear color-space handling with documented linear aliases and unsupported color-space/legacy-encoding failures, render-option and reusable `Renderer.outputColorSpace` plus `currentColorSpace` and `_outputColorSpace` output color-space controls, base color `flipY`, raw material/background/current IBL texture `premultiplyAlpha`, tangent-space and object-space normal maps with `normalScale`, bump maps with `bumpScale`, metallic/roughness/specular maps, AO/light maps with selected UV channels, Phong specular maps with selected UV channels, toon gradient maps, alpha/displacement maps, alpha test with invalid values failing clearly, common material CSS string and color-like color controls plus scalar/vector controls with malformed color containers and invalid values failing clearly, alpha hash, vertex-color and size-attenuation controls with invalid values failing clearly, transparency sorting, side/culling with clear failures for unsupported side constants, texture wrap modes, valid `unpackAlignment` compatibility state for tightly packed readable uploads, one-channel/two-channel/RGB/RGBA raw material/background/current IBL texture payloads from `texture.image` or `texture.source.data` plus canvas-like `getContext("2d").getImageData()` image payloads and OffscreenCanvas-backed image-like payloads, including direct one/two-channel map, background, scene-environment, reflection-probe, material-envMap, source-backed payload, canvas-like payload, and OffscreenCanvas-backed payload conformance plus byte, signed/unsigned normalized integer, packed 16-bit color, packed RGB9_E5/R11F_G11F_B10F color, float, and half-float typed data, and clear failures for malformed raw texture channel layouts.
- Base color, supported background, sprite/point color and alpha, line/dashed-line, normal, bump, displacement, metallic/roughness, emissive, AO, light, specular, alpha, and physical-extension map texture UV transforms (`offset`, `repeat`, `rotation`, `center`, `flipY`, and explicit matrix), with malformed transform vector containers and invalid transform or transform-boolean values failing clearly and direct explicit-matrix conformance for base/background, sprite/point color and alpha, line/dashed-line, common mesh material maps, and current physical-extension maps.
- Base/background, normal/bump, displacement, metallic/roughness, emissive, AO/light, alpha, Phong specular, toon gradient, matcap color-map, and packed physical-extension texture-group sampler wrap modes, `magFilter`/`minFilter` (`NearestFilter`/`LinearFilter` family), generated mip chains for mipmap min filters, explicit raw mipmap chains for unpacked 2D material/background texture uploads, compatible native texture anisotropy, and clear failures for unsupported sampler constants, invalid mipmap controls, malformed explicit mipmap containers, or invalid anisotropy values, including direct nearest/linear filter conformance across current physical-extension maps and repeat/mirrored wrap conformance for iridescence factor/thickness maps.
- Material `depthTest`, `depthFunc`, `depthWrite`, `colorWrite`, `polygonOffset`, `alphaHash`, `alphaToCoverage` on 4x MSAA renders including output-alpha and alpha-test threshold coverage, `premultipliedAlpha`, `toneMapped=false` output opt-out, `forceSinglePass` accepted as a native single-pass no-op, stencil state, built-in blending modes (`NoBlending`, `NormalBlending`, `AdditiveBlending`, `SubtractiveBlending`, and `MultiplyBlending`), custom blend equations/factors in the main pass including min/max equations, separate alpha equation/factors, and constant color/alpha factors, and clear failures for unsupported render-state constants and invalid render-state boolean/numeric values.
- Material-local `clippingPlanes`, `THREE.ClippingGroup`, render-option global `clippingPlanes`, plus reusable `Renderer.clippingPlanes` global fallback state, up to eight world-space planes with malformed plane containers, invalid plane values, invalid clipping controls, invalid `options.localClippingEnabled`/`Renderer.localClippingEnabled`, and over-budget inputs failing clearly, including local/group `clipIntersection`, material/group `clipShadows` for mesh and line shadow casters, source-material clipping state on custom shadow casters, alpha-to-coverage smoothing for MSAA main-pass clipping edges, and render-option plus reusable-renderer `localClippingEnabled` opt-out.
- Clearcoat, sheen, anisotropy, scalar iridescence with factor/thickness maps, specular intensity/color, roughness-aware scene-color transmission/thickness, IOR, attenuation, approximate dispersion, and related physical material maps, with malformed physical color containers and invalid physical color/scalar/vector/range values failing clearly.
- Ambient, directional, point, spot, hemisphere, RectAreaLight approximation, and LightProbe diffuse lighting, up to 64 direct lights, with CSS string light colors, malformed light color/target containers, invalid light color/numeric/range/transform matrix values, and malformed LightProbe coefficients failing clearly, including direct conformance for invalid intensity values across current direct-light classes, LightProbe plus scene-environment material combinations, `LightProbeGenerator.fromCubeRenderTarget()` cube-target readback through the WebGLRenderer marker path, and generated browser-reference corpus coverage for LightProbe Lambert/mixed material diffuse lighting, combined point/spot lighting, and finite RectAreaLight shading.
- Environment IBL from equirectangular byte/Float16/Float32 or cube and packed 2D CubeUV sharp-atlas `scene.environment`/reflection probes, plus single shared material-level env maps for supported IBL material paths, including one-channel/two-channel/RGB/RGBA raw equirectangular data expansion, `MeshBasicMaterial`/`MeshPhongMaterial`/`MeshLambertMaterial` legacy combine/reflectivity/refraction handling with env-map rotation, and PBR Standard/Physical env-map inputs including packed CubeUV sharp/base-atlas envMaps, with malformed scene-environment/reflection-probe values, malformed scene userData/reflection-probe hint containers/arrays, unsupported IBL texture input classes, raw data layouts/types, explicit mipmaps, color-space/legacy-encoding values, and invalid material-envMap scalar/rotation values failing clearly; the generated browser-reference corpus covers Phong and shared Basic/Lambert material-level env maps, while renderer-only corpus coverage exercises native PBR material-level env maps.
- Scene background colors plus reusable `Renderer.setClearColor()`/`setClearAlpha()` fallback state, including hex, CSS string, and color-like clear colors, CSS string scene/option background colors, and 2D, equirectangular, raw or encoded six-face cube, and packed 2D CubeUV sharp-atlas texture backgrounds, including RGBA color clears, `options.background: null` clearing to renderer clear state, option background overrides ignoring scene background controls, `backgroundIntensity`, approximate texture `backgroundBlurriness`, `options.environmentIntensity`, equirectangular/cube `scene.backgroundRotation`/`options.backgroundRotation`, and equirectangular/cube `scene.environmentRotation`/`options.environmentRotation`; malformed scene/option background values, malformed packed CubeUV layouts, explicit option rotations, invalid background/environment control and rotation values, invalid renderer clear-color state, and unsupported background rotations fail clearly, with direct conformance for RGBA color clear alpha, renderer clear-color fallback and explicit background precedence, CSS background colors, option texture transform/control isolation, and cube option rotation overrides, generated renderer-only corpus coverage for cube background/environment option rotation overrides, and generated browser-reference corpus coverage for color background overrides.
- Render-option, render-target, and reusable `Renderer.setSize()`/`getSize()` plus `setDrawingBufferSize()`/`getDrawingBufferSize()` and `setPixelRatio()`/`getPixelRatio()` compatibility state and `setViewport()`/`getViewport()`/`getCurrentViewport()` plus `setScissor()`/`setScissorTest()` state in output pixel coordinates, including Vector2/Vector4-style inputs, default-only common-renderer viewport depth-range validation, getter clone/target population, target dimension precedence over renderer size fallback, disabled target scissor tests, sized/scissored active-render-target `Renderer.clear()`/`clearColor()`/`clearDepth()` color/depth writeback plus packed depth-stencil stencil-byte writeback including cube faces/mips, sized `clearTarget()` writeback, source-rectangle `copyFramebufferToTexture()` base/mip and active cube-face/mip CPU copies, raw/canvas-like/OffscreenCanvas-backed source `copyTextureToTexture()` base/mip CPU copies into raw destinations, read-only WebGL `Renderer.coordinateSystem`, inert `Renderer.info` compatibility counters with WebGLInfo- and CommonRenderer-style `update()`/`reset()`/`dispose()`, inert `Renderer.debug` shader-diagnostic state, inert `Renderer.inspector` lifecycle and copy-hook state, inert `Renderer.domElement` output/client/offset-size/bounding-rect/style mirror plus style-property, attribute, event-target, and `ViewHelper.render()` probes with clear context/export/capture failures, conservative `Renderer.capabilities` probes including WebGL limit fields, draw-buffer limits, texture support flags, and renderer-level `getMaxAnisotropy()`, and conservative `hasFeature()`/`hasFeatureAsync()`/`hasCompatibility()` plus `isOccluded()` probes, inert `Renderer.extensions`, scratch `Renderer.properties`/`renderLists` helpers, inert `Renderer.state` buffer and state-level setter probes with raw WebGL binding/upload failures, inert `Renderer.xr` enabled/cameraAutoUpdate/framebuffer-scale/controller/reference-space/session/base-layer/binding/frame/environment/depth-texture/depth/foveation/camera-texture/event-target dispatch probes for `CubeCamera.update()` and adjacent helper compatibility with real session binding failing clearly, validated inert WebGLRenderer-style constructor parameters for common context attributes plus read-only `alpha`/`depth`/`stencil`/`logarithmicDepthBuffer` metadata, CommonRenderer-style `isRenderer`/`initialized` readiness probes, `highPrecision=false` metadata with `true` failing clearly, `samples`/`currentSamples`/`isOutputTarget` sample/output-target probes, default `outputBufferType` with `getOutputBufferType()`/`getColorBufferType()`, cloned `getContextAttributes()` readback, and clear `getContext()` unsupported failures, clear color/depth/stencil value state, material-set `compile()`/`compileAsync()` compatibility hooks, no-op default-framebuffer/unsized-target `clear()`/`clearAsync()` behavior plus `init()`/`initRenderTarget()`/`initTexture()`/`initTextureAsync()`/`hasInitialized()`/`clearTarget()`/`clearColor()`/`clearColorAsync()`/`clearDepth()`/`clearDepthAsync()`/`clearStencil()`/`clearStencilAsync()`/`resetState()`/`resetGLState()`/`dispose()`/`forceContextLoss()`/`forceContextRestore()`/`setAnimationLoop()`/`getAnimationLoop()` hooks for pass-owned buffers and object-lifetime cleanup, and boolean-validated no-op `autoClear`/`autoClearColor`/`autoClearDepth`/`autoClearStencil` flags, plus 4x MSAA render-option/target sample counts resolved back to normal output and target readback buffers, with invalid sizes, size updateStyle flags, pixel ratios, rectangles, non-default viewport depth ranges, constructor parameters including constructor-level `samples > 1` or non-default `outputBufferType`, target scissor flags, compile material values, info booleans/update values, debug values, inspector values, capability/extension/property/render-list/state probes, DOM style/attribute/event/canvas probes, XR booleans/values/events/controller/reference-space binding, clear values/booleans, resource-init targets/textures, external WebGL texture/framebuffer handles, copy targets, unsupported texture-copy inputs, missing active framebuffer copy targets, animation-loop callbacks, auto-clear flags, and sample counts failing clearly; external framebuffer/texture handle APIs and no-active-target framebuffer copies fail clearly as unsupported.
- `Renderer.setEffects()` is accepted as a validated no-op WebGLRenderer compatibility hook; actual image effects stay explicit through per-render `options.postProcessing`, and malformed effect containers fail clearly.
- `Renderer.transmissionResolutionScale` is stored as validated positive finite WebGLRenderer compatibility state and scales the scene-color texture sampled by physical transmission.
- `Renderer.isWebGLRenderer` is exposed as a compatibility marker for Three.js helpers that branch on WebGLRenderer-style target readback, including covered `LightProbeGenerator.fromCubeRenderTarget()` cube-target readback, simple `EffectComposer`/`RenderPass` target-state readback, `ClearPass` active-target clear-state flows, standard CopyShader `ShaderPass`/`TexturePass`/`SavePass` fullscreen flows, `OutputPass` fullscreen output-copy flow, CopyShader additive accumulation through `SSAARenderPass` and low-opacity `TAARenderPass` samples, `StereoEffect` scissored eye-camera renders, `Reflector`/`Refractor` helper-owned target prepasses, plus `EXRExporter`/`KTX2Exporter` render-target exports, while WebGL context access remains explicitly unsupported and `MaskPass` fails clearly through that path.
- `Renderer.state` exposes inert `buffers.color`, `buffers.depth`, and `buffers.stencil` setter/lock/reset probes, state-level blending/material/flip-sided/cull-face/line-width/polygon-offset/scissor-test/scissor/viewport probes, `buffers.depth.getReversed()`/`setReversed(false)` compatibility state with reversed-depth enablement failing clearly, no-op `reset()`/`unbindTexture()` hooks, and clear failures for raw WebGL capability, framebuffer, draw-buffer, program, texture binding/upload/storage, and uniform-buffer state methods.
- `Renderer.debug.getShaderAsync()` exists as a validated async clear-failure probe because generated backend shader source is not exposed by the scene-oriented native renderer.
- `Renderer.onDeviceLost` is assignable CommonRenderer callback state with a default no-op callback and clear validation; native device-loss events are not surfaced by the scene-oriented renderer.
- `Renderer.renderBufferDirect()`, `Renderer.renderObject()`, non-null `Renderer.setRenderObjectFunction()`, non-null `Renderer.setOutputRenderTarget()`/`setCanvasTarget()`, direct `Renderer.setTexture2D()`/`setTextureCube()`/`setTextureCubeDynamic()`/`setTexture3D()`/`setTexture2DArray()`, `Renderer.copyTextureToTexture3D()`, `Renderer.compute()`/`computeAsync()`, `Renderer.getArrayBufferAsync()`, `Renderer.resolveTimestampsAsync()`, and `Renderer.waitForGPU()` calls exist and fail clearly as unsupported low-level WebGL/WebGPU buffer/program/texture-binding, texture-layer copy, common output/canvas target, compute, storage-buffer readback, timestamp-query, GPU-sync, or renderer-internal render-object dispatch paths; `Renderer.getRenderObjectFunction()` returns `null`, `Renderer.setRenderObjectFunction(null)` is accepted as a clear operation, `Renderer.getOutputRenderTarget()`/`getCanvasTarget()` return `null`, `Renderer.setOutputRenderTarget(null)`/`setCanvasTarget(null)` are accepted as clear operations, and scene rendering remains through `Renderer.render()` and `renderToTarget()`.
- `THREE.ArrayCamera` sub-camera viewport composition for PNG, RGBA, and target output, including depth and object-id target metadata, plus `THREE.CubeCamera` six-face cube color/depth/object-id target rendering, including active mip target writeback, active-mip target scissor clipping, scissored active cube-face/mip clear preservation, explicit cube-face color readback, captured color texture reuse as cube background/environment inputs, and object-id reverse lookup metadata, with malformed nested camera containers failing clearly.
- Flat auxiliary `options.renderMode` passes for white masks, RGB object IDs, view-space normals, and normalized depth grayscale, with base texture, alpha-map, and `alphaHash` cutouts, direct conformance for post-processing bypass, invalid render mode values failing clearly, generated browser-reference corpus coverage for simple mask plus normal render-mode output, and renderer-only generated corpus coverage for object-id plus depth output.
- Directional, spot, point, and up to four directional cascaded shadow maps can be packed across up to twelve native shadow-map array layers, including multiple visible shadow-casting directional/spot lights and point-light combinations; reusable `Renderer.shadowMap.enabled` defaults to true for current scene-oriented behavior and can suppress renderer-owned shadow maps, while `shadowMap.autoUpdate`/`needsUpdate` compatibility state is accepted and `shadowMap.type` selects Basic single-compare sampling or PCF-family 3×3 sampling; common PCF shadow options including bias, normal bias, `radius`, and PCF-irrelevant `blurSamples` inputs are accepted, with malformed shadow flags/containers, invalid renderer shadow-map booleans/type constants, invalid shadow numeric/range values, over-budget shadow layer sets, and malformed light userData/cascade hints failing clearly; explicit mesh `material.shadowSide` face filtering, `alphaToCoverage` shadow-caster cutoff approximation, mesh/line/billboard `alphaHash` shadow-caster opacity cutouts, directional/spot/point line-object shadow casting with line material base-map/alpha-map cutouts, directional/spot/point sprite and point billboard shadow casting with source-material base-map/alpha-map cutouts, mesh custom depth/distance shadow material selected base-map and alpha-map UV-channel cutouts plus base-map and alpha-map texture transforms, displacement, visibility flags, and wireframe line-list shadows, source-material selected base-map and alpha-map UV-channel cutouts plus base-map and alpha-map texture transforms, `alphaHash` opacity, `alphaToCoverage` opacity cutouts, `shadowSide`, clipping, displacement, and wireframe with custom shadow materials, sprite/point billboard custom shadow material alpha cutouts, sprite custom-depth/custom-distance source/custom base-map and alpha-map texture transforms, and point-light point-billboard custom-distance source/custom base-map and alpha-map texture transforms plus selected geometry UV channels are honored.
- `Renderer.shadowMap.transmitted` is accepted as validated boolean compatibility state alongside `shadowMap.autoUpdate`, `shadowMap.needsUpdate`, and `shadowMap.type`; it currently does not change scene output because transmitted shadow-map passes are not modeled separately.
- ACES Filmic, Linear, Reinhard, Cineon, default identity Custom, AgX, and Neutral tone mapping with reusable `Renderer.toneMapping`/`currentToneMapping`/`toneMappingExposure` state plus `NoToneMapping` opt-out and `Renderer.needsFrameBufferTarget=false` inline-output probe, render-option and reusable `Renderer.outputColorSpace`/`currentColorSpace`/`_outputColorSpace` output color-space controls, post-processing exposure, contrast, saturation, vignette, grayscale, invert, and `enabled=false` opt-out controls with malformed containers and invalid effect values failing clearly, custom WGSL fragment bodies with premultiplied-alpha output wrapping and malformed override values failing clearly, lines, and point billboards.
- `THREE.Sprite`/`SpriteMaterial` CPU billboard expansion with color, opacity, texture maps, scene fog, center, scale, rotation, size attenuation, layers, render ordering, directional/spot/point shadow casting with source-material base-map/alpha-map cutouts, directional/spot custom-depth and point custom-distance shadow cutouts, and WebGL-compatible unlit `receiveShadow` no-op behavior, with invalid billboard center/scale/rotation and size-attenuation values failing clearly and generated browser-reference corpus coverage for a mapped rotated billboard plus a directional sprite shadow caster and renderer-only generated corpus coverage for alpha-map and alphaHash/alphaToCoverage cutouts.
- Line material arrays with geometry groups, including dashed material-array groups with active dash gaps, WebGL-compatible `linecap`/`linejoin` and unlit `receiveShadow` no-ops, plus `LineDashedMaterial` dash/gap/scale patterns including `scale=0` solid-line behavior and custom `lineDistance` attributes including descending spans for common line, line segment, line loop, and instanced line cases, with invalid line scalar values, invalid line cap/join strings, and malformed line-distance attributes failing clearly.
- Object/camera layer filtering and `frustumCulled` sphere culling for renderable objects plus layer filtering for lights, plus native draw-order keys for group order, `renderOrder`, material id, WebGL material variant, transmissive/transparent buckets, projected object-or-geometry bounding-sphere z, object/insertion ties, `sortObjects`, and custom opaque/transparent sort callbacks with object/material/geometry/group plus group-order/render-order/z/material-variant render-item metadata, with invalid layer containers/masks, `frustumCulled`, `renderOrder`, sort option, and reusable-renderer setter values failing clearly.
- `Renderer.opaque` and `Renderer.transparent` default to true and gate native opaque plus transmissive/transparent render buckets as CommonRenderer compatibility flags, including transmissive bucket gating, with invalid flag values failing clearly.
- `THREE.LOD` auto-update selection for the active camera, including distance, camera zoom, and hysteresis threshold behavior, with invalid auto-update flags, camera zoom, malformed level entries, or level distance/hysteresis values failing clearly.
- `THREE.Fog` and `THREE.FogExp2` in mesh, shadow, sprite, point, and line material paths, using view-space depth fog with CSS string fog colors, `material.fog` opt-out, malformed fog objects/color containers, and invalid fog color/parameter values failing clearly, including direct Fog/FogExp2 mesh, shadow receiver, sprite, point, and line conformance plus generated browser-reference corpus coverage for linear fog opt-out behavior.
- Packaged artifacts are built and install-verified in CI across Linux x64, Linux arm64, macOS x64, macOS arm64, and Windows x64.

Distance to the project goal:

- Ready for many static or pre-animated glTF/VRM-style render jobs.
- Not yet full Three.js scene compatibility.
- The remaining work is mostly breadth, edge-case parity, conformance testing, packaging hygiene, and performance, not one single missing renderer feature.

Treat the goal as achieved only when a published compatibility matrix and golden-image regression corpus pass across the supported platforms.

## P0 - Contract, Tests, And Packaging

| Task | Impact | Notes |
|---|---:|---|
| Publish compatibility matrix | High | `docs/compatibility.md` is linked from the root README, documents supported/partial/unsupported feature areas, links to a versioned Khronos glTF Sample Asset Coverage list, and lists every packaged platform target from `packages/renderer/package.json`. `docs/release-checklist.md` requires release notes to call out compatibility changes, golden-reference decisions, and fixture-scoped golden tolerance updates before tagging, and `test:docs` validates public Markdown file links, keeps the sample-asset list synchronized with the committed `gltf.test.mjs` fixture constants, verifies CI and the compatibility matrix stay synchronized with package platform targets, and verifies the release checklist keeps those gates documented. Remaining work is extending the browser-generated golden reference corpus beyond Linux x64 enough to graduate golden parity from partial. |
| Add golden-image parity tests | High | `test/browser-reference/` renders the generated corpus with browser Three.js/WebGLRenderer as an offline reference generator, applies fixture-level background/environment controls, pre-clears each frame for deterministic viewport/scissor references, records browser-default `NoToneMapping` state in the manifest, exposes automation results for a Playwright-driven headless generator, downloads a `manifest.json` describing the Three.js revision, renderer state, and fixture list, and `test:golden` validates that manifest before comparing saved PNG references when `HEADLESS_THREE_BROWSER_REFERENCE_DIR` is set or the platform default reference directory exists. A browser-generated Linux x64 reference corpus is committed with default mean RGBA tolerance `18` and no fixture-scoped tolerance exceptions; renderer-only classifications cover raw PBR material-envMap and ArrayCamera viewport composition coverage that the current WebGL reference corpus does not exercise, CI requires it on the Linux x64 matrix target, and `HEADLESS_THREE_REQUIRE_BROWSER_REFERENCES=1` turns missing references into a clear failure when another platform is ready to require them. Production runtime remains Node + `wgpu`; remaining work is adding platform-scoped corpora for the other supported targets and enabling required all-platform golden comparison in CI. |
| Build a curated scene corpus | High | Initial generated invariant corpus covers transparent materials, custom blend equations/factors, 4x-MSAA alpha-to-coverage, renderer-only alpha-to-coverage alpha-test and clipping smoothing, stencil-masked render state, option-level background overrides, renderer-only ArrayCamera viewport composition, renderer-only CubeCamera face/update capture, render-option viewport/scissor clipping, global, material-local, and nested ClippingGroup planes, LightProbe Lambert and mixed Standard/Physical/Phong/Toon diffuse lighting, point/spot light shading, RectAreaLight shading, MeshBasicMaterial wireframe output, MeshDepthMaterial depth, renderer-only non-Basic depth-packing, displacement-map, and wireframe output, renderer-only MeshDistanceMaterial range, displacement-map, and wireframe output, MeshNormalMaterial flat, tangent/object-space normal-map, and bump-map output, MeshMatcapMaterial map plus renderer-only flat-shading, tangent/object-space normal-map, and bump-map lookup output, MeshToonMaterial renderer-only fallback bands, gradient-map, renderer-only normal/bump-map perturbation, and alpha-map cutout output, ShadowMaterial receiver shadows with tint validation and fog opt-out, mapped SpriteMaterial billboards, renderer-only billboard alphaHash/alphaToCoverage cutouts, sprite alpha-map cutouts, sprite shadow casting, and sprite/point billboard custom shadow alpha-map cutouts, scene fog opt-out behavior, renderer-only signed raw material/background DataTextures, renderer-only narrow raw IBL, explicit texture-matrix/sRGB base-map sampling, linear output color-space conversion, renderer-only custom WGSL premultiplied-alpha output, renderer-side scene override-material output, simple mask, normal render-mode output, and renderer-only object-id and depth render-mode output, 2D, equirectangular, renderer-only raw, CubeUV cube, and packed CubeUV background textures, and renderer-only cube background option rotations, Phong, Basic/Lambert, PBR, renderer-only CubeUV cube and packed CubeUV material-level env maps, and renderer-only cube environment option rotations, custom grouped sort callbacks, skinning, morphs, avatar-style toon/skinned combinations, IBL, shadows including renderer-only multiple directional shadows, textured alpha-tested, renderer-only point UV-channel sampling, and instanced point billboards, basic lines, simple, renderer-only selected-UV-channel and wide-line, and textured alpha-tested dashed lines, renderer-only instanced texture UV streams, renderer-only renderable-object frustum culling, BatchedMesh instance colors, deleted/inactive geometry ranges, per-object culling, and custom sorting, LOD zoom selection/groups, and pathological geometry in CI, and generated NodePerformanceTest-shaped 10k-node/mesh/material/texture-definition glTF loader stress; optional browser-reference comparison covers the WebGLRenderer-compatible generated fixtures and skips renderer-only fixtures that browser WebGL cannot reference; glTF tests cover local geometry/material loading, vertex-color attributes, morph targets, skinning attributes, encoded data-URI textures, GLB bufferView images, external image files, external buffer resolution, committed Khronos glTF Sample Assets ABeautifulGame, AlphaBlendModeTest, AnimatedCube, AnimatedColorsCube, AnimatedTriangle, AnimationPointerUVs, AnisotropyBarnLamp, AnisotropyDiscTest, AnisotropyRotationTest, AnisotropyStrengthTest, AntiqueCamera, AttenuationTest, Avocado, BarramundiFish, BoomBox, BoomBoxWithAxes, Box, Box With Spaces, BoxAnimated, BoxInterleaved, BoxTextured, BoxTexturedNonPowerOfTwo, BoxVertexColors, BrainStem, Cameras, CarbonFibre, CarConcept, CesiumMan, CesiumMilkTruck, ChairDamaskPurplegold, ChronographWatch, ClearCoatTest, ClearCoatCarPaint, ClearcoatWicker, CommercialRefrigerator, CompareAlphaCoverage, CompareAmbientOcclusion, CompareAnisotropy, CompareBaseColor, CompareClearcoat, CompareDispersion, CompareEmissiveStrength, CompareIor, CompareIridescence, CompareMetallic, CompareNormal, CompareRoughness, CompareSheen, CompareSpecular, CompareTransmission, CompareVolume, Corset, Cube, CubeVisibility, DamagedHelmet, DirectionalLight, DiffuseTransmissionPlant, DiffuseTransmissionTeacup, DiffuseTransmissionTest, DispersionTest, DragonAttenuation, DragonDispersion, Duck, EmissiveStrengthTest, EnvironmentTest, FlightHelmet, Fox, GlamVelvetSofa, GlassBrokenWindow, GlassHurricaneCandleHolder, GlassVaseFlowers, InterpolationTest, IridescenceAbalone, IORTestGrid, IridescenceDielectricSpheres, IridescenceLamp, IridescenceMetallicSpheres, IridescenceSuzanne, IridescentDishWithOlives, Lantern, LightVisibility, LightsPunctualLamp, MandarinOrange, MaterialsVariantsShoe, MetalRoughSpheres, MetalRoughSpheresNoTextures, MeshPrimitiveModes, MeshoptCubeTest, MorphPrimitivesTest, MorphStressTest, MosquitoInAmber, MultiUVTest, MultipleScenes, NegativeScaleTest, NormalTangentMirrorTest, NormalTangentTest, OrientationTest, PlaysetLightTest, PointLightIntensityTest, PotOfCoals, PotOfCoalsAnimationPointer, PrimitiveModeNormalsTest, RecursiveSkeletons, RiggedFigure, RiggedSimple, ScatteringSkull, SciFiHelmet, SheenChair, SheenCloth, SheenTestGrid, SheenWoodLeatherSofa, SimpleInstancing, SimpleMaterial, SimpleMeshes, SimpleMorph, SimpleSkin, SimpleSparseAccessor, SimpleTexture, SpecGlossVsMetalRough, SpecularSilkPouf, SpecularTest, Sponza, StainedGlassLamp, SunglassesKhronos, Suzanne, TextureCoordinateTest, TextureEncodingTest, TextureSettingsTest, TextureTransformTest, TextureTransformMultiTest, ToyCar, TransmissionOrderTest, TransmissionRoughnessTest, TransmissionTest, TransmissionThinwallTestGrid, Triangle, TriangleWithoutIndices, TwoSidedPlane, Unicode❤♻Test, UnlitTest, USDShaderBallForGltf, VertexColorTest, VirtualCity, WaterBottle, and XmpMetadataRoundedCube fixtures covering external binary buffers, canonical textured cube geometry, URI-encoded space-containing and Unicode buffer/image paths, byteStride interleaved attributes, imported cameras, imported-camera metallic/roughness sphere grids, imported `KHR_lights_punctual` directional, point, and spot lights, textured PlaysetLightTest directional/point lighting with emissive-strength lamp geometry, `KHR_node_visibility` filtering for imported mesh and punctual-light hierarchies, shared encoded JPEG/PNG texture reuse across double-sided transmission lamp materials, `KHR_materials_emissive_strength` factors, external animation buffers, textured quaternion animation, animation interpolation modes, material color animation pointers, source-level `KHR_animation_pointer` texture-transform tracks across physical material slots, animated morph weight tracks with normal targets, dense morph weight stress tracks, multi-primitive morph target meshes, two-bone skinned animation, full rigged figure bone animation, multi-primitive BrainStem skinned animation, textured character skinning, textured vehicle wheel animation with shared JPEG maps, textured animal skinning with multiple skeletal animation clips, recursive skinned hierarchies with normalized RGBA vertex colors, alpha modes, alpha coverage material variants, AO texture material pairs, anisotropy scalar/texture comparison pairs, real anisotropic carbon-fiber material maps, real anisotropic clearcoated barn-lamp maps with emissive filament and transmission glass, multi-mesh antique camera base-color/roughness/normal texture sets, single-mesh corset tangent-space ORM/normal maps, base-color factor, texture, and vertex-color material variants, textured vertex-color swatches, clearcoat/IOR comparison variants, clearcoat car-paint normal-map texture transforms, multi-extension car concept variants with clearcoat paint, glass transmission, emissive dashboard, and iridescence metadata, emissive coal and copper clearcoat texture maps, animation-pointer pot heat-dome texture-transform source metadata, textured clearcoat wicker normal maps, dispersion comparison pairs, dispersion/IOR prism grids, real dispersion dragon thickness maps, dedicated IOR/specular/transmission grids, emissive strength comparison pairs, iridescence factor texture comparison pairs, iridescence dielectric and metallic IOR/thickness sphere grids, iridescence Suzanne thickness textures with punctual light, real abalone iridescence/thickness maps, iridescent dish transmission/volume/IOR glass with olives/gold-leaf PBR maps, real eyewear transmission/volume/IOR/iridescence lens materials, chess-piece transmission/volume materials, amber transmission/volume/IOR shell with insect maps, multi-material toy car clearcoat/emissive/fabric-sheen/transmission maps, required-transmission refrigerator glass with animated door and emissive interior maps, Unity-exported multi-mesh lantern PBR maps, scalar PBR material factors, dense textured PBR meshes, canonical packed PBR helmet maps, multi-material FlightHelmet PBR and transmission lens maps, separate sci-fi helmet AO/metallic-roughness/normal maps, Unity-exported PBR bottle maps with shared base-color/ORM/normal/emissive textures, metallic texture comparison pairs, normal-map comparison pairs, roughness texture comparison pairs, sheen factor comparison pairs, real transformed sheen cloth texture maps, velvet sofa sheen/specular variants with transformed normal maps and imported directional light, required WebP sheen/specular sofa maps, sheen color/roughness grids, specular factor/color texture pairs, real shot-silk specular/sheen material factors, transformed damask fabric sheen/specular material maps, broken-window transmission glass color-alpha/normal maps, stained-glass lamp variants with transmission/clearcoat texture maps, hurricane candle-holder volume thickness maps, flower-vase alpha mask/transmission volume material pairs, alpha-versus-transmission material pairs, mixed alpha blend/mask/opaque/transmission ordering scenes, texture-driven transmission grids, diffuse-transmission factor/color/texture metadata grids, animated diffuse-transmission plant firefly lights, teacup ORM diffuse-transmission texture reuse, real mandarin diffuse-transmission texture sets, thin-wall versus volume transmission/IOR grids, volume attenuation/thickness comparison pairs, attenuation distance and thickness-factor/texture grids, real attenuation dragon variants with thickness maps, packed metallic/roughness sphere-grid texture samples, scalar-only metalness/roughness sphere grids, `KHR_materials_anisotropy` texture maps over roughness ramps plus explicit anisotropy rotations, direction textures, and scalar strength grids, `KHR_materials_clearcoat` factor/roughness/normal maps, `KHR_materials_iridescence` thickness maps plus transmission/volume/IOR factors, `KHR_materials_transmission` maps with `KHR_materials_volume` thickness/attenuation, USD shader-ball transmission volume thickness maps, volume-scatter skull attenuation/dispersion metadata, roughness-grid samples, and `KHR_materials_ior` factors, `KHR_materials_sheen` factors with transformed fabric textures and variant metadata, dedicated `KHR_materials_variants` shoe, stained-glass lamp, and car concept mappings plus chronograph watch variant mappings and seconds-hand animation, legacy required `KHR_materials_pbrSpecularGlossiness` metadata preservation, `KHR_materials_specular` scalar factors, HDR colors, and specular factor/color textures, meshopt-compression fallback buffers with KHR_mesh_quantization, primitive modes, primitive-mode normals/normalized color attributes, minimal indexed primitives, non-indexed primitives, organic tangent-space packed PBR meshes, normal-map and packed occlusion/roughness/metallic texture loading, explicit mirrored tangent attributes, negative determinant node transforms and front-face rendering, quaternion and matrix node rotations/translations, coordinate-system multi-mesh transform hierarchies, large multi-camera textured city scenes with animated nodes, large textured architectural Sponza scenes, shared mesh reuse across nodes, multiple glTF scenes/default scene selection, primary/secondary glTF UV routing for base-color and emissive textures, EXT_mesh_gpu_instancing, sparse accessors, glTF texture role color-space assignment for base/emissive versus metallic-roughness PNGs, sampler wrap/filter state including power-of-two and non-power-of-two PNG repeat/mipmap sampler state, axis-specific repeat/clamp/mirror modes, and linear min/mag filtering, material sidedness, mapped double-sided PBR planes, single-slot and multi-slot KHR_texture_transform across core, unlit, and clearcoat texture slots, KHR_materials_unlit, KHR_xmp and KHR_xmp_json_ld metadata packets, split external binary attribute/index buffers, and PNG/JPEG/WebP assets, minimal and generated humanoid synthetic committed VRM/VRMA extension fixtures, the VRM Consortium Seed-san practical full-avatar fixture, paired VRM Consortium VRMC_vrm expression conformance fixtures, and Pixiv three-vrm-animation's real VRMC_vrm_animation fixture in CI. Remaining work is additional platform browser references and broader exact-pixel parity work. |

| Physical transmission dispersion corpus | Medium | Renderer-only generated corpus coverage renders a transmitted glass sphere over red/blue scene-color planes, compares normal and dispersed renders, and validates edge color-channel separation. Remaining work is browser-reference golden parity once WebGL-compatible reference coverage is regenerated. |

## P1 - Scene Graph And Object Coverage

| Task | Impact | Notes |
|---|---:|---|
| `InstancedBufferGeometry` edge cases | High | Basic mesh, point, line, and dashed-line CPU expansion supports common per-instance offset/color attributes, mesh normal attributes, plus instanced UV attributes for current built-in texture streams, including Three.js' default `instanceCount = Infinity` and `meshPerAttribute` repeat values; direct conformance covers default instance counts, line-list/line-strip/line-loop expansion without inter-instance strip bridges, selected instanced texture UV channels for mesh, line, and point maps, mesh normal repeat-value indexing, repeat-value indexing across mesh, point, line, and dashed-line draws, invalid explicit instance counts, malformed instanced attributes, or per-attribute repeat values fail clearly, and custom WGSL fragment materials with unsupported arbitrary instanced vertex attributes fail clearly; remaining work is arbitrary custom vertex attributes and shader integration. |
| `THREE.BatchedMesh` parity | High | Common packed-geometry CPU expansion supports active visible instances with per-instance matrix and color textures, including visibility flags, deleted/inactive instance entries, deleted/inactive packed geometry ranges, optimized repacked active geometry ranges, packed geometry groups/material arrays including non-indexed group ranges, partial range/group intersections, group sort metadata when present, common per-object sphere frustum culling with `perObjectFrustumCulled` opt-out, range-local internal sort centers including transparent material-array groups and renderer-level sort callback item depths, `sortObjects=false` instance-order draws, `customSort` callbacks with BatchedMesh `this` binding, render camera, list validation, and no re-entry through Three.js' internal multidraw `onBeforeRender` hook while explicit BatchedMesh render callbacks still dispatch, and malformed packed matrix/color texture containers, malformed instance table entries, malformed/out-of-bounds geometry ranges, cached culling bounds, culling controls, and sort controls failing clearly. Remaining work is exact Three.js per-object frustum culling edge-case parity, broader source-group translation/preservation edge cases beyond optimized range repacking, and a native multidraw or batched drawing path for performance. |
| `renderOrder` and sorting edge cases | High | Group order, `renderOrder`, material id, WebGL material variant, transmissive/transparent buckets, projected object-or-geometry bounding-sphere z, object/insertion ties, `sortObjects`, and custom opaque/transparent sort callbacks are honored, including object/material/geometry/group plus group-order/render-order/z/material-variant render-item metadata for object and opaque/transparent grouped mesh/line/point draws and source-object metadata for BatchedMesh-expanded draws, with invalid `renderOrder`, sort option, and reusable-renderer setter values failing clearly; the generated browser-reference corpus now includes a grouped opaque-sort callback fixture. Remaining work is exact browser-golden parity for broader callback render-item edge cases. |
| Renderer bucket flags | Low | `Renderer.opaque` and `Renderer.transparent` default to true and gate native opaque plus transmissive/transparent render buckets as CommonRenderer compatibility state, with direct conformance for default rendering, opaque-only output skipping both transmissive and ordinary transparent buckets, transparent/transmissive-only output, and background-only output plus invalid flag values. Remaining work is broader browser-golden parity for render-list edge cases. |
| Sprites and billboards | High | `THREE.Sprite`/`SpriteMaterial` CPU billboards support center, scale, rotation, perspective size attenuation controls, opacity, scene fog with `material.fog` opt-out, texture and alpha maps with UV transforms, texture-map sRGB color-space decode, texture-map and alpha-map horizontal/vertical repeat/mirrored wrapping, layers, render ordering, main-pass clipping, directional/spot/point shadow casting with source-material base-map/alpha-map, `alphaHash` opacity, and `alphaToCoverage` opacity cutouts, WebGL-compatible unlit `receiveShadow` no-op behavior, and alpha-tested plus source-`alphaHash`/source-`alphaToCoverage` opacity custom depth/distance shadow material cutouts; invalid billboard center/scale/rotation and size-attenuation values fail clearly. Direct conformance covers main-pass `alphaHash` and `alphaToCoverage` opacity cutouts, directional, spot, and point-light shadow casting, source-material directional base-map, alpha-map, `alphaHash`, and `alphaToCoverage` opacity cutouts, directional/spot custom-depth and point custom-distance shadow cutouts, source/custom base-map and alpha-map texture transforms for directional custom-depth and point custom-distance shadows, custom-depth/custom-distance source `alphaHash` and source `alphaToCoverage` opacity cutouts, plus accepted `receiveShadow`, and the generated browser-reference corpus covers a mapped rotated billboard plus a directional sprite shadow-caster fixture, and renderer-only generated corpus coverage exercises alpha-map and alphaHash/alphaToCoverage cutouts plus custom shadow alpha-map cutouts. Remaining work is broader sprite shadow-caster parity and exact WebGL edge-case parity. |
| Points material parity | Medium | `PointsMaterial` CPU billboards support size, opacity, material arrays with geometry groups, rendered-size-aware frustum culling, perspective size attenuation, depth-independent orthographic sizing, square untextured point-sprite corners, selected per-point geometry UV channels when present, including distinct color-map and alpha-map channels, point-sprite map/alpha-map UVs otherwise, map/alpha-map UV transforms, color-map and alpha-map horizontal/vertical repeat/mirrored wrapping, sRGB color-space decode for maps, vertex colors, scene fog with `material.fog` opt-out, main-pass clipping, common instanced geometry offset/color attributes, selected instanced UV attributes, directional/spot/point shadow casting from expanded billboards with source-material base-map/alpha-map, `alphaHash` opacity, and `alphaToCoverage` opacity cutouts, WebGL-compatible unlit `receiveShadow` no-op behavior, alpha-tested plus source-`alphaHash`/source-`alphaToCoverage` opacity custom depth/distance shadow material cutouts, and point-light custom-distance base-map/alpha-map cutouts with selected geometry UV channels and texture transforms; invalid size and size-attenuation values fail clearly. Direct conformance covers material arrays with geometry groups, perspective attenuation, orthographic size stability, WebGL-style point-size caps, square untextured point-sprite corners, point color/alpha-map UV selection including `alphaMap` channel 3 from `uv3`, main-pass `alphaHash` and `alphaToCoverage` opacity cutouts, accepted `receiveShadow`, directional, spot, and point-light shadow casting, source-material directional base-map, alpha-map, `alphaHash`, and `alphaToCoverage` opacity cutouts, directional/spot custom-depth plus point custom-distance shadow cutouts, point custom-distance base-map/alpha-map selected geometry UV-channel cutouts plus source/custom texture transforms, and custom-depth/custom-distance source `alphaHash` and source `alphaToCoverage` opacity cutouts, and the generated browser-reference corpus covers textured alpha-tested and instanced point billboards, and renderer-only generated corpus coverage exercises point-sprite, alphaHash/alphaToCoverage, selected geometry UV sampling, and custom shadow alpha-map cutouts. Remaining work is exact WebGL point rasterization edge-case parity. |
| Line material parity edge cases | Medium | Current lines support unlit color, opacity, material arrays with geometry groups, including dashed material-array groups with active dash gaps, WebGL-compatible `linecap`/`linejoin` and unlit `receiveShadow` no-ops, scene fog with `material.fog` opt-out, vertex colors, common instanced geometry offset/color attributes, selected instanced UV attributes, alpha maps with channel 0-3 `texture.channel` UV selection and horizontal/vertical repeat/mirrored wrapping, main-pass `alphaHash` and `alphaToCoverage` opacity cutouts, directional/spot/point shadow casting for line objects with line material base-map/alpha-map, `alphaHash`, and `alphaToCoverage` opacity cutouts, and `LineBasicMaterial.map` texture RGB/alpha with sRGB color-space decode, texture UV transforms, horizontal/vertical repeat/mirrored wrapping, channel 0-3 UV selection, and distinct selected UV channels for color and alpha maps; `LineBasicMaterial.linewidth` and `LineDashedMaterial.linewidth` values greater than 1 expand to camera-facing quads. `LineDashedMaterial` dash/gap/scale patterns render for common non-instanced and instanced cases with custom `lineDistance` attributes including descending spans, while `scale=0` and missing `lineDistance` attributes stay solid like WebGL, and reconstructed map/alpha-map UVs, selected instanced map UV attributes, texture UV transforms including explicit matrices, channel 0-3 `texture.channel` UV selection including distinct color-map and alpha-map channels, dashed-map and alpha-map horizontal/vertical repeat/mirrored wrapping, dashed-map sRGB color-space decode, and interpolated vertex colors are honored; invalid linewidth, dash, gap, negative/non-finite scale, and line-distance attribute values fail clearly; direct conformance covers material arrays with geometry groups and active dashed material-array group gaps, Line/LineLoop instanced offset/color and selected map-UV expansion, dashed-line selected instanced map-UV expansion, scale-zero solid behavior, descending custom `lineDistance` spans, LineLoop closing segments including existing `lineDistance` interpolation, WebGL-compatible `linecap`/`linejoin` and unlit `receiveShadow` no-ops, no inter-instance strip bridges, main-pass `alphaHash` and `alphaToCoverage` opacity cutouts, and LineBasicMaterial and LineDashedMaterial base-map/alpha-map plus `alphaHash`/`alphaToCoverage` directional/spot/point shadow cutouts, and the generated browser-reference corpus covers simple dashed-line patterns plus textured alpha-tested dashed-line output, and renderer-only generated corpus coverage exercises selected dashed-line UV-channel texture routing and wide-line expansion. Remaining work is exact native/browser wide-line parity and broader line material option parity. |
| Scene background textures | Medium | Scene color backgrounds preserve RGBA clear alpha and reusable `Renderer.setClearColor()`/`setClearAlpha()` state supplies the fallback clear color when neither scene nor options provide a color, including hex, CSS string, and color-like renderer clear-color inputs plus CSS string scene/option background colors, while 2D, equirectangular, raw, encoded, CubeUV-mapped readable six-face cube, and packed 2D PMREM/CubeUV sharp-atlas texture background mappings are supported with output color-space conversion and approximate texture blurriness; RGBA color clears, renderer clear-color fallback/precedence, CSS background colors, option-level texture background transforms, intensity/blurriness control isolation, packed CubeUV sharp-face decoding, and malformed packed CubeUV layouts have direct conformance, and option-level color background overrides, 2D background texture transforms, and equirectangular background rotation/intensity are covered by the generated browser-reference corpus, with renderer-only generated corpus coverage for raw, CubeUV cube, and packed CubeUV background rotation. Remaining work is exact packed PMREM prefiltered LOD semantics, exact blur parity, newer mapping controls, and exact color-space parity. |
| Clipping planes | Medium | Render-option global planes, reusable `Renderer.clippingPlanes` global fallback planes, scene-level `THREE.ClippingGroup`, and material-local `clippingPlanes`/`clipIntersection` affect the main pass for meshes, sprites, points, and lines, up to eight active planes; malformed plane containers, invalid plane values, invalid clipping control values, invalid `options.localClippingEnabled`/`Renderer.localClippingEnabled`, and over-budget global/group/material plane combinations fail clearly; nested parent/child `ClippingGroup` union/intersection composition has direct conformance and renderer-only generated corpus coverage; the browser-reference generator applies fixture clipping controls to WebGLRenderer-compatible clipping fixtures; alpha-to-coverage materials smooth MSAA main-pass clipping edges, including custom WGSL fragment materials; `material.clipShadows` and group `clipShadows` clip mesh and line shadow-caster fragments in the shadow pass, including direct LineBasicMaterial and LineDashedMaterial caster coverage across directional, spot, and point lights, and source-material clipping state is carried onto custom shadow casters; and `options.localClippingEnabled: false` plus `Renderer.localClippingEnabled = false` disable material-local planes while preserving global/group planes, with per-render options overriding renderer state. Remaining work is deeper browser-golden clipping parity. |

## P1 - Materials And Textures

| Task | Impact | Notes |
|---|---:|---|
| Texture transform parity edge cases | Very high | Base color, supported background, sprite/point color and alpha, line/dashed-line, normal, bump, displacement, metallic/roughness, emissive, AO, light, specular, alpha, and physical-extension map transforms are supported, with malformed transform vector containers and invalid transform values failing clearly, including direct conformance for background `flipY` plus explicit base/background, sprite/point color and alpha, line/dashed-line, matcap, normal/bump, displacement, emissive, metallic/roughness, AO/light, Phong specular, alpha, and current physical-extension texture matrices. Direct conformance also covers explicit-matrix plus color-space interactions for current color-producing transform slots, and the generated browser-reference corpus covers explicit base-map matrix sampling with sRGB decode. Remaining work is exact browser golden-tested parity for broader texture slots. |
| Texture color-space parity | Very high | Base color, sprite, point, line/dashed-line, matcap, normal/bump, displacement, metallic/roughness, emissive, AO, light, Phong specular, alpha, toon gradient, physical scalar RGB maps, sheen color, physical specular color, supported background texture maps, and equirectangular/cube scene-environment, reflection-probe, and supported material-level LDR IBL inputs honor explicit `THREE.SRGBColorSpace`/`THREE.LinearSRGBColorSpace` plus documented linear string aliases; unsupported texture color-space/encoding values fail clearly; material and texture background output conversion supports render-option and reusable `Renderer.outputColorSpace` plus `currentColorSpace` and `_outputColorSpace` `THREE.SRGBColorSpace`/`THREE.LinearSRGBColorSpace` plus documented linear string aliases, with unsupported output color-space values failing clearly and per-render options overriding renderer state; material shader output honors `premultipliedAlpha`, raw material/background/current IBL texture uploads honor `texture.premultiplyAlpha`, `toneMapped=false`, reusable `Renderer.toneMapping`/`currentToneMapping` No/Linear/Reinhard/Cineon/ACES/default identity Custom/AgX/Neutral state, `Renderer.toneMappingExposure`, and `Renderer.needsFrameBufferTarget=false`; explicit texture matrices compose with color-space decode for current color-producing transform slots; and base color maps honor `flipY`. The browser-reference generator normalizes the same output color-space aliases, and the generated corpus plus direct renderer-state conformance cover explicit base-map matrix sampling with sRGB decode plus linear output color-space conversion. Remaining work is exact color-space handling for remaining texture slots and broader WebGLRenderer state parity. |
| Additional UV channel coverage | High | Base color maps, normal/bump maps, matcap color maps, displacement maps, metallic/roughness maps, `emissiveMap`, `aoMap`, `lightMap`, `alphaMap`, Phong `specularMap`, mesh material draws with up to two distinct `texture.channel` UV streams across current supported slots, glTF base-color/emissive texture pairs, point color/alpha maps with geometry UVs including point `alphaMap` `uv3` coverage, line/dashed-line color and alpha maps, selected instanced UV attributes for current built-in mesh/point/line texture streams including default instance counts plus line-list, line-strip, and line-loop map draws, and current physical-extension maps honor `texture.channel` values 0-3 by routing selected UV attributes through the available native UV streams, with direct base-color, normal/bump, matcap, displacement, roughness, emissive, AO, light-map, alpha-map, Phong specular, and current physical clearcoat, sheen, anisotropy, specular, transmission/thickness, and iridescence factor/thickness `uv3` coverage for channel 3 plus shared two-channel mesh material slot coverage; malformed channel values, unsupported channel indices, and mesh material draws requiring more than two distinct texture channels fail clearly. Remaining work is simultaneous arbitrary UV attribute bindings beyond two native streams and future slots. |
| More texture slots | High | Scalar iridescence plus iridescence factor/thickness maps are supported, with direct conformance for explicit matrices, `uv1`-`uv3` channel routing, horizontal/vertical repeat/mirrored wrapping, and nearest/linear sampler filtering; displacement-map conformance covers texture transforms, explicit matrices, horizontal/vertical repeat/mirrored wrapping, nearest/linear sampler filtering, secondary UV selection, and bias, with renderer-only generated corpus coverage for depth-output displacement. Remaining work is deepening exact iridescence-map and displacement edge-case parity. |
| More material classes | High | `THREE.Scene.overrideMaterial` replaces object material arrays for normal color renders and owns material-envMap discovery, with invalid override values failing clearly. Scene-level environment/reflection probes are the main supported IBL path, including direct conformance for `MeshPhongMaterial` specular environment reflection and `specularMap` masking. A single shared material-level reflection `envMap` is supported for `MeshBasicMaterial`, `MeshStandardMaterial`, `MeshPhysicalMaterial`, `MeshPhongMaterial`, and `MeshLambertMaterial` through the native IBL path with per-material intensity and shared rotation, while `envMap` properties on non-env-map material classes are accepted as no-ops with direct conformance across Depth, Distance, Matcap, Normal, and Toon mesh materials; Basic/Phong/Lambert legacy multiply/mix/add combine modes, `reflectivity`, refraction mappings with `refractionRatio`, and env-map rotation are honored, with direct Phong/Lambert env-map rotation and legacy refraction coverage; common main-pass mesh wireframes render as triangle edges; the generated browser-reference corpus covers MeshDepthMaterial BasicDepthPacking and wireframe output, MeshNormalMaterial flat output, MeshMatcapMaterial map output, MeshToonMaterial gradient-map and alpha-map cutout output, ShadowMaterial receiver shadows and fog opt-out, Phong material-level envMap, and shared Basic/Lambert material-level envMap fixtures; renderer-only generated corpus covers shared Standard/Physical material-level envMap fixtures, MeshDepthMaterial non-Basic depth-packing output, MeshNormalMaterial tangent/object-space normal-map and bump-map output, MeshMatcapMaterial flat-shading, tangent/object-space normal-map, and bump-map lookup output, MeshToonMaterial fallback bands and normal/bump-map lighting perturbation, and MeshDistanceMaterial range, displacement-map, and wireframe output, plus sprite/point billboard custom shadow alpha-map cutouts; and unsupported material classes, invalid material-envMap scalar/rotation values, PBR refraction mappings, unsupported env-map combine values, multiple distinct material env maps, and multiple distinct material-env-map rotations fail clearly. `Object3D.customDepthMaterial` and `customDistanceMaterial` are honored for mesh shadow caster base-map/alpha-map alpha-tested inputs, including selected base-map and alpha-map `uv1`-`uv3` channels, base-map and alpha-map texture transforms, displacement, visibility flags, and wireframe material inputs, and source-material base-map/alpha-map cutouts including selected base-map and alpha-map `uv1`-`uv3` channels plus base-map and alpha-map texture transforms, `alphaHash` and `alphaToCoverage` opacity cutouts, `shadowSide`, clipping state, displacement maps, and wireframe state are carried onto custom shadow casters, plus alpha-tested sprite/point billboard shadow cutouts and point custom-distance base-map/alpha-map selected geometry UV-channel cutouts; malformed custom shadow material containers and visibility values fail clearly. `MeshDepthMaterial` has direct conformance for depth-packing variants, invalid packing values failing clearly, wireframe output, displacement-map edge cases, and base/alpha-map cutouts including selected custom-shadow and source-material base-map/alpha-map `uv1`-`uv3` channels plus custom/source shadow base-map and alpha-map texture transforms. `MeshDistanceMaterial` has direct conformance for distance range/reference controls, invalid range/reference values and malformed material userData/renderer hint containers failing clearly, wireframe output, base/alpha-map cutouts including selected custom-shadow and source-material base-map/alpha-map `uv1`-`uv3` channels plus custom/source shadow base-map and alpha-map texture transforms, point-billboard base-map/alpha-map selected geometry UV-channel cutouts, and displacement-map `uv1`-`uv3` channel selection. `MeshToonMaterial` has direct conformance for fallback bands, gradient maps, normal/bump-map lighting perturbation, base-map `uv1`-`uv3` channels, emissive-map `uv1`-`uv3` channels, light-map `uv1`-`uv3` channels, and alpha-map cutouts. `ShadowMaterial` covers the current transparent receiver path with direct conformance for color tint, opacity, scene fog, fog opt-out, and output conversion. Remaining work is deepening `ShadowMaterial` exact parity, `PointsMaterial` rasterization edge parity, `SpriteMaterial` shadow-caster parity, broader custom shadow material translation, `MeshToonMaterial` browser-golden edge-case parity, exact PBR material-level envMap/refraction parity, plus `LineDashedMaterial` edge cases. |
| Physical material parity gaps | High | Scalar iridescence and iridescence factor/thickness maps are approximated, scene-color transmission uses a roughness/IOR-aware filter with approximate dispersion, malformed physical color containers and invalid physical color/scalar/vector/range values fail clearly, and direct conformance covers scalar clearcoat/roughness IBL response plus transmission volume attenuation color/distance behavior; committed glTF coverage verifies transmission maps, volume thickness/attenuation, and IOR factors. Remaining work is adding/spec-checking unsupported features, exact iridescence parity, exact attenuation parity against browser references, exact transmission mip/bicubic parity, and additional clearcoat/normal/specular BRDF edge cases against Three.js. |
| Blending modes and render state | High | `NoBlending`, `NormalBlending`, `AdditiveBlending`, `SubtractiveBlending`, `MultiplyBlending`, `CustomBlending` equations/factors including min/max equations, separate alpha equation/factors, and constant color/alpha factors, `depthTest`, `depthFunc`, `depthWrite`, `colorWrite`, `polygonOffset`, `alphaHash`, `alphaToCoverage` on 4x MSAA renders including output-alpha and alpha-test threshold coverage, `premultipliedAlpha` including custom WGSL fragment output, `toneMapped=false` output opt-out, reusable `Renderer.toneMapping`/`currentToneMapping` with No/Linear/Reinhard/Cineon/ACES/default identity Custom/AgX/Neutral modes, `toneMappingExposure`, and `needsFrameBufferTarget=false`, transparent default depth writes, `forceSinglePass` accepted as a native single-pass no-op, boolean-validated `dithering`, supported `precision` strings, and mesh wireframe line hints accepted as native no-ops, and stencil state are honored in the main pass; unsupported blending, custom blend equation/factor, depth function, stencil function, stencil operation constants, tone-mapping constants, unsupported precision strings, unsupported wireframe line hint strings, and invalid render-state boolean/numeric values fail clearly. Direct conformance covers all supported depth comparison constants, all supported stencil comparison functions, all supported stencil z-pass operations, stencil write/func masks, custom blending, stencil masking, material and renderer tone-mapping opt-outs, renderer tone-mapping exposure and Linear/Reinhard/Cineon/default identity Custom/AgX/Neutral modes, accepted `forceSinglePass`, accepted `dithering`, `precision`, and mesh wireframe line hints, custom-WGSL premultiplied output, and the current 4x-MSAA alpha-to-coverage path, and the generated browser-reference corpus covers those paths. Remaining work is exact alpha-to-coverage/sample-mask parity across WebGLRenderer states. |
| Shader material strategy | High | `ShaderMaterial`, `RawShaderMaterial`, NodeMaterial, and built-in material `onBeforeCompile` customizations fail clearly unless a headless WGSL fragment override is provided through documented renderer hints or top-level custom fragment aliases, except for the narrow built-in Three.js fullscreen CopyShader and OutputShader pass adapters used by covered EffectComposer helpers; direct conformance covers both opt-in custom-fragment surfaces and the helper exceptions; malformed override values plus malformed material userData/renderer hint containers fail clearly. Current custom WGSL fragments are not Three.js shader parity. Remaining work is a fuller strategy for Three.js shader graph/material translation and `onBeforeCompile` parity. |
| Compressed texture path | Medium | KTX2/Basis/`THREE.CompressedTexture` inputs and compressed texture format constants fail clearly with pre-decode guidance across material, material-envMap, background, scene-environment, and reflection-probe texture slots; direct conformance covers material-map and material-envMap compressed textures, compressed-format texture objects, explicit mipmaps, DataArrayTexture/Data3DTexture failures across material/background/environment/probe/envMap slots, opaque browser-like image objects without readable or drawable pixel APIs, canvas-like image object uploads, OffscreenCanvas-backed image-like uploads, readable `texture.source.data` payloads, malformed source containers, mismatched raw payload lengths, and packed-depth raw texture type constants across material/background/environment/probe/envMap slots. Array/3D texture inputs, cube/PMREM material texture-slot inputs across mesh, sprite, point, line, and physical-material extension slots, opaque browser image objects without readable or drawable pixel APIs, and raw DataTexture inputs with mismatched lengths fail clearly across those slots; raw material/background/current IBL half-float DataTextures decode binary16 texels, signed and unsigned integer DataTextures normalize texels for material/background and current environment/reflection-probe/material-envMap paths, and packed `UnsignedShort4444Type`/`UnsignedShort5551Type`/`UnsignedInt5999Type`/`UnsignedInt101111Type` DataTextures unpack color channels for material/background and current IBL paths before RGBA8 upload. Remaining work is native compressed texture decode support and broader texture format support. |
| Mip/filter/aniso settings | Medium | Base/background, normal/bump, displacement, metallic/roughness, emissive, AO/light, alpha, Phong specular, toon gradient, matcap color-map, and packed physical-extension texture groups respect wrap modes plus `NearestFilter`/`LinearFilter`-family `magFilter`/`minFilter`, with direct coverage for glTF non-power-of-two repeat/mipmap sampler state, linear min/mag filtering, background texture repeat/mirrored wrapping, base color-map repeat/mirrored wrapping, normal-map repeat/mirrored wrapping, bump-map repeat/mirrored wrapping, displacement-map repeat/mirrored wrapping, metalness-map repeat/mirrored wrapping, roughness-map repeat/mirrored wrapping, matcap color-map repeat/mirrored wrapping, Phong specular-map repeat/mirrored wrapping, AO-map repeat/mirrored wrapping, emissive-map repeat/mirrored wrapping, light-map repeat/mirrored wrapping, alpha-map repeat/mirrored wrapping, sprite color-map repeat/mirrored wrapping, sprite alpha-map repeat/mirrored wrapping, point color-map repeat/mirrored wrapping, point alpha-map repeat/mirrored wrapping, line color-map repeat/mirrored wrapping, line alpha-map repeat/mirrored wrapping, dashed-line color-map repeat/mirrored wrapping, dashed-line alpha-map repeat/mirrored wrapping, toon gradient-map horizontal repeat/mirrored wrapping, current physical-extension map nearest/linear filtering, and iridescence factor/thickness map repeat/mirrored wrapping; unsupported sampler constants, invalid anisotropy values, malformed texture source containers, invalid `unpackAlignment` values, and malformed explicit mipmap containers fail clearly; mipmap min filters generate native mip chains from the source level for supported material/background texture uploads; raw `DataTexture`-style explicit mipmap chains, including half-float levels, upload for unpacked 2D material/background texture slots; material/background/current IBL texture slots accept WebGL-compatible `unpackAlignment` values for tightly packed readable uploads, and material/background sampler slots accept non-default texture anisotropy and use native sampler anisotropy when the effective sampler is linear-filtered, while explicit mipmap arrays still fail clearly for packed physical-extension maps and scene-environment/reflection-probe/material-envMap texture slots. Conflicting sampler settings, including anisotropy, between maps packed into one native texture fail clearly. Remaining work is deeper browser LOD parity. |

## P1 - Lighting, Shadows, And IBL

| Task | Impact | Notes |
|---|---:|---|
| Multiple shadow-casting lights | Very high | Multiple visible shadow-casting lights are supported when their packed native depth-array usage stays within twelve layers: directional and spot lights use one layer, point lights use six layers, and cascaded directional shadows use one layer per cascade. Over-budget shadow sets fail clearly, and renderer-only generated corpus coverage exercises multiple directional shadow maps. Remaining work is true shadow atlas allocation beyond the fixed layer budget and deeper browser-golden parity for mixed shadow-light scenes. |
| Shadow behavior parity | High | Common `shadow.camera`, rectangular directional/spot/cascaded map sizes, up to four directional cascades, square point-light cube-face maps, reusable `Renderer.shadowMap.enabled` opt-out gating, `Renderer.shadowMap.type` Basic single-compare sampling plus PCF-family 3×3 sampling, bias, normal bias, `shadow.radius` PCF scaling, PCF-irrelevant `shadow.blurSamples` inputs, cast/receive rules, explicit mesh `material.shadowSide` face filtering, texture base-map and alpha-map alpha-tested casters, `alphaToCoverage` shadow-caster cutoff approximation, mesh/line/billboard `alphaHash` shadow-caster opacity cutouts, directional/spot/point line-object shadow casters with line material base-map/alpha-map cutouts, directional/spot/point sprite and point-billboard shadow casters with source-material base-map/alpha-map cutouts, mesh custom depth/distance shadow material selected base-map and alpha-map UV-channel cutouts plus base-map and alpha-map texture transforms, displacement, visibility flags, and wireframe line-list shadows, source-material selected base-map and alpha-map UV-channel cutouts plus base-map and alpha-map texture transforms, `alphaHash` opacity cutouts, `shadowSide`, clipping, displacement, and wireframe with custom shadow materials, directly covered sprite/point billboard custom shadow material alpha cutouts, sprite custom-depth/custom-distance source/custom base-map and alpha-map texture transforms, and point-light point-billboard custom-distance source/custom base-map and alpha-map texture transforms plus selected geometry UV channels are supported; malformed shadow flags/containers, invalid renderer shadow-map booleans/type constants, invalid shadow numeric/range values including map-size vector/dimension aliases, non-square point-light shadow map sizes, custom shadow material visibility values, and malformed light userData/cascade hint containers/values or over-budget cascade hints fail clearly. Direct conformance now covers renderer shadowMap gating, type validation, Basic single-compare versus PCF/PCFSoft/VSM filtered type sampling, mesh base-map and alpha-map shadow cutouts, alphaToCoverage shadow-caster cutoff approximation including line and billboard casters, mesh/line/billboard `alphaHash` shadow opacity cutouts, mesh custom shadow selected base-map and alpha-map UV-channel cutouts plus base-map and alpha-map texture transforms, custom shadow material visibility, source-material selected base-map and alpha-map UV-channel cutouts plus base-map and alpha-map texture transforms, `alphaHash`, `alphaToCoverage` opacity cutouts, `shadowSide`, clipping, displacement, and wireframe with mesh custom shadow materials, Line/LineSegments/LineLoop directional/spot/point shadows, LineBasicMaterial and LineDashedMaterial base-map/alpha-map plus `alphaHash`/`alphaToCoverage` directional/spot/point shadow cutouts, billboard source-material base-map, alpha-map, `alphaHash`, and `alphaToCoverage` custom-shadow cutouts, sprite custom/source base-map and alpha-map texture transforms for custom-depth/custom-distance shadows, point-light point-billboard custom-distance source/custom base-map and alpha-map texture transforms plus selected geometry UV-channel maps, and mesh custom shadow wireframes. Renderer-only generated corpus coverage exercises multiple directional shadow maps and sprite/point billboard custom shadow alpha-map cutouts. Remaining work is VSM-specific blur behavior, broader custom shadow material translation, transparency parity, and deeper golden-tested behavior. |
| Renderer shadow-map compatibility state | Low | `Renderer.shadowMap.transmitted` is stored as validated boolean compatibility state alongside `autoUpdate`, `needsUpdate`, and `type`. It is currently state-only because transmitted shadow-map passes are not modeled separately. |
| `RectAreaLight` | Medium | One-sided finite-area approximation is supported for direct lighting, with direct conformance for front/back orientation and size-based radiance scaling, and the browser-reference generator initializes Three.js' RectAreaLight uniforms for a generated finite-area light fixture. Remaining work is exact Three.js LTC/LUT parity. |
| `LightProbe` support | Medium | Three.js `LightProbe` spherical harmonics contribute diffuse indirect lighting and honor visibility plus camera layer filtering, with invalid coefficient values failing clearly, direct conformance for visibility filtering, camera layer filtering, Standard, Physical, Lambert, Phong, and Toon diffuse paths, scene-environment combinations across those lit material models, and `LightProbeGenerator.fromCubeRenderTarget()` cube-target readback through the WebGLRenderer marker path, plus generated browser-reference corpus coverage for Lambert plus mixed Standard/Physical/Phong/Toon diffuse lighting. Remaining work is exact golden-tested parity with the full material/environment matrix. |
| Environment/background rotation controls | Medium | `scene.environmentIntensity`, `options.environmentIntensity`, reflection-probe intensity, `scene.backgroundIntensity`, `options.backgroundIntensity`, `options.backgroundBlurriness`, approximate texture `backgroundBlurriness`, equirectangular/cube `scene.backgroundRotation`/`options.backgroundRotation`, and equirectangular/cube `scene.environmentRotation`/`options.environmentRotation` are supported for current background/environment texture mappings; explicit option rotations are validated independently of active maps, and invalid background color/control/rotation values, invalid environment control/rotation values, and unsupported background rotations fail clearly. Direct conformance covers option texture background intensity/blurriness control isolation plus cube background/environment option rotation overrides, with generated renderer-only corpus fixtures for the same option paths. Remaining work is exact blur parity and exact background/environment mapping controls. |
| PMREM compatibility | Medium | Cube reflection environment inputs, including CubeUV-mapped six-face cube and packed 2D PMREM/CubeUV sharp/base-atlas scene-environment, reflection-probe, and material-envMap inputs, are converted through the renderer's CPU IBL precompute path with direct conformance. Direct conformance still covers clear failures for PBR material refraction mappings and malformed packed 2D PMREM/CubeUV scene-environment, reflection-probe, and material-envMap layouts. Remaining work is native Three.js PMREM generation and exact prefiltered LOD semantics beyond the decoded sharp/base atlas faces. |

## P2 - Cameras, Targets, And Render Pipeline Features

| Task | Impact | Notes |
|---|---:|---|
| Array/cube camera support | Medium | `THREE.ArrayCamera` sub-cameras compose into PNG, RGBA, and target output using each sub-camera viewport, including target depth readback, composed object-id target metadata, and reusable `Renderer.setRenderTarget()` state for composed color/depth targets; `THREE.CubeCamera` renders six RGBA cube target faces plus optional depth/object-id faces into `WebGLCubeRenderTarget.texture.image`/`source.data`, nonzero `activeMipmapLevel` writes the captured faces into the corresponding cube texture mip entry, active-mip target scissor clipping and scissored active cube-face/mip clear preservation have direct conformance, object-id cube renders expose reverse lookup metadata, `Renderer.readRenderTargetPixels()` can read explicit cube faces, captured color textures can be reused as cube background/environment inputs, and `CubeCamera.update(renderer, scene)` drives the renderer-compatible active cube-face/mip target state flow while preserving inert `Renderer.xr` enabled/cameraAutoUpdate/framebuffer-scale/controller/reference-space/session/base-layer/binding/frame/environment/depth-texture/depth/foveation/camera-texture/event-target dispatch probes, with real XR session binding failing clearly, malformed nested camera containers failing clearly, and renderer-only generated corpus coverage for ArrayCamera viewport composition and CubeCamera face/update capture. Remaining work is native PMREM generation and exact WebGL cube viewport/face semantics. |
| Render target depth output | Medium | Single-output non-array targets with `depthTexture` populate RGBA8 normalized depth readback for plain depth targets, normalized scalar `Float32Array` data for `FloatType`, `Uint16Array` half-float data for `HalfFloatType`, scalar unsigned typed arrays for `UnsignedByteType`/`UnsignedShortType`/`UnsignedIntType` depth textures, and `UnsignedInt248Type` data with normalized depth24 in the high bits plus a zero stencil byte, using depth-tested visible geometry, updating depth texture image/source containers, and marking written textures `needsUpdate`; packed `UnsignedInt248Type` clear paths update stencil bytes while preserving depth bits, including active cube faces/mips, and depth-only clears preserve existing stencil bytes; malformed targets, target image containers, nested target texture/mipmap/source containers, unsupported explicit depth texture types, and unsupported format/type pairings fail clearly; `Renderer.readRenderTargetPixels()` and `readRenderTargetPixelsAsync()` read stored CPU color data from regular targets, explicit cube faces, and color attachment texture indices into caller-provided buffers, while async readback can allocate a matching buffer when omitted and accepts the common-renderer async `(target, x, y, width, height, textureIndex, faceIndex)` argument shape; direct conformance covers near/far ordering through `renderToTarget()`, `options.target`, and reusable `Renderer.setRenderTarget()` state for `FloatType`, `HalfFloatType`, and packed `UnsignedInt248Type` depth readback, `UnsignedShortType` scalar readback, texture writeback, scissor clipping, transparent default/explicit depth writes, base-texture and alpha-map alpha-test cutouts, `alphaHash` depth cutouts, readback of regular target data, explicit cube faces, and typed MRT color attachments, and unsupported type/format validation through both target APIs. Remaining work is native hardware depth-texture/stencil readback and exact WebGL render-target depth semantics. |
| Multiple render targets | Medium | Single-output target texture objects, `target.texture` arrays, `target.textures`, and MRT-shaped targets write back RGBA8 data by default, Alpha/Red/RG/RGB/RGBA and RedIntegerFormat/RGIntegerFormat/RGBIntegerFormat/RGBAIntegerFormat target texture formats, normalized `Float32Array` data for `FloatType`, normalized signed/unsigned integer arrays for `ByteType`/`ShortType`/`IntType` and `UnsignedShortType`/`UnsignedIntType`, packed `Uint16Array` data for `UnsignedShort4444Type`/`UnsignedShort5551Type`, packed `Uint32Array` RGB9_E5 data for `UnsignedInt5999Type`, packed `Uint32Array` R11F_G11F_B10F data for `UnsignedInt101111Type`, or `Uint16Array` half-float data for `HalfFloatType` color textures while top-level `target.data` remains RGBA8, including texture image/source containers and `needsUpdate` marking; direct conformance covers typed readback through `target.texture`, one-element `target.texture` arrays, `target.textures[0]`, single-attachment MRT-shaped targets, `options.target`, reusable `Renderer.setRenderTarget()` state for regular targets plus typed single-attachment target arrays, and `Renderer.readRenderTargetPixels()` attachment-index and cube-face reads, as well as explicit secondary `"color"`, `"mask"`, `"object-id"`, `"normal"`, and `"depth"` MRT attachments across regular-camera, ArrayCamera, and CubeCamera paths, including RG/FloatType secondary color readback. Regular-camera, ArrayCamera, and CubeCamera MRT-shaped targets can populate secondary color textures with explicit multi-pass `"color"`, `"mask"`, `"object-id"`, `"normal"`, and `"depth"` attachments declared by `texture.userData.headlessThreeRenderer.renderMode`; `Renderer.getMRT()` returns `null` and `Renderer.setMRT(null)` is accepted as a helper clear operation; unhinted secondary color attachments, array/3D target texture objects, compressed target texture objects or compressed target format constants, regular-camera cube target texture objects, unsupported color target texture formats/types, and arbitrary native MRT shader outputs including non-null `Renderer.setMRT()` fail clearly, including representative `options.target` failure coverage. Remaining work is native MRT support for deferred-style workflows. |
| MSAA controls | Medium | Render-option and target sample counts support 4x MSAA with resolve back to normal output and target readback buffers, with direct conformance for render-option `samples` plus target `samples` and `sampleCount` aliases through `renderToTarget()`, `options.target`, and reusable `Renderer.setRenderTarget()` state; `Renderer.samples` reports `0` because constructor-level default MSAA is unsupported, while `Renderer.currentSamples` reflects the active target sample count and `Renderer.isOutputTarget` reports default-output versus active-target state; constructor-level `samples > 1` fails clearly with per-render/target MSAA guidance; material `alphaToCoverage` uses native MSAA alpha-to-coverage for 4x renders; unsupported sample counts fail clearly; the generated browser-reference corpus covers the current 4x-MSAA alpha-to-coverage path. Remaining work is broader WebGL sample-count/state parity. |
| Viewport/scissor support | Medium | Render-option viewport/scissor rectangles, `target.viewport`/`target.scissor` with `target.scissorTest`, and reusable `Renderer.setSize()`/`getSize()` plus `setDrawingBufferSize()`/`getDrawingBufferSize()` and `setPixelRatio()`/`getPixelRatio()` compatibility state and `setViewport()`/`setScissor()`/`setScissorTest()` state are supported in output pixel coordinates, with direct conformance for render-option, `renderToTarget()`, `options.target`, reusable `Renderer.setRenderTarget()` state for target-owned dimensions and viewport/scissor fields including disabled target scissor tests, renderer-state size and drawing-buffer-size fallback, renderer-state pixel-ratio no-scaling output-pixel behavior, renderer-state Vector2/Vector4-style getter targets, default-only common-renderer viewport depth-range validation, `Renderer.getDrawingBufferSize()`/`getViewport()`/`getCurrentViewport()`/`getScissor()` clone plus target-object population, sized/scissored active-render-target `Renderer.clear()`/`clearColor()`/`clearDepth()` color/depth writeback including cube faces/mips plus packed depth-stencil stencil-byte writeback for active cube faces/mips, sized `clearTarget()` writeback, source-rectangle `copyFramebufferToTexture()` base/mip and active cube-face/mip CPU copies, raw/canvas-like/OffscreenCanvas-backed source `copyTextureToTexture()` base/mip CPU copies into raw destinations, and inert `Renderer.domElement` output/client/offset-size/bounding-rect/style mirroring plus style-property, attribute, event-target, canvas-export, and `ViewHelper.render()` offset-size/viewport-restore probes; inert `Renderer.info` counters with WebGLInfo- and CommonRenderer-style `update()`/`reset()`/`dispose()`, inert `Renderer.debug` shader-diagnostic state, inert `Renderer.inspector` lifecycle and copy-hook state, conservative `Renderer.capabilities` WebGL limit, draw-buffer, texture probes, and renderer-level `getMaxAnisotropy()`, and conservative `hasFeature()`/`hasFeatureAsync()`/`hasCompatibility()` plus `isOccluded()` probes, inert `Renderer.extensions`, scratch `Renderer.properties`/`renderLists` helpers, inert `Renderer.xr` enabled/cameraAutoUpdate/framebuffer-scale/controller/reference-space/session/base-layer/binding/frame/environment/depth-texture/depth/foveation/camera-texture/event-target dispatch probes, read-only renderer metadata and readiness probes including `alpha`/`depth`/`stencil`/`logarithmicDepthBuffer` plus `isRenderer`/`initialized`, `highPrecision=false` metadata with `true` failing clearly, reusable clear depth/stencil value state plus default-framebuffer/unsized-target `Renderer.clear()`/`clearAsync()` behavior and `clearTarget()`/`clearColor()`/`clearColorAsync()`/`clearDepth()`/`clearDepthAsync()`/`clearStencil()`/`clearStencilAsync()`/`init()`/`initRenderTarget()`/`initTexture()`/`initTextureAsync()`/`hasInitialized()`/`resetState()`/`resetGLState()`/`dispose()`/`setAnimationLoop()`/`getAnimationLoop()` hooks, and `autoClear`/`autoClearColor`/`autoClearDepth`/`autoClearStencil` flags are accepted as compatibility state because each native render pass owns its buffers and native resources follow object lifetime; explicit options override target and renderer state, while target dimensions override renderer size fallback; invalid sizes, size updateStyle flags, pixel ratios, rectangles, non-default viewport depth ranges, info booleans/update values, debug values, inspector values, capability/extension/property/render-list probes, DOM style/attribute/event/canvas probes, XR booleans/values/events/controller/reference-space/session binding, clear values/booleans, resource-init targets/textures, external WebGL texture/framebuffer handles, copy targets, unsupported texture-copy inputs, missing active framebuffer copy targets, animation-loop callbacks, auto-clear flags, and malformed scissor flags fail clearly; external framebuffer/texture handle APIs and no-active-target framebuffer copies fail clearly as unsupported; the browser-reference generator applies fixture viewport/scissor options and the generated corpus covers a clipped render-option rectangle. Remaining work is exact WebGLRenderer state-machine parity. |
| `Renderer.setEffects()` compatibility hook | Low | `Renderer.setEffects()` accepts no argument, `null`, or an array as a validated no-op for WebGLRenderer compatibility while real post-processing remains scoped to `options.postProcessing`; malformed effect containers fail clearly. No remaining work under the current scene-oriented contract; exact WebGLRenderer post-processing pipeline parity remains outside scope. |
| `Renderer.transmissionResolutionScale` compatibility state | Low | `Renderer.transmissionResolutionScale` defaults to `1`, stores positive finite values for WebGLRenderer compatibility, scales the scene-color texture sampled by physical transmission, and has direct conformance for low-resolution softening plus invalid values failing clearly. Remaining work is exact WebGLRenderer edge-case parity for downscaled transmission. |
| `Renderer.isWebGLRenderer` marker | Low | `Renderer.isWebGLRenderer` is `true` for Three.js helper branches that choose WebGLRenderer-compatible readback signatures, with direct coverage for `LightProbeGenerator.fromCubeRenderTarget()` reading renderer-owned cube targets, simple `EffectComposer`/`RenderPass` target-state readback, `ClearPass` active-target clear-state flows, standard CopyShader `ShaderPass`/`TexturePass`/`SavePass` fullscreen flows, `OutputPass` fullscreen output-copy flow, CopyShader additive accumulation through `SSAARenderPass` and low-opacity `TAARenderPass` samples, WebGLCubeRenderTarget and CommonRenderer `CubeRenderTarget` clear-state flows, `StereoEffect` scissored eye-camera renders, WebGLRenderer-style object `onBeforeRender`/`onAfterRender` callback dispatch including `Reflector`/`Refractor` helper-owned target prepasses, and `EXRExporter`/`KTX2Exporter` exporting renderer-owned targets; `ClearMaskPass` can touch the inert stencil state, while `MaskPass`, `Renderer.getContext()`, and other direct WebGL paths still fail clearly. Remaining work is broader helper interoperability audits. |
| `Renderer.info` compatibility counters | Low | Inert WebGLInfo and CommonRenderer-style diagnostics expose memory, render, and compute counter fields including timestamp counters, validated `autoReset`, WebGL draw-mode `update(count, mode, instanceCount)`, CommonRenderer object-style `update(object, count, instanceCount)`, `updateTimestamp(type, time)`, `reset()`, and `dispose()` without mutating normal render output. Remaining work is deeper diagnostics parity only if future helpers require backend-owned timing data. |
| `Renderer.state` buffer probes | Low | Inert `Renderer.state.buffers.color`, `Renderer.state.buffers.depth`, and `Renderer.state.buffers.stencil` setter/lock/reset probes plus state-level blending/material/flip-sided/cull-face/line-width/polygon-offset/scissor-test/scissor/viewport probes and fallback-style color/depth/stencil aliases cover common WebGLRenderer-adjacent helper calls, numeric `Renderer.state.scissor()`/`viewport()` rectangles are accepted, `Renderer.state.buffers.depth.getReversed()` reports `false`, `setReversed(false)`/`setReversedDepth(false)` are accepted as clear operations, reversed depth enablement fails clearly because reversed depth buffers are unsupported, `Renderer.state.reset()`/`resetVertexState()`/`unbindTexture()` are accepted as no-op compatibility hooks, and raw WebGL capability, framebuffer, draw-buffer, program, MRT indexed blending, vertex-array binding, texture binding/upload/storage, and uniform-buffer state methods including `updateUBOMapping()`/`uniformBlockBinding()`/`bindBufferBase()` fail clearly. Remaining work is deeper WebGL state-buffer parity, which remains outside the scene-oriented contract. |
| `Renderer.debug.getShaderAsync()` clear failure | Low | `Renderer.debug.getShaderAsync()` validates scene, camera, and object inputs, then rejects with guidance because generated backend shader source is not exposed by the scene-oriented native renderer. Remaining work is none unless future native shader introspection becomes a supported API. |
| `Renderer.onDeviceLost` callback state | Low | `Renderer.onDeviceLost` is assignable callback state with a default no-op callback and clear validation for CommonRenderer compatibility. No remaining work under the current scene-oriented contract; native device-loss event delivery remains outside scope. |
| Low-level WebGL/WebGPU direct rendering hooks | Low | `Renderer.renderBufferDirect()`, `Renderer.renderObject()`, non-null `Renderer.setRenderObjectFunction()`, non-null `Renderer.setOutputRenderTarget()`/`setCanvasTarget()`, direct `Renderer.setTexture2D()`/`setTextureCube()`/`setTextureCubeDynamic()`/`setTexture3D()`/`setTexture2DArray()`, `Renderer.copyTextureToTexture3D()`, `Renderer.compute()`/`computeAsync()`, `Renderer.getArrayBufferAsync()`, `Renderer.resolveTimestampsAsync()`, and `Renderer.waitForGPU()` calls fail clearly as unsupported because this package does not expose WebGL buffer binding, renderer-internal render-object dispatch, direct material program dispatch, browser WebGL texture units, direct texture binding, backend texture-layer copies, backend-owned common-renderer output/canvas targets, WebGPU compute pipelines, storage-buffer readback, timestamp query pools, or direct GPU synchronization; `Renderer.getRenderObjectFunction()` returns `null`, `Renderer.setRenderObjectFunction(null)` is accepted as a clear operation, `Renderer.getOutputRenderTarget()`/`getCanvasTarget()` return `null`, and `Renderer.setOutputRenderTarget(null)`/`setCanvasTarget(null)` are accepted as clear operations. Remaining work is limited to additional clear failures for low-level rendering APIs that fall outside the scene-oriented contract. |
| Object ID/mask/normal/depth render modes | Low | Initial `options.renderMode: "mask"`, `"object-id"`, `"normal"`, and `"depth"` passes are supported for flat segmentation, normal output, and normalized depth grayscale, including base texture, alpha-map, and `alphaHash` cutouts plus object-id target reverse lookup metadata; regular-camera, ArrayCamera, and CubeCamera MRT-shaped targets can populate hinted secondary `"mask"`, `"object-id"`, `"normal"`, and `"depth"` texture attachments through explicit multi-pass readback; invalid render mode values fail clearly; direct conformance covers base-texture, alpha-map, and `alphaHash` cutouts across mask/object-id/normal/depth passes, post-processing bypass, object-id target reverse lookup metadata through `renderToTarget()`/`options.target` for regular, ArrayCamera, and CubeCamera output plus reusable `Renderer.setRenderTarget()` regular-camera output, metadata clearing on later color renders, and hinted auxiliary target attachments, the browser-reference generator and generated corpus cover simple mask and normal render-mode output, and renderer-only generated corpus covers object-id and depth output. Remaining work is native MRT integration. |

## P2 - Performance And Scale

| Task | Impact | Notes |
|---|---:|---|
| Persistent resource cache | High | Renderer instances reuse the wgpu device, compiled base pipelines, default textures/bind groups, cached static mesh vertex/index buffers, guarded per-slot write-updated mesh uniform buffers/bind groups, cached write-updated background texture bind groups, a guarded write-updated post-processing uniform buffer, cached guarded main color, transmission scene-color, and post-processing scratch textures, cached post-processing source bind groups, cached guarded readback buffers, cached non-default sampler handles, cached regular 2D prepared texture uploads, cached packed physical-layer uploads, cached environment-dependent CPU IBL maps, cached uploaded IBL GPU bind groups, cached common per-texture bind groups, cached combined AO/physical map bind groups, cached built-in material state override pipelines, cached custom WGSL material pipelines, the global BRDF LUT used by IBL precompute across renders, and bounded cache caps/eviction for renderer-owned content, bind-group, pipeline, scratch texture, and readback caches. Direct conformance verifies reusable renderers reflect mutated mesh transform, material color, material texture bytes, background texture bytes, scene/material/reflection-probe environment texture bytes, transmission scene-color changes, and post-processing scene-color/uniform changes across renders while these caches are active; unit coverage verifies bounded cache insertion retains requested entries; `pnpm -C packages/renderer run profile:animated` profiles reusable-renderer animated transform/material-update workloads. Remaining work is using profile data to tune incremental scene updates. |
| Incremental scene updates | High | Reusable `Renderer` instances now cache version-guarded static mesh, line, point, BatchedMesh packed-range geometry extraction, implicit non-indexed line/point source-index streams, non-instanced dashed-line expansion, material texture payload extraction, base and PBR texture sampler/transform state, material base color/opacity extraction, PBR/render-state color-like slot extraction, material render-state extraction, material scalar feature-slot extraction, point-billboard expansion, and sprite-billboard expansion across frames, so transform/material-only animation avoids re-reading stable position/normal/UV/index/group data, reallocating stable implicit line/point source indices, stable line-distance/dash expansion inputs, stable texture bytes, stable base/PBR texture sampler state, stable CSS/color-like material colors, stable alpha/blending/depth/stencil/side/shading-model state, stable physical/depth/distance scalar feature slots, and stable billboard UV/color/quad expansion while `BufferAttribute.needsUpdate`/version, `Texture.needsUpdate`/version, dashed-line draw/type/dash/line-distance changes, texture sampler/transform changes, material color/opacity/PBR color-slot changes, material render-state/scalar feature changes, and billboard transform/camera/size input changes still invalidate the relevant cache. Remaining work is profile-driven expansion beyond the current extraction caches. |
| GPU skinning/morphing path | Medium | CPU baking is simple and correct enough for stills, with invalid skinning matrix values, morph target influence values, and morph mode flags failing clearly, but expensive for dense animated characters. |
| Large scene memory budget tests | Medium | Scale regression tests render 1,936 separate mesh objects, a 2,048-group nested transform hierarchy with 256 visible meshes, a mixed 144-mesh raw-texture/light scene, 7,056 `InstancedMesh` instances, 4,096 colored point billboards, 225 unique raw textures, 169 unique encoded textures, a 512 x 512 raw material texture, a 512 x 512 RGBA output-size frame, load generated NodePerformanceTest-shaped glTF graphs with 10k nodes/meshes/materials/texture definitions, and cover the supported 64-light budget in CI; direct conformance covers clear failure for scenes with more than 64 visible non-ambient lights; and platform-specific scale budget notes are documented. Remaining work is broader production-scene ceilings beyond the current regression floors. |
| Parallel texture decode/precompute | Medium | CPU IBL irradiance and prefiltered-specular face precompute runs in scoped per-face workers with deterministic face ordering, top-level IBL irradiance, prefiltered-specular, and BRDF LUT stages are scheduled together before deterministic joins, native mesh preparation parallelizes geometry/material texture conversion across larger mesh lists while preserving source order and mesh-indexed errors, scene-level background texture plus environment-map preparation starts on scoped settings workers so decode/precompute can overlap with each other and validation, built-in mesh map conversion inside a single heavy mesh joins deterministic scoped workers, base map plus alpha-map conversion for sprite/point/line-style single objects overlaps in scoped workers, and single-mesh preparation starts surface/common texture conversion and physical-extension map conversion together in scoped workers before deterministic joins and packing. Remaining work is identifying additional non-IBL texture/environment precompute scheduling opportunities. |

## P2 - Node Loader Ergonomics

| Task | Impact | Notes |
|---|---:|---|
| Document Node loader setup | High | Node `GLTFLoader` setup is documented for relative paths, POSIX/Windows absolute paths, `file://` URLs, `FileLoader`/`fetch`, encoded image buffers, PNG/JPEG/WebP data URI and Blob URL images, GLB bufferView image handling, path resolution, and optional image polyfills. `test:docs` audits the setup guide links and verifies it names every exported loader helper/class from the public API index. Remaining work is keeping examples synced with future helper behavior changes. |
| Provide asset loading helpers | Medium | `loadGltfFromFile`, `loadVrmFromFile`, `loadVrmAnimationFromFile`, `applyVrmAnimation`, `createNodeGltfLoader`, `createEncodedImageTextureLoader`, `EncodedImageTextureLoader`, `installLocalFileFetch`, and `resolveLocalAssetPath` help Node loaders parse local glTF/GLB/VRM/VRMA files from relative paths, absolute paths, or `file://` URLs with plugin hooks, seek VRMA avatar animations to still-frame times through mixer `setTime()` or `update()` fallback from direct VRM/VRMA objects or loaded glTF wrapper objects, honor `updateVrm=false`, and expose renderer-supported encoded texture buffers from normalized relative/local absolute/`file://` helper roots and loader `setPath()` prefixes, PNG/JPEG/WebP data URIs, helper-normalized PNG/JPEG/WebP GLB bufferView images, and PNG/JPEG/WebP Blob URLs for custom flows; the encoded image loader supports both callback-style `load()` and promise-style `loadAsync()` plus optional `LoadingManager` item start/end/error notifications, with malformed helper boolean options, path values, option containers, plugin/loader callbacks, custom managers, avatar animation inputs/options, mixer/action contracts, glTF image metadata, remote asset/root URLs, unsupported encoded data URI/Blob URL image types, and unsupported external or embedded compressed image payloads failing clearly. Remaining work is future avatar workflow polish beyond current full-avatar fixture coverage. |
| Add end-to-end glTF/VRM examples | High | `examples/render-gltf.mjs` renders a local glTF/GLB asset fully in Node using the exported loader helpers, `examples/render-vrm.mjs` renders a local VRM avatar with optional VRMA animation through Pixiv loader plugins and the glTF-wrapper-friendly `applyVrmAnimation` flow, tests render committed geometry/material, vertex-color, morph-target, skinned, encoded-texture, and GLB bufferView-image glTF fixtures through the exported local glTF helper, tests load committed minimal/generated-humanoid synthetic, practical full-avatar, and real external VRM/VRMA extension fixtures through the VRM helpers, and the generated corpus includes a synthetic avatar-style toon/skinned scene. |

## Acceptance Criteria For "Fully Supports Three.js Scenes"

- Compatibility matrix exists and is kept versioned with every release.
- A representative scene corpus renders without crashes and with acceptable visual diffs against Three.js reference output.
- Unsupported features fail clearly or are explicitly marked unsupported in docs.
- `InstancedMesh`, core material classes, texture transforms/color spaces, layers, render order, multiple shadow lights, sprites, and common glTF/VRM scenes are covered by tests.
- Packaged npm artifacts install and render on Linux x64, Linux arm64, macOS x64, macOS arm64, and Windows x64.
- The public API remains scene-oriented: `render(scene, camera, options)`, `renderAsync(scene, camera, options)`, and target/readback output.
