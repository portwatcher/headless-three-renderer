# TODO: Three.js Scene Support Roadmap

## Goal

Render normal Three.js scene graphs in Node.js through the TypeScript adapter,
N-API, and the native `wgpu` renderer.

Non-goal: provide a WebGL context in Node.js or emulate the full
`THREE.WebGLRenderer` API/state machine. The public contract is scene input and
image/target output, not browser WebGL compatibility.

## Current Status

Current state: strong alpha for mesh-centric still rendering.

Already supported in the current implementation:

- `THREE.Scene` or `THREE.Object3D` root + `THREE.Camera` input, PNG/RGBA output with malformed scene/children/camera/userData/option containers, unsupported format values, invalid explicit/aspect-derived dimensions, and invalid camera clipping/matrix containers or values failing clearly, reusable renderer, and target-like raw RGBA output with texture image/source writeback.
- Meshes, skinned meshes, sprites, geometry groups, vertex colors, indices, normals, UVs, object visibility flags, object world transforms, and common `THREE.BatchedMesh` packed-geometry CPU expansion with per-instance matrices, colors, visibility flags, packed geometry groups/material arrays and group sort metadata when present, per-object sphere frustum culling, range-local internal sort centers, `sortObjects`, and `customSort` callback context/camera/list handling, with malformed geometry attribute/data containers, invalid geometry attribute/index/group/draw-range, malformed material containers, visibility, transform matrix values, malformed or out-of-bounds BatchedMesh geometry ranges, cached culling bounds, and culling/sort controls failing clearly.
- `THREE.InstancedMesh` CPU expansion with `instanceMatrix` and `instanceColor`, with invalid explicit instance counts and malformed instance matrix/color attributes failing clearly.
- `THREE.InstancedBufferGeometry` CPU expansion for mesh, point, and line geometry with common per-instance offset and color attributes, including Three.js' default `instanceCount = Infinity`, `meshPerAttribute` repeat values, dashed line expansion, and invalid explicit instance counts, malformed instanced attributes, or per-attribute repeat values failing clearly.
- CPU skinning and morph target baking before native rendering, with malformed mesh/skeleton containers, invalid skinning matrix values, malformed morph attribute containers, morph target influence values, and malformed morph mode flags failing clearly.
- `MeshBasicMaterial`, `MeshDepthMaterial` with supported depth-packing modes and clear failures for unsupported depth-packing constants, `MeshDistanceMaterial`, `MeshLambertMaterial`, `MeshMatcapMaterial`, `MeshNormalMaterial`, `MeshPhongMaterial`, `MeshToonMaterial`, `ShadowMaterial`, `MeshStandardMaterial`, and a substantial `MeshPhysicalMaterial` subset, including common main-pass mesh wireframe output.
- Base color, opacity, material visibility, premultiplied alpha, base/sprite/point/line/matcap/emissive/light/toon gradient/physical color map, supported background texture, and equirectangular/cube scene-environment and reflection-probe LDR IBL sRGB/linear color-space handling, output color-space controls, base color `flipY`, tangent-space and object-space normal maps, bump maps, metallic/roughness/specular maps, AO/light maps with selected UV channels, Phong specular maps with selected UV channels, toon gradient maps, alpha/displacement maps, alpha test with invalid values failing clearly, common material color/scalar/vector controls with malformed color containers and invalid values failing clearly, alpha hash, vertex-color and size-attenuation controls with invalid values failing clearly, transparency sorting, side/culling with clear failures for unsupported side constants, texture wrap modes, one-channel/two-channel/RGB/RGBA raw material/background texture payloads including byte, signed/unsigned normalized integer, packed 16-bit color, float, and half-float typed data, and clear failures for malformed raw texture channel layouts.
- Base color, supported background, sprite/point color and alpha, line, normal, bump, displacement, metallic/roughness, emissive, AO, light, specular, alpha, and physical-extension map texture UV transforms (`offset`, `repeat`, `rotation`, `center`, `flipY`, and explicit matrix), with malformed transform vector containers and invalid transform or transform-boolean values failing clearly and direct explicit-matrix conformance for base/background, sprite/point color and alpha, line, common mesh material maps, and current physical-extension maps.
- Base/background, normal/bump, displacement, metallic/roughness, emissive, AO/light, alpha, Phong specular, toon gradient, matcap color-map, and packed physical-extension texture-group sampler wrap modes, `magFilter`/`minFilter` (`NearestFilter`/`LinearFilter` family), generated mip chains for mipmap min filters, explicit raw mipmap chains for unpacked 2D material/background texture uploads, compatible native texture anisotropy, and clear failures for unsupported sampler constants, invalid mipmap controls, malformed explicit mipmap containers, or invalid anisotropy values, including direct wrap/filter conformance for iridescence factor/thickness maps.
- Material `depthTest`, `depthFunc`, `depthWrite`, `colorWrite`, `polygonOffset`, `alphaHash`, `alphaToCoverage` on 4x MSAA renders, `premultipliedAlpha`, stencil state, built-in blending modes (`NoBlending`, `NormalBlending`, `AdditiveBlending`, `SubtractiveBlending`, and `MultiplyBlending`), custom blend equations/factors in the main pass, and clear failures for unsupported render-state constants and invalid render-state boolean/numeric values.
- Material-local `clippingPlanes`, `THREE.ClippingGroup`, plus render-option global `clippingPlanes`, up to eight world-space planes with invalid plane values, invalid clipping controls, invalid `options.localClippingEnabled`, and over-budget inputs failing clearly, including local/group `clipIntersection`, group `clipShadows`, and `options.localClippingEnabled` opt-out.
- Clearcoat, sheen, anisotropy, scalar iridescence with factor/thickness maps, specular intensity/color, roughness-aware scene-color transmission/thickness, IOR, attenuation, and related physical material maps, with malformed physical color containers and invalid physical color/scalar/vector/range values failing clearly.
- Ambient, directional, point, spot, hemisphere, RectAreaLight approximation, and LightProbe diffuse lighting, up to 64 direct lights, with malformed light color/target containers, invalid light color/numeric/range/transform matrix values, and malformed LightProbe coefficients failing clearly, including generated browser-reference corpus coverage for combined point/spot lighting and finite RectAreaLight shading.
- Environment IBL from equirectangular or cube `scene.environment`/reflection probes, plus single shared material-level env maps for supported IBL material paths, including `MeshBasicMaterial` legacy combine/reflectivity/refraction handling and `MeshPhongMaterial`/`MeshLambertMaterial` legacy combine/reflectivity handling, with malformed scene-environment/reflection-probe values, malformed scene userData/reflection-probe hint containers/arrays, and invalid material-envMap scalar/rotation values failing clearly; the generated browser-reference corpus covers Phong plus shared Basic/Lambert material-level env maps.
- Scene background colors plus 2D, equirectangular, and raw or encoded six-face cube texture backgrounds, including `options.background: null` clearing, option background overrides ignoring scene background controls, `backgroundIntensity`, approximate texture `backgroundBlurriness`, `options.environmentIntensity`, equirectangular/cube `scene.backgroundRotation`/`options.backgroundRotation`, and equirectangular/cube `scene.environmentRotation`/`options.environmentRotation`; malformed scene/option background values, explicit option rotations, invalid background/environment control and rotation values, and unsupported background rotations fail clearly, with generated browser-reference corpus coverage for color background overrides.
- Render-option and render-target viewport/scissor rectangles in output pixel coordinates, plus 4x MSAA render-option/target sample counts resolved back to normal output and target readback buffers, with invalid rectangles, target scissor flags, and sample counts failing clearly.
- `THREE.ArrayCamera` sub-camera viewport composition for PNG, RGBA, and target output, plus `THREE.CubeCamera` six-face cube color/depth target rendering, including active mip target writeback and captured color texture reuse as cube background/environment inputs.
- Flat auxiliary `options.renderMode` passes for white masks, RGB object IDs, and view-space normals, with invalid render mode values failing clearly, generated browser-reference corpus coverage for simple mask plus normal render-mode output, and renderer-only generated corpus coverage for object-id output.
- Directional, spot, point, and up to four directional cascaded shadow maps for a single shadow-casting light; common PCF shadow options including bias, normal bias, `radius`, and PCF-irrelevant `blurSamples` inputs are accepted, with malformed shadow flags/containers and invalid shadow numeric/range values failing clearly; explicit mesh `material.shadowSide` face filtering, `alphaToCoverage` shadow-caster cutoff approximation, directional/spot/point sprite and point billboard shadow casting, mesh custom depth/distance shadow material alpha cutouts and displacement, and sprite/point billboard custom shadow material alpha cutouts are honored; additional shadow-casting lights plus malformed light userData/cascade hints fail clearly.
- ACES Filmic tone mapping, output color-space controls, simple post-processing with malformed containers and invalid effect values failing clearly, custom WGSL fragment bodies with malformed override values failing clearly, lines, and point billboards.
- `THREE.Sprite`/`SpriteMaterial` CPU billboard expansion with color, opacity, texture maps, scene fog, center, scale, rotation, size attenuation, layers, render ordering, directional/spot/point shadow casting, and WebGL-compatible unlit `receiveShadow` no-op behavior, with invalid billboard center/scale/rotation and size-attenuation values failing clearly and generated browser-reference corpus coverage for a mapped rotated billboard.
- Line material arrays with geometry groups, plus `LineDashedMaterial` dash/gap/scale patterns and custom `lineDistance` attributes for common line, line segment, line loop, and instanced line cases, with invalid line scalar values and malformed line-distance attributes failing clearly.
- Object/camera layer filtering for renderable objects and lights, plus native draw-order keys for group order, `renderOrder`, material id, WebGL material variant, transmissive/transparent buckets, projected geometry bounding-sphere z, object/insertion ties, `sortObjects`, and custom opaque/transparent sort callbacks with object/material/geometry/group render-item metadata, with invalid layer containers/masks, `renderOrder`, sort option, and reusable-renderer setter values failing clearly.
- `THREE.LOD` auto-update selection for the active camera, with invalid auto-update flags, camera zoom, malformed level entries, or level distance/hysteresis values failing clearly.
- `THREE.Fog` and `THREE.FogExp2` in mesh, shadow, sprite, point, and line material paths, using view-space depth fog with `material.fog` opt-out and malformed fog objects/color containers plus invalid fog color/parameter values failing clearly, including generated browser-reference corpus coverage for linear fog opt-out behavior.
- Packaged artifacts are built and install-verified in CI across Linux x64, Linux arm64, macOS x64, macOS arm64, and Windows x64.

Distance to the project goal:

- Ready for many static or pre-animated glTF/VRM-style render jobs.
- Not yet full Three.js scene compatibility.
- The remaining work is mostly breadth, edge-case parity, conformance testing, packaging hygiene, and performance, not one single missing renderer feature.

Treat the goal as achieved only when a published compatibility matrix and golden-image regression corpus pass across the supported platforms.

## P0 - Contract, Tests, And Packaging

| Task | Impact | Notes |
|---|---:|---|
| Add golden-image parity tests | High | `test/browser-reference/` renders the generated corpus with browser Three.js/WebGLRenderer as an offline reference generator, downloads a `manifest.json` describing the Three.js revision and fixture list, and `test:golden` validates that manifest before comparing saved PNG references when `HEADLESS_THREE_BROWSER_REFERENCE_DIR` is set. CI invokes the harness in its documented no-reference skip mode so the entry point stays wired. Production runtime remains Node + `wgpu`; remaining work is committing a platform-scoped reference corpus and turning the CI harness into a required golden comparison. |
| Build a curated scene corpus | High | Initial generated invariant corpus covers transparent materials, custom blend equations/factors, 4x-MSAA alpha-to-coverage, stencil-masked render state, option-level background overrides, ArrayCamera viewport composition, renderer-only CubeCamera face capture, render-option viewport/scissor clipping, global, material-local, and nested ClippingGroup planes, LightProbe Lambert and mixed lit-material diffuse lighting, point/spot light shading, RectAreaLight shading, MeshBasicMaterial wireframe output, MeshDepthMaterial depth and wireframe output, renderer-only MeshDistanceMaterial range and wireframe output, MeshNormalMaterial output, MeshMatcapMaterial map output, MeshToonMaterial gradient-map and alpha-map cutout output, ShadowMaterial receiver shadows and fog opt-out, mapped SpriteMaterial billboards, scene fog opt-out behavior, renderer-only signed raw material/background DataTextures, explicit texture-matrix/sRGB base-map sampling, linear output color-space conversion, simple mask, renderer-only object-id, and normal render-mode output, 2D, equirectangular, and renderer-only cube background textures, material-level env maps, custom grouped sort callbacks, skinning, morphs, avatar-style toon/skinned combinations, IBL, shadows, points/dashed lines/basic lines, BatchedMesh instance colors, per-object culling, and custom sorting, LOD/groups, and pathological geometry in CI; optional browser-reference comparison covers the WebGLRenderer-compatible generated fixtures and skips renderer-only fixtures that browser WebGL cannot reference; glTF tests cover local geometry/material loading, vertex-color attributes, morph targets, skinning attributes, encoded data-URI textures, GLB bufferView images, external image files, external buffer resolution, and synthetic committed VRM/VRMA extension fixtures in CI. Remaining work is committed glTF Sample Assets, real external VRM/VRMA fixtures, and committed browser-generated references. |

## P1 - Scene Graph And Object Coverage

| Task | Impact | Notes |
|---|---:|---|
| `InstancedBufferGeometry` edge cases | High | Basic mesh, point, line, and dashed-line CPU expansion supports common per-instance offset/color attributes, including Three.js' default `instanceCount = Infinity` and `meshPerAttribute` repeat values, with invalid explicit instance counts, malformed instanced attributes, or per-attribute repeat values failing clearly; remaining work is arbitrary custom vertex attributes and shader integration. |
| `THREE.BatchedMesh` parity | High | Common packed-geometry CPU expansion supports active visible instances with per-instance matrix and color textures, including visibility flags, packed geometry groups/material arrays and group sort metadata when present, common per-object sphere frustum culling with `perObjectFrustumCulled` opt-out, range-local internal sort centers, `sortObjects=false` instance-order draws, `customSort` callbacks with BatchedMesh `this` binding, render camera, and list validation, and malformed packed matrix/color texture containers, malformed/out-of-bounds geometry ranges, cached culling bounds, culling controls, and sort controls failing clearly. Remaining work is exact Three.js per-object frustum culling edge-case parity, broader source-group preservation edge cases, and a native multidraw or batched drawing path for performance. |
| `renderOrder` and sorting edge cases | High | Group order, `renderOrder`, material id, WebGL material variant, transmissive/transparent buckets, projected geometry bounding-sphere z, object/insertion ties, `sortObjects`, and custom opaque/transparent sort callbacks are honored, including object/material/geometry/group render-item metadata for grouped draws and source-object metadata for BatchedMesh-expanded draws, with invalid `renderOrder`, sort option, and reusable-renderer setter values failing clearly; the generated browser-reference corpus now includes a grouped opaque-sort callback fixture. Remaining work is exact browser-golden parity for broader callback render-item edge cases. |
| Sprites and billboards | High | `THREE.Sprite`/`SpriteMaterial` CPU billboards support center, scale, rotation, perspective size attenuation controls, opacity, scene fog with `material.fog` opt-out, texture and alpha maps with UV transforms, texture-map sRGB color-space decode, layers, render ordering, main-pass clipping, directional/spot/point shadow casting, WebGL-compatible unlit `receiveShadow` no-op behavior, and alpha-tested custom depth/distance shadow material cutouts; invalid billboard center/scale/rotation values fail clearly. Direct conformance covers directional, spot, and point-light shadow casting, custom depth/distance shadow cutouts, plus accepted `receiveShadow`, and the generated browser-reference corpus covers a mapped rotated billboard. Remaining work is broader sprite shadow-caster parity and exact WebGL edge-case parity. |
| Points material parity | Medium | `PointsMaterial` CPU billboards support size, opacity, perspective size attenuation, depth-independent orthographic sizing, selected per-point geometry UV channels when present, including distinct color-map and alpha-map channels, point-sprite map/alpha-map UVs otherwise, map/alpha-map UV transforms, sRGB color-space decode for maps, vertex colors, scene fog with `material.fog` opt-out, main-pass clipping, common instanced geometry offset/color attributes, directional/spot/point shadow casting from expanded billboards, WebGL-compatible unlit `receiveShadow` no-op behavior, and alpha-tested custom depth/distance shadow material cutouts; invalid size values fail clearly. Direct conformance covers perspective attenuation, orthographic size stability, point UV selection, accepted `receiveShadow`, directional, spot, and point-light shadow casting, and custom depth/distance shadow cutouts, and the generated browser-reference corpus covers instanced point billboards. Remaining work is hardware cap behavior, circular point-sprite edge cases, and exact WebGL edge-case parity. |
| Line material parity edge cases | Medium | Current lines support unlit color, opacity, material arrays with geometry groups, scene fog with `material.fog` opt-out, vertex colors, common instanced geometry offset/color attributes, alpha maps with selected `texture.channel` UVs, and `LineBasicMaterial.map` texture RGB/alpha with sRGB color-space decode, texture UV transforms, channel 0-3 UV selection, and distinct selected UV channels for color and alpha maps; `LineBasicMaterial.linewidth` and `LineDashedMaterial.linewidth` values greater than 1 expand to camera-facing quads. `LineDashedMaterial` dash/gap/scale patterns render for common non-instanced and instanced cases with custom `lineDistance` attributes, reconstructed map/alpha-map UVs, texture UV transforms including explicit matrices, selected `texture.channel` UVs including distinct color-map and alpha-map channels, dashed-map sRGB color-space decode, and interpolated vertex colors; invalid linewidth, dash, gap, scale, and line-distance attribute values fail clearly; the generated browser-reference corpus covers a simple dashed-line pattern. Remaining work is exact native/browser wide-line parity and broader line material option parity. |
| Scene background textures | Medium | 2D, equirectangular, and raw or encoded six-face cube texture background mappings are supported with output color-space conversion and approximate texture blurriness; option-level color background overrides, 2D background texture transforms, and equirectangular background rotation/intensity are covered by the generated browser-reference corpus, with renderer-only generated corpus coverage for raw cube background rotation. PMREM/CubeUV background mappings fail clearly. Remaining work is exact blur parity, newer mapping controls, and exact color-space parity. |
| Clipping planes | Medium | Render-option global planes, scene-level `THREE.ClippingGroup`, and material-local `clippingPlanes`/`clipIntersection` affect the main pass for meshes, sprites, points, and lines, up to eight active planes; invalid plane values, invalid clipping control values, invalid `options.localClippingEnabled`, and over-budget global/group/material plane combinations fail clearly; nested parent/child `ClippingGroup` union/intersection composition has direct conformance and renderer-only generated corpus coverage; the browser-reference generator applies fixture clipping controls to WebGLRenderer-compatible clipping fixtures; `material.clipShadows` and group `clipShadows` clip shadow-caster fragments in the shadow pass; and `options.localClippingEnabled: false` disables material-local planes while preserving global/group planes. Remaining work is alpha-to-coverage smoothing. |

## P1 - Materials And Textures

| Task | Impact | Notes |
|---|---:|---|
| Texture transform parity edge cases | Very high | Base color, supported background, sprite/point color and alpha, line, normal, bump, displacement, metallic/roughness, emissive, AO, light, specular, alpha, and physical-extension map transforms are supported, with malformed transform vector containers and invalid transform values failing clearly, including direct conformance for explicit base/background, sprite/point color and alpha, line, matcap, normal/bump, displacement, emissive, metallic/roughness, AO/light, Phong specular, alpha, and current physical-extension texture matrices. Direct conformance also covers explicit-matrix plus color-space interactions for current color-producing transform slots, and the generated browser-reference corpus covers explicit base-map matrix sampling with sRGB decode. Remaining work is exact browser golden-tested parity for broader texture slots. |
| Texture color-space parity | Very high | Base color, sprite, point, line, matcap, emissive, light, toon gradient, sheen color, physical specular color, supported background texture maps, and equirectangular/cube scene-environment, reflection-probe, and supported material-level LDR IBL inputs honor explicit `THREE.SRGBColorSpace`/`THREE.LinearSRGBColorSpace`; unsupported texture color-space/encoding values fail clearly; material and texture background output conversion supports `THREE.SRGBColorSpace`/`THREE.LinearSRGBColorSpace`, with unsupported output color-space values failing clearly; explicit texture matrices compose with color-space decode for current color-producing transform slots; base color maps honor `flipY`; and standard material shaders honor `premultipliedAlpha`. The generated browser-reference corpus covers explicit base-map matrix sampling with sRGB decode plus linear output color-space conversion. Remaining work is exact color-space handling for remaining texture slots and broader WebGLRenderer state parity. |
| Additional UV channel coverage | High | Base color maps, normal/bump maps, matcap color maps, displacement maps, metallic/roughness maps, `emissiveMap`, `aoMap`, `lightMap`, `alphaMap`, Phong `specularMap`, simple mesh base/alpha map pairs, point color/alpha maps with geometry UVs, line/dashed-line color and alpha maps, and current physical-extension maps honor `texture.channel` values 0-3 by routing selected UV attributes through the available native UV streams; malformed channel values, unsupported channel indices, and mixed non-primary channels outside currently multi-stream-capable simple mesh base/alpha, point, and line draws fail clearly. Remaining work is simultaneous arbitrary UV attribute bindings and future slots. |
| More texture slots | High | Scalar iridescence plus iridescence factor/thickness maps are supported, with direct conformance for explicit matrices, secondary UV selection, horizontal/vertical wrap modes, and nearest/linear sampler filtering; displacement-map conformance covers texture transforms, explicit matrices, sampler wrap/filter settings, secondary UV selection, and bias. Remaining work is deepening exact iridescence-map and displacement edge-case parity. |
| More material classes | High | Scene-level environment/reflection probes are the main supported IBL path, including direct conformance for `MeshPhongMaterial` specular environment reflection and `specularMap` masking. A single shared material-level reflection `envMap` is supported for `MeshBasicMaterial`, `MeshStandardMaterial`, `MeshPhysicalMaterial`, `MeshPhongMaterial`, and `MeshLambertMaterial` through the native IBL path with per-material intensity and shared rotation, while `envMap` properties on non-env-map material classes are accepted as no-ops; Basic legacy multiply/mix/add combine modes, `reflectivity`, and refraction mappings with `refractionRatio` are honored; Phong/Lambert legacy multiply/mix/add combine modes and `reflectivity` are honored for material env maps; common main-pass mesh wireframes render as triangle edges; the generated browser-reference corpus covers MeshDepthMaterial BasicDepthPacking and wireframe output, MeshNormalMaterial output, MeshMatcapMaterial map output, MeshToonMaterial gradient-map and alpha-map cutout output, ShadowMaterial receiver shadows and fog opt-out, Phong material-level envMap, and shared Basic/Lambert material-level envMap fixtures; renderer-only generated corpus covers MeshDistanceMaterial range and wireframe output; and unsupported material classes, invalid material-envMap scalar/rotation values, non-Basic refraction mappings, unsupported env-map combine values, multiple distinct material env maps, and multiple distinct material env-map rotations fail clearly. `Object3D.customDepthMaterial` and `customDistanceMaterial` are honored for mesh shadow caster alpha-tested and displacement material inputs, plus alpha-tested sprite/point billboard shadow cutouts; malformed or wireframe custom shadow materials fail clearly. `MeshDepthMaterial` has direct conformance for depth-packing variants, invalid packing values failing clearly, wireframe output, displacement-map edge cases, and base/alpha-map cutouts. `MeshDistanceMaterial` has direct conformance for distance range/reference controls, invalid range/reference values and malformed material userData/renderer hint containers failing clearly, wireframe output, base/alpha-map cutouts, and displacement-map UV channel selection. `MeshToonMaterial` has direct conformance for fallback bands, gradient maps, base-map UV channels, emissive-map UV channels, light-map secondary UVs, and alpha-map cutouts. `ShadowMaterial` covers the current transparent receiver path with color, opacity, scene fog, fog opt-out, and output conversion. Remaining work is deepening `ShadowMaterial` exact parity, `PointsMaterial` cap/circular billboard parity, `SpriteMaterial` shadow-caster parity, broader custom shadow material translation, `MeshToonMaterial` browser-golden edge-case parity, exact non-Basic material-level envMap/refraction parity, plus `LineDashedMaterial` edge cases. |
| Physical material parity gaps | High | Scalar iridescence and iridescence factor/thickness maps are approximated, scene-color transmission uses a roughness/IOR-aware filter with approximate dispersion, malformed physical color containers and invalid physical color/scalar/vector/range values fail clearly, and direct conformance covers transmission volume attenuation color/distance behavior. Remaining work is adding/spec-checking unsupported features, exact iridescence parity, exact attenuation parity against browser references, exact transmission mip/bicubic parity, and clearcoat/normal/specular BRDF edge cases against Three.js. |
| Blending modes and render state | High | `NoBlending`, `NormalBlending`, `AdditiveBlending`, `SubtractiveBlending`, `MultiplyBlending`, `CustomBlending` equations/factors including constant color/alpha factors, `depthTest`, `depthFunc`, `depthWrite`, `colorWrite`, `polygonOffset`, `alphaHash`, `alphaToCoverage` on 4x MSAA renders, `premultipliedAlpha`, transparent default depth writes, and stencil state are honored in the main pass; unsupported blending, custom blend equation/factor, depth function, stencil function, and stencil operation constants plus invalid render-state boolean/numeric values fail clearly. Direct conformance covers custom blending, stencil masking, and the current 4x-MSAA alpha-to-coverage path, and the generated browser-reference corpus covers those paths. Remaining work is exact alpha-to-coverage/sample-mask parity across WebGLRenderer states. |
| Shader material strategy | High | `ShaderMaterial`, `RawShaderMaterial`, NodeMaterial, and built-in material `onBeforeCompile` customizations fail clearly unless a headless WGSL fragment override is provided; malformed override values plus malformed material userData/renderer hint containers fail clearly. Current custom WGSL fragments are not Three.js shader parity. Remaining work is a fuller strategy for Three.js shader graph/material translation and `onBeforeCompile` parity. |
| Compressed texture path | Medium | KTX2/Basis/`THREE.CompressedTexture` inputs fail clearly with pre-decode guidance across material, background, scene-environment, and reflection-probe texture slots; array/3D texture inputs, cube/PMREM material texture-slot inputs, unsupported special raw texture type constants, browser image/canvas-like texture objects, and raw DataTexture inputs with mismatched lengths fail clearly across those slots; raw material/background half-float DataTextures decode binary16 texels, signed and unsigned integer DataTextures normalize texels for material/background and current environment/reflection-probe paths, and packed `UnsignedShort4444Type`/`UnsignedShort5551Type` DataTextures unpack color channels before RGBA8 upload. Remaining work is native compressed texture decode support and broader texture format support. |
| Mip/filter/aniso settings | Medium | Base/background, normal/bump, displacement, metallic/roughness, emissive, AO/light, alpha, Phong specular, toon gradient, matcap color-map, and packed physical-extension texture groups respect wrap modes plus `NearestFilter`/`LinearFilter`-family `magFilter`/`minFilter`, with direct coverage for iridescence factor/thickness map wrap and filter behavior; unsupported sampler constants, invalid anisotropy values, malformed texture source containers, and malformed explicit mipmap containers fail clearly; mipmap min filters generate native mip chains from the source level for supported material/background texture uploads; raw `DataTexture`-style explicit mipmap chains, including half-float levels, upload for unpacked 2D material/background texture slots; material/background sampler slots accept non-default texture anisotropy and use native sampler anisotropy when the effective sampler is linear-filtered, while explicit mipmap arrays still fail clearly for packed physical-extension maps and scene-environment/reflection-probe texture slots. Conflicting sampler settings, including anisotropy, between maps packed into one native texture fail clearly. Remaining work is deeper browser LOD parity. |

## P1 - Lighting, Shadows, And IBL

| Task | Impact | Notes |
|---|---:|---|
| Multiple shadow-casting lights | Very high | A single shadow-casting directional/spot/point light is supported; additional visible shadow-casting lights fail clearly. Remaining work is native multi-shadow-map rendering. |
| Shadow behavior parity | High | Common `shadow.camera`, rectangular directional/spot/cascaded map sizes, up to four directional cascades, square point-light cube-face maps, bias, normal bias, `shadow.radius` PCF scaling, PCF-irrelevant `shadow.blurSamples` inputs, cast/receive rules, explicit mesh `material.shadowSide` face filtering, texture alpha-tested casters, `alphaToCoverage` shadow-caster cutoff approximation, directional/spot/point sprite and point-billboard shadow casters, mesh custom depth/distance shadow material alpha cutouts and displacement, and directly covered sprite/point billboard custom shadow material alpha cutouts are supported; malformed shadow flags/containers, invalid shadow numeric/range values, non-square point-light shadow map sizes, and malformed light userData/cascade hint containers/values or over-budget cascade hints fail clearly. Remaining work is VSM-specific blur behavior if a VSM shadow mode is added, broader custom shadow material translation, transparency parity, and deeper golden-tested behavior. |
| `RectAreaLight` | Medium | One-sided finite-area approximation is supported for direct lighting, and the browser-reference generator initializes Three.js' RectAreaLight uniforms for a generated finite-area light fixture. Remaining work is exact Three.js LTC/LUT parity. |
| `LightProbe` support | Medium | Three.js `LightProbe` spherical harmonics contribute diffuse indirect lighting and honor visibility plus camera layer filtering, with invalid coefficient values failing clearly, direct conformance for visibility filtering plus Standard, Lambert, Phong, and Toon diffuse paths and camera layer filtering, and generated browser-reference corpus coverage for Lambert plus mixed Standard/Phong/Toon diffuse lighting. Remaining work is exact golden-tested parity with all material/environment combinations. |
| Environment/background rotation controls | Medium | `scene.environmentIntensity`, `options.environmentIntensity`, reflection-probe intensity, `scene.backgroundIntensity`, `options.backgroundIntensity`, `options.backgroundBlurriness`, approximate texture `backgroundBlurriness`, equirectangular/cube `scene.backgroundRotation`/`options.backgroundRotation`, and equirectangular/cube `scene.environmentRotation`/`options.environmentRotation` are supported for current background/environment texture mappings; explicit option rotations are validated independently of active maps, and invalid background color/control/rotation values, invalid environment control/rotation values, and unsupported background rotations fail clearly. Remaining work is exact blur parity and exact background/environment mapping controls. |
| PMREM compatibility | Medium | Cube reflection environment inputs are converted through the renderer's CPU IBL precompute path; direct conformance covers clear failures for refraction and PMREM/CubeUV scene environment, reflection-probe, and material-envMap inputs. Remaining work is deciding whether to accept Three.js PMREM outputs, prefiltered cubemaps, or keep CPU precompute from source environment maps. |

## P2 - Cameras, Targets, And Render Pipeline Features

| Task | Impact | Notes |
|---|---:|---|
| Array/cube camera support | Medium | `THREE.ArrayCamera` sub-cameras compose into PNG, RGBA, and target output using each sub-camera viewport, including target depth readback, and the generated browser-reference corpus covers ArrayCamera viewport composition; `THREE.CubeCamera` renders six RGBA cube target faces plus optional depth faces into `WebGLCubeRenderTarget.texture.image`/`source.data`, nonzero `activeMipmapLevel` writes the captured faces into the corresponding cube texture mip entry, and captured color textures can be reused as cube background/environment inputs, with renderer-only generated corpus coverage for face capture. Remaining work is PMREM/native environment-capture automation and exact WebGL cube viewport/face semantics. |
| Render target depth output | Medium | Single-output non-array targets with `depthTexture` populate RGBA8 normalized depth readback for plain depth targets, normalized scalar `Float32Array` data for `FloatType`, `Uint16Array` half-float data for `HalfFloatType`, scalar unsigned typed arrays for `UnsignedByteType`/`UnsignedShortType`/`UnsignedIntType` depth textures, and `UnsignedInt248Type` data with normalized depth24 in the high bits plus a zero stencil byte, using depth-tested visible geometry and updating depth texture image/source containers; malformed targets, target image containers, nested target texture/mipmap/source containers, unsupported explicit depth texture types, and unsupported format/type pairings fail clearly; direct conformance covers near/far ordering, texture writeback, scissor clipping, transparent default/explicit depth writes, base-texture and alpha-map alpha-test cutouts, and unsupported type/format validation. Remaining work is native hardware depth-texture/stencil readback and exact WebGL render-target depth semantics. |
| Multiple render targets | Medium | Single-output target texture objects, one-element `target.texture` arrays, `target.textures[0]`, and single-attachment MRT-shaped targets write back RGBA8 data by default, Alpha/Red/RG/RGB/RGBA target texture formats, normalized `Float32Array` data for `FloatType`, normalized signed/unsigned integer arrays for `ByteType`/`ShortType`/`IntType` and `UnsignedShortType`/`UnsignedIntType`, packed `Uint16Array` data for `UnsignedShort4444Type`/`UnsignedShort5551Type`, packed `Uint32Array` RGB9_E5 data for `UnsignedInt5999Type`, or `Uint16Array` half-float data for `HalfFloatType` color textures while top-level `target.data` remains RGBA8, including texture image/source containers; multiple color attachment targets, array/3D target texture objects, regular-camera cube target texture objects, and unsupported color target texture formats/types fail clearly. Remaining work is native MRT support for masks, normals, IDs, and deferred-style workflows. |
| MSAA controls | Medium | Render-option and target sample counts support 4x MSAA with resolve back to normal output and target readback buffers; material `alphaToCoverage` uses native MSAA alpha-to-coverage for 4x renders; unsupported sample counts fail clearly; the generated browser-reference corpus covers the current 4x-MSAA alpha-to-coverage path. Remaining work is broader WebGL sample-count/state parity. |
| Viewport/scissor support | Medium | Render-option viewport/scissor rectangles and `target.viewport`/`target.scissor` with `target.scissorTest` are supported in output pixel coordinates, with invalid rectangles and malformed `target.scissorTest` values failing clearly; the browser-reference generator applies fixture viewport/scissor options and the generated corpus covers a clipped render-option rectangle. Remaining work is exact WebGLRenderer state-machine parity and broader tiled-render ergonomics. |
| Object ID/mask/normal render modes | Low | Initial `options.renderMode: "mask"`, `"object-id"`, and `"normal"` passes are supported for flat segmentation and normal output, including base texture and alpha-map cutouts plus object-id target reverse lookup metadata; invalid render mode values fail clearly; direct conformance covers base-texture and alpha-map cutouts across mask/object-id/normal passes plus object-id target reverse lookup metadata, the browser-reference generator and generated corpus cover simple mask and normal render-mode output, and renderer-only generated corpus covers object-id output. Remaining work is MRT integration. |

## P2 - Performance And Scale

| Task | Impact | Notes |
|---|---:|---|
| Persistent resource cache | High | Reuse GPU buffers, textures, pipelines, IBL maps, and bind groups across renders. |
| Incremental scene updates | High | Avoid rebuilding every mesh/material/texture for animation frames. |
| GPU skinning/morphing path | Medium | CPU baking is simple and correct enough for stills, with invalid skinning matrix values, morph target influence values, and morph mode flags failing clearly, but expensive for dense animated characters. |
| Large scene memory budget tests | Medium | Scale regression tests render many meshes, many unique raw and encoded textures, and the supported 64-light budget in CI; direct conformance covers clear failure for scenes with more than 64 visible non-ambient lights; and platform-specific scale budget notes are documented. Remaining work is larger memory ceilings. |
| Parallel texture decode/precompute | Medium | IBL and image decode can dominate render latency. |

## P2 - Node Loader Ergonomics

| Task | Impact | Notes |
|---|---:|---|
| Document Node loader setup | High | Node `GLTFLoader` setup is documented for local files, `FileLoader`/`fetch`, encoded image buffers, PNG/JPEG/WebP data URI and Blob URL images, GLB bufferView image handling, path resolution, and optional image polyfills. Remaining work is keeping examples synced with future helper APIs. |
| Provide asset loading helpers | Medium | `loadGltfFromFile`, `loadVrmFromFile`, `loadVrmAnimationFromFile`, `applyVrmAnimation`, `createNodeGltfLoader`, `createEncodedImageTextureLoader`, `EncodedImageTextureLoader`, `installLocalFileFetch`, and `resolveLocalAssetPath` help Node loaders parse local glTF/GLB/VRM/VRMA files with plugin hooks, seek VRMA avatar animations to still-frame times, and expose renderer-supported encoded texture buffers from local files, PNG/JPEG/WebP data URIs, helper-normalized PNG/JPEG/WebP GLB bufferView images, and PNG/JPEG/WebP Blob URLs for custom flows, with malformed helper boolean options, path values, option containers, plugin/loader callbacks, custom managers, avatar animation inputs, glTF image metadata, and unsupported external or embedded compressed image payloads failing clearly. Remaining work is future avatar workflow polish around real external fixtures. |
| Add end-to-end glTF/VRM examples | High | `examples/render-gltf.mjs` renders a local glTF/GLB asset fully in Node using the exported loader helpers, `examples/render-vrm.mjs` renders a local VRM avatar with optional VRMA animation through Pixiv loader plugins and `applyVrmAnimation`, tests render committed geometry/material, vertex-color, morph-target, skinned, encoded-texture, and GLB bufferView-image glTF fixtures through the exported local glTF helper, tests load committed synthetic VRM/VRMA extension fixtures through the VRM helpers, and the generated corpus includes a synthetic avatar-style toon/skinned scene. Remaining work is committed glTF Sample Assets plus real external VRM/VRMA fixtures. |

## Acceptance Criteria For "Fully Supports Three.js Scenes"

- Compatibility matrix exists and is kept versioned with every release.
- A representative scene corpus renders without crashes and with acceptable visual diffs against Three.js reference output.
- Unsupported features fail clearly or are explicitly marked unsupported in docs.
- `InstancedMesh`, core material classes, texture transforms/color spaces, layers, render order, multiple shadow lights, sprites, and common glTF/VRM scenes are covered by tests.
- Packaged npm artifacts install and render on Linux x64, Linux arm64, macOS x64, macOS arm64, and Windows x64.
- The public API remains scene-oriented: `render(scene, camera, options)` and target/readback output.
