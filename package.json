{
  "name": "headless-three",
  "private": true,
  "version": "0.0.0",
  "description": "Headless Three.js ecosystem — monorepo root",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/portwatcher/headless-three-renderer.git"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r --stream build",
    "build:ts": "pnpm -r --stream build:ts",
    "test": "pnpm -r --stream test"
  }
}
